### 阶段三：前端台账式表单与数据流转落地开发配置方案

依据工程架构设计，本阶段核心任务是实现高密度参数的高效输入、状态管理及数据的前后端交互。以下为具体的落地开发步骤：

#### 1. 状态管理与数据总线配置 (Pinia)
**目标文件**：`frontend/src/store/parameterStore.ts` 与 `frontend/src/store/snapshotStore.ts`
* **构建参数状态机**：在 `parameterStore.ts` 中初始化状态对象，显式区分单洞（38个参数）与双洞（41个参数）的数据结构。
* **设定默认值保底**：将 12 个高级设置参数（规范推荐值/经验值）作为 Store 的初始状态硬编码写入。
* **定义状态流转方法**：开发 `updateParam`（单字段更新）、`overrideAll`（全量覆写，用于案例加载与 Excel 导入）等 Action。
* **建立快照管理库**：在 `snapshotStore.ts` 中配置 CRUD（增删改查）逻辑，管理快照的序列化对象，支持通过 `localStorage` 进行前端持久化。
* **扩展快照序列管理**：在 snapshotStore.ts 中增设多分区快照序列（Snapshot Sequence）的数据结构。以【分区起点里程】与【分区终点里程】为核心索引，支持对多个关联快照对象进行联合存储、排序与聚合调用，支撑后续的 3D 成果拼接。

#### 2. 表单分区分级与 UI 组件构建 (Element Plus)
**目标文件**：`frontend/src/components/ui/ParameterForm.vue`
* **物理隔离参数模块**：引入 `<el-collapse>` 折叠面板组件，将 38/40 个参数拆分为“地质水文参数”、“结构尺寸参数”、“材料属性参数”三个独立业务区块。
* **按层级披露数据**：
    * **基础输入区**：直接暴露核心参数与复选组件（通过 `<el-checkbox>` 或 `<el-switch>` 实现）。
    * **高级默认区**：折叠隐藏默认参数。用户展开后允许干预修改。
* **响应式表单校验**：结合 Element Plus 的 `FormRules`，为核心字段添加数值范围与格式拦截。

* **追加里程定位参数**：在“基础输入区”的首要位置，新增【分区起点里程】与【分区终点里程】数值输入框，并配置 FormRules 校验逻辑（如终点里程应大于起点里程），确保空间拼接基准数据的准确性。

#### 3. 典型案例一键赋值机制开发
**目标文件**：`frontend/src/components/ui/CaseSelector.vue`
* **封装案例库源数据**：在本地定义包含诸如“标准岩溶发育区双洞”、“高水压富水断层单洞”等工程案例的 JSON 集合。
* **UI 触发与联动**：开发下拉选择组件 `<el-select>`，监听 `change` 事件。当用户选中特定案例时，触发 `parameterStore.overrideAll(caseData)`，实现表单数据的瞬间全量覆盖。

#### 4. Excel 批量 IO 集成
**目标文件**：`frontend/src/utils/excelIO.ts`
* **引入核心库**：基于 `xlsx` (SheetJS) 封装读写类。
* **多分区模板适配**：调整空 Excel 模板的表头生成逻辑，嵌入跨分区（起终点里程）的列定义。
* **序列化快照批量生成**：在 excelIO.ts 中升级解析器。读取用户上传的跨分区 Excel 后，按里程区段自动进行数据切片，依次生成对应的状态字典，并批量封装为快照序列推送到 snapshotStore 存档。
* **开发下载模板功能**：根据当前的洞型（单/双洞），生成包含标准表头的空 Excel 模板，配置数据有效性提示供用户下载。
* **开发上传解析功能**：利用 `<el-upload>` 组件拦截文件读取，在 `excelIO.ts` 中将二进制文件解析为 JSON 字典，随后映射并推送到 `parameterStore` 中。导出功能同理，将当前状态机数据导出为 `.xlsx` 归档。

#### 5. 快照持久化视图与回溯流转
**目标文件**：依托 `frontend/src/components/ui/SnapshotSidebar.vue` 开发侧边栏快照列表组件
* **捕获快照动作**：在界面配置“保存快照”按钮。点击时，将 `parameterStore` 的当前状态深拷贝为 JSON，附加当前时间戳（`Date.now()`）、用户备注以及当前的安全系数/临界水头等计算结果。
* **快照回溯切换**：在列表 UI 中点击任意历史快照缩略图，将保存的 JSON 状态重新反序列化，并注入 `parameterStore`，表单与 3D 视图即可同步更新为当时的极限工况设定。
* **多分区模板适配**：调整空 Excel 模板的表头生成逻辑，嵌入跨分区（起终点里程）的列定义。

* **序列化快照批量生成**：在 excelIO.ts 中升级解析器。读取用户上传的跨分区 Excel 后，按里程区段自动进行数据切片，依次生成对应的状态字典，并批量封装为快照序列推送到 snapshotStore 存档。

**目标文件**：依托 `frontend/src/views/Dashboard.vue` 核心工作台视图
* **功能定位**：作为整体界面的骨架，将新建的快照侧边栏组件（SnapshotSidebar.vue）挂载到页面特定区域。当用户在侧边栏点击回溯时，由于底层的数据状态已被 parameterStore 覆写，依赖该 Store 的兄弟组件（即 ParameterForm.vue 和 Viewer3D.vue）会自动通过 Vue 的响应式机制完成同步更新，实现从表单数值到 3D 渲染效果的同步回溯。

#### 6. API 封装与前后端联调提交
**目标文件**：`frontend/src/api/index.ts`
* **Axios 实例配置**：设定 baseURL、请求拦截器（如组装 Bearer Token 鉴权头）与响应拦截器（统一处理 HTTP 异常）。
* **定义计算契约接口**：封装 `calculateDrainage(data)` 函数，目标路由对齐后端的 `POST /api/v1/calculate/drainage`。
* **触发计算指令**：在 UI 层的“开始计算”按钮上绑定防抖点击事件。提取 Store 中的完整参数组发起 POST 请求。接收到后端返回的间距、孔径等优化结果后，存入 Store，为下一阶段的三维空间坐标映射做好数据准备。


## 阶段3-前端启动与Debug联调工程方案

针对“前端台账式表单与数据流转”阶段的开发配置，为确保数据状态机（Pinia）与底层逻辑的准确映射，需建立标准化的本地启动规范与调试（Debug）链路。以下为详细可落地的工程实施步骤：

### 一、 本地双端环境启动与冒烟测试

在开始重负载表单调试前，必须确保前后端物理隔离环境的健康运行。

**1. 后端计算服务（Mock/计算端）启动**
* **操作指令**：在 `backend` 目录下，激活虚拟环境后执行 `uvicorn app.main:app --reload --port 8000`。
* **自检动作**：
  * 浏览器访问 `http://localhost:8000/docs`。
  * 在 Swagger UI 中手动构造一个包含 38/41 个参数的 JSON 体，向 `/api/v1/calculate/drainage` 发送 POST 请求。
  * **验收标准**：确认接口返回 HTTP 200，且能输出包含“间距”、“孔径”等预期结构的 JSON 数据，排除后端 Pydantic 模型定义错误（如触发 HTTP 422 校验失败）。

**2. 前端开发服务器启动**
* **操作指令**：在 `frontend` 目录下执行 `npm run dev`。
* **自检动作**：
  * 访问 Vite 提供的本地端口（通常为 `http://localhost:1420`）。
  * **验收标准**：页面正常渲染 Element Plus 的折叠面板组件，无控制台白屏报错。

---

### 二、 核心模块 Debug 策略与工具链

针对本阶段的 38/41 个重度交互参数，需采用结构化的排查路径。

**1. 数据状态机 (Pinia) 调试逻辑**
* **核心工具**：必须安装并启用浏览器插件 **Vue.js devtools**。
* **排查路径**：
  * **单点更新验证**：在 UI 界面拖拽滑块或修改输入框，打开 Vue Devtools 的 Pinia 面板，观察 `parameterStore` 中对应的字段是否实时精准更新，检查是否存在“响应式断开”（例如修改深层对象未触发视图更新）的问题。
  * **全量覆写验证**：点击“典型案例一键赋值”，在 Pinia 面板中检查 38/41 个参数是否瞬间全部变为案例预设值。
  * **保底值验证**：刷新页面，检查 Store 初始化的 12 个默认参数（如规范经验值）是否已正确挂载并反显在隐藏的“高级设置区”。

**2. 序列化与持久化 (快照功能) 调试逻辑**
* **排查路径**：
  * **存入测试**：点击保存快照，打开浏览器开发者工具 -> `Application` -> `Local Storage`（或 IndexedDB）。检查名为 `snapshots` 的 key 中是否写入了完整的 JSON 字符串，并核对时间戳和基础参数是否丢失。
  * **读取测试**：点击侧边栏的历史快照。在 Console 中打印 `JSON.parse` 后的数据字典，核对是否与保存时完全一致，并观察 UI 表单是否成功被逆向填充。
  * **多段联合读取测试**：勾选加载多个区段快照，在 Console 中深度检验聚合后 JSON 对象的里程连续性。重点核查各区段参数映射是否存在键值覆盖、里程缺失或空间坐标错位等异常边界情况。

**3. Excel 批量 IO 调试逻辑**
* **核心工具**：浏览器 Console 配合标准测试用例。
* **排查路径**：
  * 准备一份制造了“脏数据”（如应当填数字的地方填了字母、缺少必填项）的 Excel 文件。
  * 上传该文件，检查 `excelIO.ts` 中的解析逻辑是否抛出异常，并验证 Element Plus 的 `ElMessage.error` 是否能向用户准确弹出“第 X 行 Y 列数据格式错误”的阻断提示。
  * 测试空模板下载，确认导出的 `.xlsx` 文件表头与 `parameterStore` 的 Schema 严格对应。
  * 里程冲突排查路径：构造包含特定“脏数据”（如里程重叠、里程倒挂、关键参数越界）的多分区 Excel 模板进行上传测试。核验 excelIO.ts 的切片解析逻辑是否能精准识别并在界面抛出“里程区段冲突”的阻断提示，保障批量生成的快照序列数据无误。
---

### 三、 前后端接口网络联调 (Axios) 专项排查

当点击“开始计算”触发数据流转时，重点排查前后端的数据契约对齐情况。

**1. 请求拦截与日志打印**
* 在 `frontend/src/api/index.ts` 的 Axios 拦截器中注入 Debug 逻辑：
  ```typescript
  // 开发环境专用的请求拦截日志
  axiosInstance.interceptors.request.use(config => {
    console.group('🚀 [API Request]: ', config.url);
    console.log('Payload:', config.data);
    console.groupEnd();
    return config;
  });
  ```

**2. 经典异常类型与处置方案**
* **HTTP 422 Unprocessable Entity**：
  * **原因**：前端发送的 JSON 数据类型或字段名，与后端 `schemas.py` 中 Pydantic 定义的不一致（例如后端要求 `float`，前端发了 `string`；或漏传了必填字段）。
  * **处置**：打开 Chrome DevTools 的 `Network` 面板，查看引发 422 的具体 Request Payload，对照 Swagger 文档补齐或转换参数类型。
* **跨域拦截 (CORS Error)**：
  * **原因**：前端 5173 端口直接请求后端 8000 端口触发浏览器同源策略。
  * **处置**：在后端的 `main.py` 中确认 `CORSMiddleware` 已正确配置，并允许了前端的 Origin，或者在前端 `vite.config.ts` 中配置 server proxy 代理转发。
* **长耗时请求超时 (Timeout)**：
  * **原因**：排水计算模型逻辑复杂，处理时间超过 Axios 默认超时时间。
  * **处置**：在 Axios 实例创建时，将 `timeout` 调整为更宽裕的值（如 `30000` 毫秒），并确保前端在等待期间正确渲染全屏的 `v-loading` 加载动画，防止用户重复点击。

