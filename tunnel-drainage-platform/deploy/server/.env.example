# tunnel-drainage-platform/deploy/server/.env.example
# 生产环境配置参数模板

# 宿主对外暴露网络端口
HTTP_PORT=80
HTTPS_PORT=443

# 日志输出等级 (debug | info | warning | error)
LOG_LEVEL=info

# 数据库持久化路径 (容器内部路径)
DB_PATH=/app/data/tunnel_params.db

# 系统运行环境
APP_ENV=production
