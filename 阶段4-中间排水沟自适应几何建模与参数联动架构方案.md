# 阶段4-中间排水沟自适应几何建模与参数联动架构方案

## 1. Objectives (目标与范式定义)

### 1.1 业务与技术需求解构
在隧道工程排水系统设计中，路面排水系统根据工程地质、水文条件、纵坡坡度及交通规范，通常分为两种典型断面形式：
1. **三沟式断面（含中央深排水沟 + 左右侧边沟）**：适用于地下水丰富、渗水量大、需设置中心深埋主排水管/中央水沟的高水压隧道。
2. **双侧沟式断面（仅左右侧边沟，无中央排水沟）**：适用于中低渗水量或特定路面结构隧道，路面中央平整连续，水流通过路面横坡汇入两侧排水边沟。

根据《隧道工程施工图》与《排水系统构造大样图》标准工程规范：
- **排水沟**（两侧边沟及中央深排水沟）均为纯混凝土/钢筋混凝土构筑的水流通道，**水沟槽身内不铺设管道**；
- **水沟槽底与侧壁严禁穿模二衬**，各水沟底标高与外壁必须严格受控于二次衬砌内轮廓以内（保留安全距离）；
- **纵向排水盲管**（左右各 1 根）敷设于**初期支护内侧、二次衬砌外侧的隧道拱脚位置**，用于收集环向盲管汇水；
- **横向排水盲管**连接拱脚纵向排水管，以**固定 3% 下倾坡度**穿过二次衬砌结构。无论是否有中间水沟，**严禁取消横向穿衬管**；在有中间水沟时引水排入中央深排水沟侧壁，在无中间水沟时通过自适应移动侧沟位置与标高，使横向管顺畅穿衬排入侧边沟；
- **环向排水盲管**敷设于初期支护与二次衬砌交界面（背水面），顺马蹄形拱弧向下延伸，**到达拱脚处的纵向排水管即刻终止收口**，严禁向下延伸侵入路面或破坏横向排水管的 3% 坡度。

为了使平台具备全工况自适应的可视化表达能力，需要在前端引入**“是否有中间排水沟”**（`has_central_ditch`）控制参数，实现从 UI 输入、数据持久化、Excel 导入导出、参数台账对标到 3D 视口路面、水沟及排水管网几何拓扑的端到端联动，同时严格保持与后端力学计算引擎的隔离。

### 1.2 核心目标清单
1. **前端表单交互增强**：在 `ParameterForm.vue` 结构尺寸参数区新增直观的双选控件（设置-三沟式 / 不设置-双侧沟），提供即时响应式参数绑定。
2. **3D 几何与管网引擎动态重构**：
   - 在 `TunnelGenerator.ts` 中依据参数动态分支构建“有中间排水沟”与“无中间排水沟”两种路面与排水槽拓扑结构，引入平直段网格均匀细分，杜绝 Earcut 扇形剖分与对角线伪影；水沟底面与二衬内弧保留安全间隙，严禁穿模；
   - 在 `math.ts` 中修正 `HorseshoeArcCurve`，使环向盲管平顺收口于拱脚纵向管坐标，消除伸入水沟底的折角畸变；
   - 在 `DrainagePipeGenerator.ts` 中依据工程施工图重构空间管网：水沟槽身保持纯净无管道，纵向盲管置于二衬外/初支内拱脚，横向排水管以严格 3% 坡度穿透二衬自适应排入中央水沟侧壁（三沟式）或排入自适应迁移后的侧边沟（双侧沟式），严禁取消横向管。
3. **全链路影响范围闭环**：
   - `parameterStore.ts` 与 `snapshotStore.ts` 状态机无缝扩展与向下兼容。
   - `excelIO.ts` 批量导入模板生成、Excel 导入解析（含严格布尔语义转换，防范 JS 假真值陷阱）及快照导出的双向映射。
   - `ParameterDatabase.vue` 参数台账管理与多维对标分析的高亮比对展示。
   - `Viewer3D.vue` 与 `CompareView.vue` 多维视口实时随动。
4. **计算引擎严格解耦**：该参数作为纯 3D 几何与工程台账属性，不参与后端渗流与衬砌力学平衡计算，后端通过 Pydantic `extra="ignore"` 实现平稳过滤。

---

## 2. Constraints (约束与设计边界)

1. **计算引擎绝对零侵入约束 (Engine Isolation)**：
   - 后端水力学解析公式（Darcy 渗流、Manning 管道流速）及二衬受力极限承载力计算模型保持纯净，不得因新增字段引发接口报错或计算逻辑扰动。
2. **二衬绝对防穿模约束 (Zero Lining Penetration)**：
   - 中间深排水沟槽底与左右侧水沟底面及外壁必须严格受控于二衬仰拱内表面以内（保持 $\ge 0.03\text{m} \sim 0.05\text{m}$ 安全厚度），严禁任何水沟实体向下侵入二衬内轮廓。
3. **拓扑连续性与 Earcut 零退化约束 (Topological Integrity)**：
   - 在无中间排水沟模式下，路面中央顶沿为连续平直标高线，不得残留中央下沉凹槽或多余凸点，且中央平直段需进行对称节点细分采样，消除跨越 $8.2\text{m}$ 产生的大跨度极狭长扇形三角形；
   - 排水沟几何体 `ditchMesh` 仅生成左右 2 个侧沟 U 型槽，绝对不能出现空顶点、孤立面片或内部隔板。
4. **工程施工图物理一致性与管网保留约束 (Construction Drawing Consistency)**：
   - 水沟槽体内部严禁生成管道；
   - 纵向排水管必须严格锚定于隧道左右拱脚交界面（二衬外、初支内）；
   - **严禁在双侧沟工况下取消横向排水管**；横向排水管必须从拱脚纵向管引出，以固定 $3\%$ 下倾坡度穿透二衬排入水沟；
   - 环向盲管末端精准收口于拱脚纵向管，保持马蹄形几何连续性。
5. **向下兼容与历史数据容错约束 (Backward Compatibility)**：
   - 针对历史数据库记录、旧版快照或未包含 `has_central_ditch` 字段的 Excel 文件，系统统一设置默认回退值 `has_central_ditch = true`（默认保持原三沟式标杆工况），保证历史数据加载 100% 零崩溃。
6. **渲染层级与深度写入规范 (Render Pipeline Consistency)**：
   - 保持 `roadMesh`（`renderOrder: 10`）、`ditchMesh`（`renderOrder: 12`）与 `pipeMeshes`（`renderOrder: 50`）的 PBR 材质参数、`depthWrite: true` 及 `polygonOffset` 设定，确保双范式（Cyber/Studio）下的透明光影质感。

---

## 3. Architecture (系统架构与数据流模型)

### 3.1 端到端数据流架构模型 (Data Flow Diagram)

```mermaid
flowchart TD
    subgraph UI_Layer ["用户交互与台账层"]
        A1["ParameterForm.vue<br/>(中间排水沟单选开关)"] -->|updateParam| S1["parameterStore.ts<br/>(activeTunnelState)"]
        A2["excelIO.ts<br/>(Excel 批量导入)"] -->|parseUploadFile| S2["snapshotStore.ts<br/>(Snapshot 序列)"]
        A3["ParameterDatabase.vue<br/>(工程入库 / 对标分析)"] <-->|REST API| DB[(SQLite: database/parameters)]
    end

    subgraph Core_State ["核心状态机与快照流"]
        S1 -->|响应式发布 / 脏标记| V1["Viewer3D.vue<br/>(renderSceneData)"]
        S2 -->|extractSnapshotValue| V1
        S1 -.->|计算请求 (自动忽略)| BE["Backend Calculate Engine<br/>(Pydantic: extra='ignore')"]
    end

    subgraph ThreeJS_Engine ["3D 几何建模与管网渲染层"]
        V1 -->|传递 has_central_ditch| TG["TunnelGenerator.ts<br/>(createInternalRoadAndDitchGeometry)"]
        V1 -->|传递 has_central_ditch| DPG["DrainagePipeGenerator.ts<br/>(updateLongPipes / updateLatPipes)"]
        V1 --> MC["math.ts<br/>(HorseshoeArcCurve 拱脚收口)"]
        
        TG -->|has_central_ditch === true| B1["【三沟主体分支】<br/>• roadShape: 顶沿双边槽+中央凹槽避让<br/>• ditchShapes: 3个无管道 8-Vertex U型槽(防穿模)"]
        TG -->|has_central_ditch === false| B2["【双侧沟主体分支】<br/>• roadShape: 顶沿双边槽+中央平整细分<br/>• ditchShapes: 2个自适应拱脚承接U型槽(防穿模)"]
        
        DPG -->|has_central_ditch === true| P1["【三沟管网分支】<br/>• 纵向管: 左右拱脚(二衬外初支内)共2条<br/>• 横向管: 拱脚向中央水沟侧壁以3%坡度穿二衬汇流"]
        DPG -->|has_central_ditch === false| P2["【双侧沟管网分支】<br/>• 纵向管: 左右拱脚(二衬外初支内)共2条<br/>• 横向管: 严禁取消，自拱脚向侧边沟以3%坡度穿二衬排入"]

        B1 --> R1["Extrude & toNonIndexed<br/>(无封顶面 + 正交面法线)"]
        B2 --> R1
        
        R1 --> M1["roadMesh (renderOrder: 10)"]
        R1 --> M2["ditchMesh (renderOrder: 12)"]
        P1 --> M3["pipeMeshes (renderOrder: 50)"]
        P2 --> M3
    end
```

---

### 3.2 影响范围与模块矩阵 (Impact Scope Matrix)

| 模块 / 文件路径 | 影响性质 | 改造内容与职责 |
| :--- | :--- | :--- |
| `frontend/src/store/parameterStore.ts` | **状态契约扩展** | 扩展 `SingleTubeParams` 与 `DoubleTubeParams` 接口，定义 `has_central_ditch: boolean`（默认 `true`）；在 `overrideAll` 中加入兼容回退。 |
| `frontend/src/store/snapshotStore.ts` | **快照持久化** | 确保快照提取函数 `extractSnapshotValue` 能够正确提取布尔型 `has_central_ditch`，在工况切片与回溯中维持拓扑配置。 |
| `frontend/src/components/ui/ParameterForm.vue` | **交互界面注入** | 在“📏 结构尺寸参数”区域添加 `中间排水沟(3D专用)` 的 Radio 控件，触发实时响应与脏标记。 |
| `frontend/src/utils/math.ts` | **几何曲线收口** | 修正 `HorseshoeArcCurve`：环向盲管沿初支内壁马蹄形圆弧延伸至拱脚交界点 $( \pm X_{\text{long\_foot}}, Y_{\text{long\_foot}} )$ 即刻收口，剔除伸入水沟底的折角延伸。 |
| `frontend/src/components/three/TunnelGenerator.ts` | **3D主体几何重构** | 1. 构造函数接收 `has_central_ditch`；<br/>2. 建立三沟/双侧沟拓扑分支；<br/>3. 双侧沟平直段补充节点细分；<br/>4. 水沟底面严密校验仰拱内轮廓，严禁穿模二衬。 |
| `frontend/src/components/three/DrainagePipeGenerator.ts` | **3D管网拓扑重构** | 1. 彻底移出水沟内管道；<br/>2. 纵向管固定定位至两侧拱脚初支/二衬交界处；<br/>3. **严禁取消横向管**：以固定 3% 坡度穿二衬自适应排入中央水沟侧壁（三沟）或侧边沟（双侧沟）。 |
| `frontend/src/components/three/Viewer3D.vue` | **视口管线联动** | 从当前表单或 Snapshot 中解包 `has_central_ditch`，实例化 `TunnelGenerator` 与 `DrainagePipeGenerator` 并驱动场景重绘。 |
| `frontend/src/utils/excelIO.ts` | **导入导出与类型安全** | `fieldMapping` 补充中英映射；`downloadTemplate` 增加示例；`parseUploadFile` 引入 `parseBooleanCell` 规避假真值陷阱；导出时标准化格式化。 |
| `frontend/src/views/ParameterDatabase.vue` | **台账与对标** | `PARAM_META_DICT` 增加元数据说明；在对标抽屉中高亮比对各工程配置差异，旧数据缺失时平稳回退。 |
| `frontend/src/views/CompareView.vue` | **分屏对比联动** | 通过 `Viewer3D` 实例接收不同快照工况，支持直观分屏对比“三沟式 vs 双侧沟式”空间视觉差异。 |
| `backend/app/models/schemas.py` | **后端零侵入** | 依托 `ConfigDict(extra="ignore")` 机制，前端上报的 `has_central_ditch` 自动被计算引擎安全旁路。 |

---

### 3.3 3D 路面、水沟与管网自适应几何数学模型

#### 3.3.1 空间基准标高与拱脚关键点解算
设隧道等效内半径为 $r$，衬砌外轮廓半径为 $r_2$，衬砌厚度 $t = \max(0, r_2 - r)$。
- 三心圆内轮廓基准：$R_1 = 1.05r, R_2 = 0.65r, R_3 = 1.80r$
- 衬砌背水面（初支内壁）外轮廓基准：$R_{1,\text{outer}} = R_1 + t, R_{2,\text{outer}} = R_2 + t, R_{3,\text{outer}} = R_3 + t$
- 侧墙圆心偏移：$\Delta x = R_1 - R_2, \Delta y = \sqrt{(R_3 - R_2)^2 - \Delta x^2}$
- 隧道下沉高差与仰拱圆心标高：$H_{\text{side}} = \max(0, 0.7 \cdot 2.1r - R_1 + \Delta y - R_3), \quad y_{\text{invertCenter}} = -H_{\text{side}} + \Delta y$
- 拱脚交界面三角投影常数：
  $$\cos\theta_{\text{foot}} = \frac{\Delta x}{R_3 - R_2}, \quad \sin\theta_{\text{foot}} = \frac{\Delta y}{R_3 - R_2}$$
- **拱脚纵向排水管空间轴心坐标**：
  $$X_{\text{long\_foot}} = (R_3 + t) \cdot \frac{\Delta x}{R_3 - R_2} = \Delta x + R_{2,\text{outer}} \cdot \cos\theta_{\text{foot}}$$
  $$Y_{\text{long\_foot}} = y_{\text{invertCenter}} - (R_3 + t) \cdot \frac{\Delta y}{R_3 - R_2} = -H_{\text{side}} - R_{2,\text{outer}} \cdot \sin\theta_{\text{foot}}$$
  （左右拱脚对称坐标分别为 $-X_{\text{long\_foot}}$ 与 $+X_{\text{long\_foot}}$，敷设于二衬外、初支内交界面）
- **路面与水沟几何标高与防穿模包络**：
  - 路面顶标高：$y_{\text{road}} = y_{\text{invertCenter}} - (R_3 - 0.8\text{m})$
  - **二衬仰拱内表面安全包络线**：在任意横向位置 $x$，二衬仰拱内壁标高为 $y_{\text{lining\_inner}}(x) = y_{\text{invertCenter}} - \sqrt{\max(0, R_3^2 - x^2)}$
  - **中央深排水沟底标高**：$y_{\text{bot\_central}} = y_{\text{invertCenter}} - R_3 + 0.05\text{m}$（严格高出仰拱内底 $5\text{cm}$，严禁穿模二衬），槽宽区间 $[-0.35\text{m}, +0.35\text{m}]$
  - **侧边水沟底标高（三沟工况）**：$y_{\text{bot\_side}} = y_{\text{road}} - 0.3\text{m}$，侧沟外壁 $X_{\text{sideRight}} = \sqrt{\max(0, R_3^2 - (y_{\text{invertCenter}} - y_{\text{bot\_side}})^2)} - 0.05\text{m}$（留出 $5\text{cm}$ 安全防穿模间隙）

```
【断面几何与拓扑示意图】

1. 三沟式断面 (has_central_ditch = true)
   初支背水面:         /================== (环向盲管) ==================\
   拱脚管位:         (○) 拱脚纵向管                           (○) 拱脚纵向管
                      \  (3%坡度穿衬横向管)             (3%坡度)  /
   路面与水沟:  [-W] === [左侧边沟] ========= [中央深水沟] ========= [右侧边沟] === [+W]
                           |_|                    |_|                   |_|
                      (槽身无管道)          (侧壁开孔排入)          (槽身无管道)
   二衬内轮廓:  ~~~~~~~~~~~~~~~~~~~~~~~~~(严禁水沟穿模二衬)~~~~~~~~~~~~~~~~~~~~~~~~~

2. 双侧沟式断面 (has_central_ditch = false)
   初支背水面:         /================== (环向盲管) ==================\
   拱脚管位:         (○) 拱脚纵向管                           (○) 拱脚纵向管
                      \  (3%坡度穿衬排入)               (3%坡度)  /
   路面与水沟:  [-W] === [自适应侧沟] ==== (均匀细分平直路面) ==== [自适应侧沟] === [+W]
                           |_|                                          |_|
                      (槽身无管道)                                  (槽身无管道)
   二衬内轮廓:  ~~~~~~~~~~~~~~~~~~~~~~~~~(严禁水沟穿模二衬)~~~~~~~~~~~~~~~~~~~~~~~~~
```

#### 3.3.2 状态 A：三沟式拓扑构建算法 (`has_central_ditch === true`)
1. **路面多边形 `roadShape` (顶沿倒序扫描)**：
   - 沿 $X$ 轴从 $+halfRoadW$ 扫描至 $-halfRoadW$；
   - 依次切入右侧边沟槽口：`addDitchNotch(sideRightX, sideRightXInner, yBot_side)`；
   - 切入中央水沟槽口：`addDitchNotch(centralRightX, centralLeftX, yBot_central)`；
   - 依次切入左侧边沟槽口：`addDitchNotch(sideLeftXInner, sideLeftX, yBot_side)`；
   - 闭合至 $-halfRoadW$。
2. **水沟多边形 `ditchShapes` (3 个单圈 8 顶点 CCW U 型槽，纯混凝土实体，内部无管道，防穿模)**：
   - `ditchShapes.push(createUShape(centralLeftX, centralRightX, roadY, yBot_central, 0.05));`
   - `ditchShapes.push(createUShape(sideLeftX, sideLeftXInner, roadY, yBot_side, 0.05));`
   - `ditchShapes.push(createUShape(sideRightXInner, sideRightX, roadY, yBot_side, 0.05));`
3. **排水管网 `DrainagePipeGenerator` 拓扑**：
   - **环向盲管 `ringMesh`**：沿拱弧贴初支延伸至左右拱脚 $(\pm X_{\text{long\_foot}}, Y_{\text{long\_foot}})$ 收口；
   - **纵向排水管 `longMesh`**：在左右拱脚 $(\pm X_{\text{long\_foot}}, Y_{\text{long\_foot}})$ 生成 2 条纵向泄水主管（水沟内不放管道）；
   - **横向排水管 `latMesh`**：从左右拱脚纵向管引出，向中央水沟侧壁以固定 $3\%$ 下倾坡度穿透二衬排入，避免中心重叠对穿：
     - 起点：$(\pm X_{\text{long\_foot}}, Y_{\text{long\_foot}}, z_i)$；
     - 终点：$(\pm 0.35\text{m}, Y_{\text{long\_foot}} - 0.03 \cdot (X_{\text{long\_foot}} - 0.35\text{m}), z_i)$。

#### 3.3.3 状态 B：双侧沟式拓扑构建算法 (`has_central_ditch === false`)
1. **侧水沟自适应位置与标高动态迁移 (Adaptive Side Ditch Placement)**：
   - 为确保 3D 视口演示中横向排水盲管的直观连通与物理合理性，并严格遵循**严禁取消横向穿衬管**的原则：
   - 双侧沟工况下，侧水沟向拱脚承接区合理外移，使其紧邻二衬拱脚内侧：
     $$X_{\text{sideRight\_dual}} = X_{\text{long\_foot}} - 0.05\text{m}, \quad X_{\text{sideRightInner\_dual}} = X_{\text{sideRight\_dual}} - 0.40\text{m}$$
     $$X_{\text{sideLeft\_dual}} = -X_{\text{long\_foot}} + 0.05\text{m}, \quad X_{\text{sideLeftInner\_dual}} = X_{\text{sideLeft\_dual}} + 0.40\text{m}$$
   - **自适应防穿模沟底标高**：
     $$y_{\text{bot\_side\_dual}} = y_{\text{invertCenter}} - \sqrt{\max(0, R_3^2 - X_{\text{sideRight\_dual}}^2)} + 0.05\text{m}$$
     确保槽底严格高出二衬内弧 $5\text{cm}$，严禁穿模。
2. **路面多边形 `roadShape` (顶沿倒序扫描与细分防退化)**：
   - 沿 $X$ 轴从 $+halfRoadW$ 扫描至 $-halfRoadW$；
   - 切入右侧自适应沟槽：`addDitchNotch(X_sideRight_dual, X_sideRightInner_dual, yBot_side_dual)`；
   - **中央平直段节点均匀插值**：在 $x \in [X_{\text{sideLeftInner\_dual}}, X_{\text{sideRightInner\_dual}}]$ 区间内，均匀插入 $N=8$ 个离散采样点，保持高程 $y = \text{roadY}$，对齐底沿圆弧网格密度，彻底消除 Earcut 扇形三角形退化；
   - 切入左侧自适应沟槽：`addDitchNotch(X_sideLeftInner_dual, X_sideLeft_dual, yBot_side_dual)`；
   - 闭合至 $-halfRoadW$。
3. **水沟多边形 `ditchShapes` (2 个自适应单圈 8 顶点 CCW U 型槽，纯混凝土实体，内部无管道)**：
   - `ditchShapes.push(createUShape(X_sideLeft_dual, X_sideLeftInner_dual, roadY, yBot_side_dual, 0.05));`
   - `ditchShapes.push(createUShape(X_sideRightInner_dual, X_sideRight_dual, roadY, yBot_side_dual, 0.05));`
   - 不压入中央水沟 Shape。
4. **排水管网 `DrainagePipeGenerator` 拓扑自适应 (横向管完整保留)**：
   - **环向盲管 `ringMesh`**：保持在左右拱脚收口；
   - **纵向排水管 `longMesh`**：保持左右拱脚 2 条；
   - **横向排水管 `latMesh`**：**严禁取消**。从左右拱脚纵向管引出，以固定 $3\%$ 下倾坡度穿透二衬排入自适应侧沟外壁：
     - 起点：$(\pm X_{\text{long\_foot}}, Y_{\text{long\_foot}}, z_i)$；
     - 终点：$(\pm X_{\text{sideRight\_dual}}, Y_{\text{long\_foot}} - 0.03 \cdot |X_{\text{long\_foot}} - X_{\text{sideRight\_dual}}|, z_i)$。

---

## 4. Work Breakdown Structure (实施工作分解结构)

### WBS 1: 状态机与数据类型扩展 (`parameterStore.ts` & `snapshotStore.ts`)
- **1.1** 在 `parameterStore.ts` 中的 `SingleTubeParams` 与 `DoubleTubeParams` 添加字段定义：
  ```typescript
  has_central_ditch: boolean; // 是否设置中间排水沟 (3D视口与台账专用)
  ```
- **1.2** 在 `singleParams` 与 `doubleParams` 的默认初始化对象中注入 `has_central_ditch: true`。
- **1.3** 在 `overrideAll` 数据规整逻辑中补充：
  ```typescript
  has_central_ditch: data.has_central_ditch !== undefined ? Boolean(data.has_central_ditch) : true
  ```
- **1.4** 验证 `snapshotStore.ts` 中的 `extractSnapshotValue` 正常支持提取布尔参数。

---

### WBS 2: 3D 几何建模引擎拓扑分支重构 (`TunnelGenerator.ts`)
- **2.1** 更新 `TunnelGenerator` 构造函数入参签名，在末尾添加 `has_central_ditch: boolean = true` 并在类私有成员中保存：
  ```typescript
  constructor(
    type: TunnelType,
    start_chainage: number,
    end_chainage: number,
    r: number,
    aspect_ratio: number = 1.0,
    D_spacing: number = 30.0,
    r1: number = 5.5,
    r2: number = 6.5,
    rg: number = 8.0,
    c: number = 50.0,
    delta_l_min: number = 1.0,
    has_central_ditch: boolean = true
  )
  ```
- **2.2** 在 `createInternalRoadAndDitchGeometry` 方法签名中增加 `has_central_ditch: boolean` 参数。
- **2.3** 严格执行二衬防穿模校验与双侧沟自适应迁移：
  ```typescript
  // 拱脚空间参数
  const cosFoot = dx_offset / (R3_base - R2_base);
  const sinFoot = dy_offset / (R3_base - R2_base);
  const xFoot = R3_base * cosFoot; // 二衬内轮廓拱脚 X
  
  // 侧沟定位：三沟式位于路缘，双侧沟式自适应外移至拱脚承接区
  const sideRightX = has_central_ditch ? (max_x_lining - 0.05) : (xFoot - 0.05);
  const sideRightXInner = sideRightX - 0.40;
  const sideLeftX = -sideRightX;
  const sideLeftXInner = -sideRightXInner;

  // 侧沟槽底安全高程（严格高出仰拱内弧 5cm 防穿模）
  const yLiningAtDitch = invertCenterY - Math.sqrt(Math.max(0, R3_base * R3_base - sideRightX * sideRightX));
  const yBot_side = Math.max(roadY - 0.3, yLiningAtDitch + 0.05);
  ```
- **2.4** 在 `roadShape` 生成流程中，增加条件分支判定与中央平直段插值：
  ```typescript
  roadShape.lineTo(halfRoadW + ox, roadY);
  addDitchNotch(sideRightX, sideRightXInner, yBot_side);
  
  if (has_central_ditch) {
    addDitchNotch(centralRightX, centralLeftX, yBot_central);
  } else {
    // 双侧沟模式：在中央平直段插入采样点，防止 Earcut 大跨度剖分退化
    const midSteps = 8;
    for (let i = 1; i < midSteps; i++) {
      const mx = sideRightXInner - (i / midSteps) * (sideRightXInner - sideLeftXInner);
      roadShape.lineTo(mx + ox, roadY);
    }
  }
  
  addDitchNotch(sideLeftXInner, sideLeftX, yBot_side);
  roadShape.lineTo(-halfRoadW + ox, roadY);
  ```
- **2.5** 在 `ditchShapes` 生成流程中，动态分支生成防穿模 U 型槽：
  ```typescript
  if (has_central_ditch) {
    ditchShapes.push(createUShape(centralLeftX, centralRightX, roadY, yBot_central, 0.05)); // 中央深水沟
  }
  ditchShapes.push(createUShape(sideLeftX, sideLeftXInner, roadY, yBot_side, 0.05)); // 左侧边沟
  ditchShapes.push(createUShape(sideRightXInner, sideRightX, roadY, yBot_side, 0.05)); // 右侧边沟
  ```

---

### WBS 3: 几何曲线平顺收口与 3D 排水管网真实物理重构 (`math.ts` & `DrainagePipeGenerator.ts`)
- **3.1 修正 `math.ts` 中的 `HorseshoeArcCurve` 曲线端点**：
  - 移除原先强行拉伸至水沟槽底的 `legSteps` 畸变折角代码；
  - 使曲线两端精准起止于拱脚纵向管坐标 $(\pm X_{\text{long\_foot}}, Y_{\text{long\_foot}})$，实现环向盲管贴初支平顺收口。
- **3.2 扩展 `DrainagePipeConfig` 接口**：
  ```typescript
  export interface DrainagePipeConfig {
    // ... 原有字段 ...
    hasCentralDitch?: boolean; // 是否包含中央排水沟 (默认 true)
  }
  ```
- **3.3 重构拱脚纵向排水管 `updateLongPipes`**：
  - 纵向管固定为 2 根（左右拱脚各 1 根），移出水沟内部；
  - 定位坐标：右拱脚 $(xOffset + X_{\text{long\_foot}}, Y_{\text{long\_foot}}, -length/2)$，左拱脚 $(xOffset - X_{\text{long\_foot}}, Y_{\text{long\_foot}}, -length/2)$。
- **3.4 重构穿衬横向排水管 `updateLatPipes` (严禁取消)**：
  - 固定坡度 $k_{\text{slope}} = 0.03$ (3%)；
  - **当 `hasCentralDitch === true`**：横向管自左右拱脚向中央水沟侧壁引出，终点为 $(xOffset \pm 0.35, Y_{\text{long\_foot}} - k_{\text{slope}} \cdot (X_{\text{long\_foot}} - 0.35), z)$；
  - **当 `hasCentralDitch === false`**：横向管自左右拱脚向自适应迁移后的侧边沟外壁引出，终点为 $(xOffset \pm X_{\text{sideRight}}, Y_{\text{long\_foot}} - k_{\text{slope}} \cdot |X_{\text{long\_foot}} - X_{\text{sideRight}}|, z)$；
  - 计算各段三维向量与旋转四元数，精确保持物理 3% 倾斜。

---

### WBS 4: 3D 视图参数解包与渲染管道更新 (`Viewer3D.vue`)
- **4.1** 在 `renderSceneData()` 方法中解包快照/当前表单中的 `has_central_ditch` 参数：
  ```typescript
  const has_central_ditch = extractSnapshotValue<boolean>(rawData, 'has_central_ditch', true);
  ```
- **4.2** 实例化 `TunnelGenerator` 与 `DrainagePipeGenerator` 时将 `has_central_ditch` 正确传入：
  ```typescript
  const tGen = new TunnelGenerator(
    tType, 
    start_chainage, 
    end_chainage, 
    r, 
    aspect_ratio, 
    D_spacing, 
    r1, 
    r2, 
    rg, 
    c, 
    1.0, 
    has_central_ditch
  );

  const dpGen = new DrainagePipeGenerator({
    // ... 原有配置 ...
    hasCentralDitch: has_central_ditch
  });
  ```

---

### WBS 5: 前端交互表单控件注入 (`ParameterForm.vue`)
- **5.1** 在 `ParameterForm.vue` 的折叠面板 2（`📏 结构尺寸参数`）中添加表单控件项：
  ```html
  <el-row :gutter="20">
    <el-col :span="12">
      <el-form-item label="中间排水沟(3D)">
        <el-radio-group 
          :model-value="formData.has_central_ditch ?? true" 
          @change="(val) => paramStore.updateParam('has_central_ditch', val)"
        >
          <el-radio :value="true">设置(三沟式)</el-radio>
          <el-radio :value="false">不设置(双侧沟)</el-radio>
        </el-radio-group>
      </el-form-item>
    </el-col>
  </el-row>
  ```
- **5.2** 确保修改该项时，Pinia 订阅器正常触发 `isDirty = true`，并在点击“固化工程参数”入库时，完整写入 `parameters_json`。

---

### WBS 6: Excel 导入导出与类型安全归一化 (`excelIO.ts`)
- **6.1** 更新 `fieldMapping` 字典，建立中文表头到英文 Key 的映射：
  ```typescript
  '中间排水沟': 'has_central_ditch',
  '是否有中间排水沟': 'has_central_ditch',
  '中间排水沟(3D专用)': 'has_central_ditch',
  '中央排水沟': 'has_central_ditch',
  ```
- **6.2** 将 `'has_central_ditch'` 加入 `standardExportKeys` 列表。
- **6.3** 在 `downloadTemplate` 生成函数中设置默认表头值与示例行数据：
  ```typescript
  if (key === 'has_central_ditch') return '是'; // 对应三沟式
  ```
- **6.4** 编写类型安全布尔转换函数，规避 JS 假真值陷阱：
  ```typescript
  function parseBooleanCell(val: any, defaultVal = true): boolean {
    if (val === undefined || val === null || val === '') return defaultVal;
    if (typeof val === 'boolean') return val;
    const str = String(val).trim().toLowerCase();
    if (['是', '有', 'true', '1', 'yes'].includes(str)) return true;
    if (['否', '无', 'false', '0', 'no'].includes(str)) return false;
    return defaultVal;
  }
  ```
- **6.5** 在 `exportCurrentData` 与 `exportSnapshotResult` 中将布尔值格式化输出为 `'是'` / `'否'`。

---

### WBS 7: 参数台账与多维对标中心元数据升级 (`ParameterDatabase.vue`)
- **7.1** 在 `PARAM_META_DICT` 增加元数据项：
  ```typescript
  has_central_ditch: { 
    label: '中间排水沟', 
    unit: '-', 
    comment: '是否设置路面中央深排水沟(是:三沟式, 否:双侧沟式，仅3D视口建模专用)' 
  }
  ```
- **7.2** 在参数值格式化工具 `formatParamValue` 中处理布尔值与历史缺失兼容：
  ```typescript
  if (key === 'has_central_ditch') {
    return (val === false || val === 'false' || val === '否') ? '否 (双侧沟)' : '是 (三沟式)';
  }
  ```
- **7.3** 验证多工程对标抽屉中，不同工程间中间排水沟设置差异能正确标红高亮展示。

---

## 5. Acceptance Criteria (量化验收指标与测试矩阵)

| 序号 | 验收场景 | 操作步骤 | 预期输出与量化指标 |
| :---: | :--- | :--- | :--- |
| **AC-1** | **表单响应式切换与脏标记** | 在 `ParameterForm.vue` 中将“中间排水沟”从“设置(三沟式)”切换为“不设置(双侧沟)” | 1. `parameterStore.currentPayload.has_central_ditch` 即刻变为 `false`；<br/>2. `isDirty` 变为 `true`，触发 3D 视图重新渲染。 |
| **AC-2** | **3D 几何双侧沟与管网展示** | 保持 `has_central_ditch = false` 渲染 3D 视口 | 1. 隧道路面中央区域 100% 平整连续，无中央下沉槽口；<br/>2. `ditchMesh` 仅生成左右 2 条侧沟 U 型槽，槽内无管道，槽底高出二衬内表面 $\ge 5\text{cm}$（严禁穿模）；<br/>3. **横向穿衬盲管完整保留**，自左右拱脚纵向盲管以 3% 下倾坡度精准穿透二衬排入自适应迁移后的侧边沟；<br/>4. 路面中央平直段网格过渡平滑，无 Earcut 大跨度狭长对角线剖分伪影。 |
| **AC-3** | **3D 几何三沟式与管网展示** | 将 `has_central_ditch` 切回 `true` 渲染 3D 视口 | 1. 路面中央精准切出 $[ -0.35\text{m}, +0.35\text{m} ]$ 槽口并下沉至 $y_{\text{bot\_central}}$（严格高出仰拱内表面 $5\text{cm}$，严禁穿模）；<br/>2. 中央深水沟 8 顶点 U 型防渗槽敞口外露，槽内无管道；<br/>3. 环向管平顺收口于拱脚纵向管，横向排水管以严格 3% 下倾坡度自拱脚穿透二衬排入中央深水沟侧壁（$X = \pm 0.35\text{m}$），无中心对穿碰撞。 |
| **AC-4** | **Excel 模板下载与批量导入** | 1. 点击“下载导入模板”；<br/>2. 填写含 `中间排水沟` 列（分别填 `是` 和 `否`）的 Excel 并上传 | 1. 导出的模板中包含“中间排水沟(3D专用)”列；<br/>2. 导入后生成的快照序列分别准确携带 `has_central_ditch: true` 与 `false`（防范 `'否'` 被误判为 true）。 |
| **AC-5** | **工程参数对标分析** | 在 `ParameterDatabase.vue` 中勾选一个三沟式工程与一个双侧沟式工程，点击“对标分析” | 1. 对标表格中正确显示“中间排水沟”行；<br/>2. 分别显示“是 (三沟式)”与“否 (双侧沟)”，且由于存在差异，单元格字体自动高亮标红。 |
| **AC-6** | **计算引擎绝对隔离** | 无论 `has_central_ditch` 取 `true` 还是 `false`，点击“执行自适应解算” | 后端计算接口 200 OK 正常返回，所有力学与水力学参数（$K, M, N, q, S$）解算完全一致，无多余字段校验异常。 |
