<!-- 阶段4-第三次-3D模型问题诊断与修正方案.md -->

# 【阶段4-3D可视化】第三次3D模型问题诊断与修正方案

在执行 `阶段4-第二次-3D可视化交互问题诊断与修正方案.md` 后，对照当前源代码实现与用户反馈的 7 项残余问题，进行逐项代码级深度诊断，输出确定性修正方案。

---

## 一、 问题总览与诊断状态矩阵

| 序号 | 用户反馈问题 | 受影响模块 | 根因分类 | 致命程度 |
| :--- | :--- | :--- | :--- | :--- |
| **P1** | 【注浆加固圈】图层没有封闭，不是马蹄形 | `Reinforcement.ts` | 几何体拓扑错误 | 🔴 高危 |
| **P2** | 没有一键标准化视角切换（正/左/俯视图） | `Viewer3D.vue` | 功能缺失 | 🟡 中危 |
| **P3** | 【系统锚杆】图层集中在一个平面 | `Reinforcement.ts` / `Viewer3D.vue` | Z 轴布设算法缺陷 | 🔴 高危 |
| **P4** | 排水管网不对称 | `DrainagePipeGenerator.ts` | `doubleSide` 逻辑缺陷 | 🟡 中危 |
| **P5** | 【水文环境】图层出现锚杆、不对称、非360° | `Environment.ts` | 几何构建逻辑错误 | 🔴 高危 |
| **P6** | 没有距离量测工具 | `Viewer3D.vue` | 功能缺失 | 🟡 中危 |
| **P7** | 【最不利探针】缺少同步颜色标注 | `PostProcessing.ts` / `Viewer3D.vue` | 衬砌色阶绑定缺失 | 🟡 中危 |

---

## 二、 逐项代码级根因诊断

### P1：注浆加固圈没有封闭，不是马蹄形

**用户预期**：注浆加固圈应该包裹在马蹄形衬砌外围，呈封闭的马蹄形环状截面，而不是一个圆柱筒。

**代码根因**：

当前 [Reinforcement.ts:L114](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Reinforcement.ts#L114) 使用 `CylinderGeometry` 生成注浆圈：

```typescript
const groutingGeom = new THREE.CylinderGeometry(1, 1, 1, 32, 1, true);
groutingGeom.rotateX(Math.PI / 2);
```

`CylinderGeometry` 生成的是**正圆柱壳**。即使通过 `rotateX(π/2)` 将轴线转至 Z 轴、通过 `scale(rOuter, rOuter, segmentLength)` 缩放，其 XY 截面始终是正圆，无法匹配 `TunnelGenerator.ts` 中 `createHorseshoeShape()` 生成的三心圆拱马蹄形截面。

**数学证明**：隧道截面由三段圆弧组合——拱顶圆弧（$R_1 = 1.05r$）、两侧边墙过渡圆弧（$R_2 = 0.65r$）、仰拱反弧（$R_3 = 1.80r$），在 `aspect_ratio` 修正后是一个非对称的复合曲线轮廓。`CylinderGeometry` 的正圆截面在拱顶区域外扩过多，在边墙与仰拱区域内缩不够，无法与衬砌结构贴合。

**核心矛盾**：注浆圈应与衬砌共享同一套马蹄形截面拓扑生成逻辑，只是半径参数不同（外圈 $r_g$，内圈 $r_2$）。

**修正方案**：

废弃 `CylinderGeometry`，改为复用 `TunnelGenerator.ts` 中 `createHorseshoeShape()` 的截面生成逻辑，构建马蹄形环状注浆圈几何体：

1. 在 `Reinforcement.ts` 中引入或复制 `createHorseshoeShape` 截面生成函数
2. 用 `rg` 作为外轮廓半径、`r2` 作为内轮廓半径（Hole），通过 `ExtrudeGeometry` 拉伸出马蹄形环状截面
3. 注浆圈的几何截面方程严格遵循 `buildPath` 等厚偏移逻辑（参见 [TunnelGenerator.ts:L112-L132](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/TunnelGenerator.ts#L112-L132)）：
   - 基准三心圆弧半径以内净空 $r$ 为参考：$R_1^{base} = 1.05 \cdot r$, $R_2^{base} = 0.65 \cdot r$, $R_3^{base} = 1.80 \cdot r$
   - 各层级通过等厚偏移量 $t = r_{target} - r$ 统一扩展：$R_i = R_i^{base} + t$
   - 外轮廓（注浆圈）：$t_{out} = r_g - r$，即 $R_1^{out} = R_1^{base} + (r_g - r)$
   - 内轮廓（Hole / 初支外缘）：$t_{in} = r_2 - r$，即 $R_1^{in} = R_1^{base} + (r_2 - r)$
4. 沿 Z 轴使用 `InstancedMesh` 分段布设，每段长度 `segmentLength`

> [!IMPORTANT]
> 由于注浆圈与衬砌使用同一套三心圆拱数学模型，必须将 `TunnelGenerator.ts` 中的 `createHorseshoeShape()` 方法**抽取为公共工具函数**（或导出为 `HorseshoeShapeBuilder`），避免代码重复。

---

### P2：没有一键标准化视角切换

**用户预期**：提供正视图（Front View）、左视图（Left View）、俯视图（Top View）等一键标准化视角切换按钮，用于快速定位到标准工程视图。

**代码根因**：

当前 [Viewer3D.vue](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue) 仅有 `OrbitControls` 自由旋转控制，未提供标准化视角预设位置与 UI 入口。

**修正方案**：

在 `Viewer3D.vue` 中新增**标准视角切换 Toolbar**：

1. **定义 6 个标准视角预设**：

   | 视角名称 | Camera Position | Controls Target | 图标符号 |
   | :--- | :--- | :--- | :--- |
   | 正视图 (Front) | `(0, 0, R * 5)` | `(0, 0, -L/2)` | `F` |
   | 左视图 (Left) | `(-R * 5, 0, -L/2)` | `(0, 0, -L/2)` | `L` |
   | 右视图 (Right) | `(R * 5, 0, -L/2)` | `(0, 0, -L/2)` | `R` |
   | 俯视图 (Top) | `(0, R * 5, -L/2)` | `(0, 0, -L/2)` | `T` |
   | 底视图 (Bottom) | `(0, -R * 5, -L/2)` | `(0, 0, -L/2)` | `B` |
   | 透视图 (Perspective) | `(R*3, R*2, R*3)` | `(0, 0, -L/2)` | `P` |

   其中 $R$ = `tunnelRadius`, $L$ = `end_chainage - start_chainage`。

2. **平滑过渡动画**：使用手写 `requestAnimationFrame` 插值实现从当前相机位置到目标位置的平滑 slerp/lerp 过渡（约 0.8s），避免瞬间跳切带来的视觉违和感。当前项目 `package.json` 中未安装 `gsap`，优先使用原生 `requestAnimationFrame` 方案以避免引入新依赖。

3. **UI 布局**：在 3D 视窗**右下角**添加一组 Glassmorphism 风格的圆角按钮组（类似 CAD 软件的 ViewCube 精简版），按钮布局为 2×3 网格。

---

### P3：系统锚杆集中在一个平面

**用户预期**：系统锚杆应沿隧道纵向 Z 轴均匀分布多环排列，而不是全部集中在一个 Z 截面。

**代码根因**：

在 [Reinforcement.ts:L486-L510](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Reinforcement.ts#L486-L510) 中，`RockBoltGenerator.updateSystemData()` 的 Z 轴分布逻辑本身是正确的：

```typescript
for (let ringIdx = 0; ringIdx < ringCount; ringIdx++) {
  const z = -(ringIdx * spacingZ);
  // ...
}
```

但问题出在 **调用方** [Viewer3D.vue:L459-L475](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue#L459-L475) 的参数传递：

```typescript
const rBoltGen = new RockBoltGenerator({
  bolts_per_ring: 12,
  spacing_z: 1.0,   // ← 锚杆纵向间距仅 1m
  start_angle: -Math.PI / 4,    // ← 弧度制 -45°
  end_angle: Math.PI + Math.PI / 4,  // ← 弧度制 225°
  // ...
});
```

**关键问题发现**：`polarToCartesian()` 函数（[math.ts:L18](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/utils/math.ts#L18)）接受的 `angleDegrees` 参数为**度**单位，而 `RockBoltGenerator` 的 `start_angle` / `end_angle` 传入的是**弧度**值。具体地：

```typescript
// math.ts
export const polarToCartesian = (
  radius: number, 
  angleDegrees: number, // ← 注意：参数名标注为 angleDegrees (度)
  // ...
)
```

```typescript
// Reinforcement.ts L495-L496
const currentAngle = startAngle + boltIdx * angleStep;  // startAngle = -π/4 ≈ -0.785 (弧度)
const polarCoords = polarToCartesian(radius, currentAngle); // ← 将弧度值传入期望度数的函数！
```

-0.785 度 ≈ -0.014 弧度，几乎为 0°。这意味着所有锚杆的 `currentAngle` 从 -0.785° 到 225°，步进值 `angleStep = (225 - (-45)) / (12 - 1) ≈ 24.5°`——**这部分恰好还是在弧度制下操作，但 `polarToCartesian` 内部又做了一次 `degreesToRadians` 转换**：

```typescript
// 最终角度 = degreesToRadians(-0.785) ≈ -0.0137 rad → 几乎重叠在 X 正半轴
```

因此 **所有 12 根锚杆的角度范围只覆盖约 -0.785° 到 +3.93°**（因为弧度数值很小被当作度数），全部挤压在 X 正半轴附近极小扇区！从正面看就像"集中在一个平面"。

**修正方案**：

1. **方案 A（推荐）**：将 `RockBoltGenerator` 构造参数中的 `start_angle` / `end_angle` 改为**度数制**，传入 `-45` 和 `225`：

   ```typescript
   const rBoltGen = new RockBoltGenerator({
     // ...
     start_angle: -45,    // 度数：从右侧仰拱脚开始
     end_angle: 225,      // 度数：到左侧仰拱脚结束
     // ...
   });
   ```

2. **方案 B**：在 `RockBoltGenerator.updateSystemData()` 中不调用 `polarToCartesian()`，改为直接使用弧度计算。但这与 `math.ts` 的公共 API 不一致，不推荐。

> [!WARNING]
> 这是一个**度/弧度单位混淆**致命错误。`polarToCartesian()` 的 API 签名明确标注参数为 `angleDegrees`（度），但 `Viewer3D.vue` 的调用方传入了 `Math.PI` 弧度值。这导致 12 根锚杆实际覆盖的周向范围仅约 **4°**，而非预期的 **270°**。

---

### P4：排水管网不对称

**用户预期**：排水管网应在隧道两侧对称布置（左右侧边沟各一条纵向管 + 中心水沟管 + 对称的横向连接管）。

**代码根因**：

在 [DrainagePipeGenerator.ts:L240-L244](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts#L240-L244) 中：

```typescript
// 1: 左侧边沟管
if (this.config.doubleSide && targetMesh.count > 1) {
  const posLeft = new THREE.Vector3(xOffset - sideDitchX, roadY + longRadius, -length / 2);
  // ...
}
```

左侧纵向管的生成受 `this.config.doubleSide` 控制。而 `doubleSide` 的值来源于 [Viewer3D.vue:L483](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue#L483)：

```typescript
doubleSide: extractSnapshotValue(rawData, 'double_side', true),
```

当快照数据中 `double_side` 字段不存在时，默认为 `true`（双侧排水），理论上管网应对称。

**实际不对称的隐蔽根因**：

问题出在 `calculateLongCount()` [DrainagePipeGenerator.ts:L137-L139](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts#L137-L139)：

```typescript
private calculateLongCount(): number {
  return this.config.doubleSide ? 3 : 2;
}
```

当 `doubleSide = true` 时，`longCount = 3`，`InstancedMesh` 最大实例数预留为 3。`updateLongPipes()` 设置 `targetMesh.count = Math.min(count, targetMesh.instanceMatrix.count)`。这部分逻辑正确。

**真正的问题在横向管**：`calculateLatCount()` 不区分 `doubleSide`，而 `updateLatPipes()` 中的双侧逻辑依赖运行时判断：

```typescript
// 左侧边沟 -> 中心水沟
if (isDoubleSide && idx < targetMesh.count) {
  // ...
}
```

但 `latCount` 在构造函数中已预算：

```typescript
const latCount = this.calculateLatCount() * (config.doubleSide ? 2 : 1);
```

> [!NOTE]
> **[推测 — 未经运行时验证]** 以下为边界条件推演：如果 `doubleSide` 在 `updateFromSnapshot()` 中被快照数据**覆盖为 false**（`params.double_side` 存在且为 false），则运行时 `isDoubleSide = false`，左侧横向管不生成，但纵向管 `count` 仍为 3——形成**纵向管对称但横向管不对称**的半残渲染。实施时需通过运行时断点验证此路径是否实际触发。

此外，**环向排水管 `updateRingPipes()` 只在一侧放置**：

```typescript
const position = new THREE.Vector3(xOffset + sideDitchX, roadY + 0.2, z);
```

始终位于 `+sideDitchX` 一侧，未在 `-sideDitchX` 侧镜像生成，导致环向管也不对称。

**修正方案**：

1. `updateRingPipes()` 中，当 `doubleSide = true` 时，在每个 Z 位置同时生成左右两侧环向管实例
2. 确保 `calculateRingCount()` 返回值在 `doubleSide` 时翻倍
3. 检查 `updateFromSnapshot()` 中 `doubleSide` 的来源，确保与后端 snapshot 数据一致
4. 横向管在 `doubleSide = true` 时，左右两侧各生成一条从侧边沟到中心水沟的连接管

> [!WARNING]
> `ringCount` 在构造函数中用于初始化 `InstancedMesh` 的 `maxCount`（[DrainagePipeGenerator.ts:L52-L53](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts#L52-L53)）。若 `calculateRingCount()` 在 `doubleSide` 时翻倍，**必须在构造函数中同步以翻倍后的值初始化 `InstancedMesh`**，否则运行时 `setMatrixAt(idx)` 中 `idx >= maxCount` 将静默溢出（Three.js 不抛异常但不渲染）。

---

### P5：水文环境图层出现锚杆、不对称、非 360°

**用户预期**：水文环境图层应仅包含水位面、地面、水流粒子与流线，呈现 360° 环绕隧道的均匀水头分布。不应出现任何锚杆状构件。

**代码根因分析（三重问题）**：

#### P5.1 水文层中"出现锚杆"

> [!NOTE]
> **[推测 — 依赖 P3 修复结果验证]** 以下判断的成立前提是 P3（锚杆角度修正）已先行修复。若 P3 修复后问题仍存在，需回溯排查 `Environment.ts`。

这**并非 Environment.ts 的 bug**，而是**图层遮挡导致的视觉混淆**。

当所有图层同时渲染时，锚杆（`RockBoltGenerator.mesh`）穿透水位面可见范围，在水文面的半透明背景下看起来像是水文层包含锚杆。根因是 P3 中所述的锚杆分布 bug（全部集中在极小角度范围），大量锚杆挤压在同一区域形成视觉密集体，与水文面叠加后产生误判。

**验证方法**：通过图层控制面板单独隐藏"系统锚杆"图层后，水文层中不应再出现锚杆状物体。如果仍然出现，则需进一步排查 `Environment.ts`。

#### P5.2 水文面不对称

[Environment.ts:L186-L216](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Environment.ts#L186-L216) 中水面几何体创建：

```typescript
const geometry = new THREE.PlaneGeometry(width, length, 64, 64);
```

这是一个**水平矩形平面**，通过 `rotation.x = -Math.PI / 2` 转为水平。但宽度 `width` 计算时：

```typescript
const width = this.config.tunnelType === 'double' 
  ? (this.config.dSpacing || 0) + this.config.tunnelRadius * 6
  : this.config.tunnelRadius * 6;
```

以隧道中心为原点，这个矩形平面在 X 轴方向的覆盖范围为 $[-width/2, +width/2]$，在 Z 轴方向覆盖 $[-length/2, +length/2]$。但 `this.waterPlane.position.z = -this.config.startChainage - length / 2`，若隧道中心不在 $X=0$（双洞模式），水面相对双洞位置会不对称。

此外，水面使用 `PlaneGeometry` 渲染的是一个**有限矩形范围**的水面，而不是 360° 环绕隧道的水体。对于地下水位面来说，它只是一个"上帝视角"的平面切片，从侧面看确实不是 360° 环绕的。

#### P5.3 水文面非 360°

这是**物理建模本身的局限**，而非代码 bug。地下水位面在工程中通常确实表达为一个无限水平面（在隧道上方某一高程处），而非围绕隧道 360° 包裹。

但用户期望的可能是：**地下水渗流场**应从四面八方向隧道汇聚，即水流粒子与流线应呈 360° 辐射状分布，而不是仅从上方向下。

当前 `initWaterParticles()` 中粒子初始位置 [Environment.ts:L292-L296](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Environment.ts#L292-L296)：

```typescript
positions[i * 3] = (Math.random() - 0.5) * spreadX;        // X 方向随机
positions[i * 3 + 1] = this.currentState.waterHead - Math.random() * 10;  // Y 方向仅在水面以下
positions[i * 3 + 2] = -this.config.startChainage - Math.random() * length; // Z 方向随机
```

粒子仅在 Y 方向从水面向下分布，`aVelocity` 也仅有向下分量 `velocities[i * 3 + 1] = -Math.random() * 2 - 0.5`。流线同样仅从上方向隧道汇聚。

**修正方案**：

1. **水面几何体**：增大 `PlaneGeometry` 覆盖范围，确保单洞/双洞模式下均以隧道轴线中心对称覆盖
2. **水流粒子 360° 分布**：修改粒子初始位置为围绕隧道圆柱坐标分布：
   ```typescript
   const theta = Math.random() * Math.PI * 2; // 0 ~ 360°
   const rDist = tunnelRadius * 2 + Math.random() * tunnelRadius * 3;
   positions[i * 3]     = rDist * Math.cos(theta); // X
   positions[i * 3 + 1] = rDist * Math.sin(theta); // Y
   positions[i * 3 + 2] = -startChainage - Math.random() * length; // Z
   ```
3. **流线辐射汇聚**：流线起点从隧道四周 360° 随机分布，终点收敛至隧道中心轴线
4. **粒子动画速度方向**：改为径向指向隧道中心的速度矢量

---

### P6：没有距离量测工具

**用户预期**：在 3D 场景中可以通过鼠标交互式测量两点间的距离（类似 CAD 中的距离量测功能）。

**代码根因**：

当前 [Viewer3D.vue](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue) 完全没有距离量测相关的代码。

**修正方案**：

新增 `MeasurementTool` 交互模块，实现以下功能：

1. **交互流程**：
   - 用户点击工具栏"📏 距离量测"按钮，进入量测模式
   - 第一次点击 3D 场景中的模型表面，记录起点 $P_1$，渲染起点标记球
   - 第二次点击模型表面，记录终点 $P_2$，渲染终点标记球
   - 自动绘制 $P_1 \to P_2$ 的虚线连接线，在中点处使用 `Sprite` 标注距离文本 `d = ||P_2 - P_1||` (保留2位小数)
   - 支持连续量测（可保留多条量测线）
   - 提供"清除量测"按钮一键移除所有量测标注

2. **技术实现**：
   - 使用 `THREE.Raycaster` 进行鼠标射线与场景 Mesh 的相交检测
   - 量测线使用 `THREE.Line` + `LineDashedMaterial` 渲染
   - 距离标签使用 `THREE.Sprite` + `CanvasTexture` 渲染
   - 量测模式下，临时禁用 `OrbitControls` 以避免拖拽冲突（仅在点击时禁用，移动时恢复）

3. **UI 入口**：在标准视角工具栏旁边增加"📏"按钮

---

### P7：最不利探针图层缺少同步颜色标注

**用户预期**：最不利受力单元所在位置应有与安全系数色阶同步的颜色标注——即在衬砌截面上，按照 24 个单元的 $K_{list}$ 安全系数值，将每个单元染色（红色 = 危险，绿色 = 安全），使用户能直观看到哪些区域受力不利。

**代码根因**：

当前 [PostProcessing.ts:L31-L53](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/PostProcessing.ts#L31-L53) 中 `generateColorSpectrum()` 已经计算了 24 单元的 `THREE.Color[]` 色阶数组（存储于内部 `this.elementColors`），但该色阶数组**未被暴露至调用方**。

在 [Viewer3D.vue:L511-L520](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue#L511-L520) 中，`probeManager.updateFromSnapshot()` 的返回值仅包含 `controlIdx`、`controlM`、`controlN`、`minK` 四个字段，用于更新 `probeInfo` 浮窗的数值显示。**`elementColors` 数组既未作为返回字段输出，也未回注到衬砌 `InstancedMesh` 的 `instanceColor` 或自定义着色器 Uniform 中**。

```typescript
// Viewer3D.vue:L512-L519 — probeRes 当前字段
const probeRes = probeManager.updateFromSnapshot(rawData, r, -start_chainage, 2.0);
probeInfo.value = {
  controlIdx: probeRes.controlIdx,
  controlM: probeRes.controlM,
  controlN: probeRes.controlN,
  minK: probeRes.minK,
  isCritical: probeRes.minK <= 2.0
};
// ← elementColors 未被消费
```

此外，当前最不利探针仅渲染了**一个球体 + 一个光环**，颜色只有红/绿两种（`isCritical` 二值判断），未在衬砌表面渲染 24 单元的连续色阶分布。

**修正方案**：

1. **衬砌截面色阶渲染**：在 `PostProcessing.ts` 中新增 `createColorRing()` 方法，生成一个围绕隧道截面的 24 段弧形色带（每段对应一个单元），颜色使用 `K_list` 驱动的 HSL 色阶
2. **色带几何体**：使用 24 个 `ExtrudeGeometry` 弧段或一个带 `vertexColors` 的 `RingGeometry` 变体，沿马蹄形截面布置
3. **动态更新**：在 `updateFromSnapshot()` 中，当 `K_list` 更新时，同步刷新色带顶点颜色
4. **探针球体颜色同步**：探针球体颜色使用 `K_list[controlIdx]` 对应的精确色阶值（而非简单的红/绿二值）

---

## 三、 目标与约束 (Objectives & Constraints)

### 1. 目标 (Objectives)

1. **P1 注浆圈马蹄形封闭**：注浆加固圈在 XY 截面上呈现与隧道衬砌同拓扑的三心圆拱马蹄形，完整封闭包覆在初支外围
2. **P2 标准视角快速切换**：提供至少 6 个标准工程视角的一键切换按钮，带平滑过渡动画
3. **P3 锚杆纵向均匀分布**：系统锚杆沿隧道纵向 Z 轴每 1m 一环、每环 12 根，覆盖 -45° 到 225° 的周向范围
4. **P4 排水管网完全对称**：双侧排水模式下，左右两侧边沟纵向管、环向管、横向连接管完全镜像对称
5. **P5 水文环境 360° 渗流场**：水流粒子从隧道四周 360° 辐射状向隧道中心汇聚
6. **P6 距离量测工具**：支持 Raycaster 交互式两点测距
7. **P7 色阶同步标注**：24 单元安全系数色阶可视化在衬砌截面上

### 2. 约束 (Constraints)

- 严禁修改后端 Snapshot 数据接口格式
- 不影响现有 OrbitControls 视图联动与 Compare 模式
- 保持 Three.js WebGL 渲染性能，维持 InstancedMesh 实例化绘图
- 不引入额外的 Three.js 插件或大型依赖（动画过渡采用原生 `requestAnimationFrame`）

---

## 四、 架构设计与模块关系 (Architecture)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Viewer3D.vue                                   │
│  - layerVisibilityState (6 layers)                                         │
│  - StandardViewToolbar: { Front, Left, Right, Top, Bottom, Perspective }   │
│  - MeasurementTool: { Raycaster pick → distance line + label }            │
│  - clippingPlanes & localClippingEnabled                                   │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │ Layer Toggle / Matrix / Color Sync
        ┌─────────────────────────┼─────────────────────────┐
        ▼                         ▼                         ▼
┌───────────────┐         ┌───────────────┐         ┌───────────────┐
│TunnelGenerator│         │ Reinforcement │         │DrainagePipeGen│
├───────────────┤         ├───────────────┤         ├───────────────┤
│HorseshoeShape │─export──│ GroutingRing: │         │SideDitch L/R  │
│Builder (公共) │  build  │  Horseshoe    │         │Center Pipe    │
│               │  shape  │  ExtrudeGeom  │         │Ring 360° Sym  │
│               │         │ RockBolt:     │         │LatPipe L/R Sym│
│               │         │  deg → deg fix│         └───────────────┘
└───────────────┘         └───────────────┘                │
        │                                                  ▼
        │                                         ┌───────────────┐
        │                                         │  Environment  │
        │                                         ├───────────────┤
        │                                         │Water Particles│
        │                                         │ 360° Radial   │
        │                                         │FlowLines 360° │
        │                                         └───────────────┘
        │
        ▼
┌───────────────┐
│ PostProcessing│
├───────────────┤
│StressProbe +  │
│24-Unit Color  │
│Ring on Section│
└───────────────┘
```

---

## 五、 工作分解结构 (Work Breakdown Structure - WBS)

### 任务 1：抽取马蹄形截面公共构建器 + 注浆圈马蹄形重构

* **路径**：
  - [TunnelGenerator.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/TunnelGenerator.ts)
  - [Reinforcement.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Reinforcement.ts)
* **关键修改**：
  1. 将 `TunnelGenerator.createHorseshoeShape()` 中截面几何构建逻辑抽取为独立导出函数 `buildHorseshoeShape(r, r_outer, offsetX, aspect_ratio): THREE.Shape`
  2. 在 `Reinforcement.ts` 中：
     - 废弃 `CylinderGeometry` 注浆圈
     - 使用 `buildHorseshoeShape(r, rg, 0, aspect_ratio)` 生成外轮廓
     - 使用 `buildHorseshoeShape(r, r2, 0, aspect_ratio)` 生成内轮廓（Hole）
     - 通过 `ExtrudeGeometry({ depth: segmentLength })` 拉伸为马蹄形环状体
     - 沿 Z 轴 `InstancedMesh` 分段布设

### 任务 2：修正锚杆角度单位（弧度→度）

* **路径**：[Viewer3D.vue](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue)
* **关键修改**：
  1. 将 `RockBoltGenerator` 构造参数中的 `start_angle` 和 `end_angle` 从弧度值改为度数值：
     ```typescript
     start_angle: -45,   // 度，对应右侧仰拱脚
     end_angle: 225,     // 度，对应左侧仰拱脚
     ```

### 任务 3：排水管网对称性修复

* **路径**：[DrainagePipeGenerator.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts)
* **关键修改**：
  1. `updateRingPipes()`：当 `doubleSide = true` 时，在每个 Z 截面同时生成 `+sideDitchX` 和 `-sideDitchX` 两侧的环向管
  2. `calculateRingCount()` 返回值在 `doubleSide` 时翻倍
  3. **构造函数中 `createInstancedMesh` 的 `maxCount` 必须同步使用翻倍后的 `ringCount`**，防止运行时 `setMatrixAt` 静默溢出
  4. 确保横向管的 `doubleSide` 判断与 `calculateLatCount()` 预分配一致

### 任务 4：水文环境 360° 渗流场重构

* **路径**：[Environment.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Environment.ts)
* **关键修改**：
  1. 粒子初始位置改为 360° 圆柱坐标分布（围绕隧道轴线）
  2. 粒子速度方向改为指向隧道中心的径向矢量
  3. 流线起点从隧道四周 360° 随机起始，终点向隧道中心轴线收敛
  4. 水面 `PlaneGeometry` 宽度以隧道中心对称布置

### 任务 5：新增标准视角切换工具栏

* **路径**：[Viewer3D.vue](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue)
* **关键修改**：
  1. 新增 `switchToStandardView(viewName: string)` 方法
  2. 实现 6 个标准视角预设（Front/Left/Right/Top/Bottom/Perspective）
  3. 平滑相机过渡动画（原生 `requestAnimationFrame` lerp/slerp 插值 0.8s）
  4. 底部右下角 Glassmorphism 视角切换按钮组 UI

### 任务 6：新增距离量测工具

* **路径**：[Viewer3D.vue](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue)
* **关键修改**：
  1. 新增 `MeasurementTool` 状态管理（`isMeasuring`, `measurePoints`, `measureLines`）
  2. 鼠标 `click` 事件监听 → `Raycaster` 射线检测 → 记录表面交点
  3. 两点确定后绘制虚线 + Sprite 标注距离
  4. 工具栏增加"📏 距离量测"和"🗑 清除量测"按钮

### 任务 7：24 单元安全系数色阶同步标注

* **路径**：
  - [PostProcessing.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/PostProcessing.ts)
  - [Viewer3D.vue](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue)
* **关键修改**：
  1. `PostProcessing.ts` 新增 `createColorRing()` 方法：沿马蹄形截面生成 24 段弧形色带
  2. 色带使用 `K_list` 驱动的 HSL 色阶着色
  3. 在 `Viewer3D.vue` 的 `renderSceneData()` 中，将 `probeRes.colors` 回注到色带 Mesh
  4. 探针球体颜色同步为 `K_list[controlIdx]` 精确色阶

---

## 六、 验收标准 (Acceptance Criteria)

1. **注浆圈马蹄形封闭** (P1)：
   - 切换到正视图（Front View），注浆加固圈呈现完整封闭的三心圆拱马蹄形截面
   - 注浆圈紧密包裹在初支外围，无圆柱形残留

2. **标准视角一键切换** (P2)：
   - 点击视角工具栏任一按钮（F/L/R/T/B/P），相机平滑过渡到对应的标准工程视角
   - 过渡动画流畅无跳变

3. **锚杆纵向均匀分布** (P3)：
   - 切换到侧视图（Left View），可见系统锚杆沿隧道纵向 Z 轴等间距排列多环
   - 切换到正视图（Front View），可见 12 根锚杆均匀覆盖 -45° 到 225° 周向范围

4. **排水管网完全对称** (P4)：
   - 正视图下，左右侧边沟纵向管、环向管、横向连接管完全镜像对称

5. **水文环境 360° 渗流** (P5)：
   - 粒子从隧道四面八方向中心汇聚，非仅从上方向下
   - 单独隐藏锚杆图层后，水文层中无任何锚杆状异物

6. **距离量测工具可用** (P6)：
   - 激活量测模式后，点击模型两点可显示虚线连接与距离标注（单位：米）

7. **24 单元色阶标注** (P7)：
   - 在隧道截面上可见 24 段弧形色带，红色区域对应低安全系数单元
   - 最不利探针球体颜色与色阶一致

8. **编译与渲染无报错**：
   - `npm run build` 打包成功，Console 无异常

---
*注：本分析方案已保存至根目录 `阶段4-第三次-3D模型问题诊断与修正方案.md`。*
