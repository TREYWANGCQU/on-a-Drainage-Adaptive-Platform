# 阶段4-第五次-3D视图排水标注与相机动画交互优化方案

## Objectives

本方案旨在针对 `tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue` 及其关联的核心三维渲染组件（`DrainagePipeGenerator.ts`、`Environment.ts`）进行功能增强与交互优化，解决当前三维可视化窗口在排水管网标注、水文粒子动画控制以及工程相机投影模式切换上的体验缺陷。

### 核心目标分解：

1. **【排水管网】图层数值标注增强**
   - 在【排水管网】图层下新增参数标注子图层（`└ 排水管网参数标注`），通过 3D Canvas Text Sprites 在空间模型中实时标出：
     - 环向排水盲管间距与管径（如 `环向盲管: Φ50mm @ 5.0m`，在涌水量 $q_{drain} > 1.5 \text{ m}^3/(\text{m}\cdot\text{d})$ 时自动同步更新为 `@ 2.5m`）；
     - 纵向排水管管径（如 `纵向排水管: Φ100mm`）；
     - 横向连接管管径（如 `横向排水管: Φ80mm`）；
     - 径向打孔排水管长度与管径（如 `径向排水管: L=4.0m Φ50mm`）。
   - 标注图层与【排水管网】父图层形成层级显隐联动，支持独立开启/关闭。

2. **【地下水粒子特效】动态变化开关**
   - 在图层控制面板中为【地下水粒子特效】图层增加独立的`动态流速开关`（播放/暂停）。
   - 允许用户在保持水文环境与粒子可见的前提下，随手冻结/恢复地下水粒子的运动步进（Shader 中的 `uTime` 更新），以便于静态结构观察、剖切分析与精准交互测距。

3. **【透视】与【等轴/正交】相机无缝切换开关**
   - 在 3D 视口工具栏中新增【透视 (Perspective)】与【等轴/正交 (Orthographic)】投影相机的切换开关。
   - 依据透视相机当前的观察距离与视场角（FOV），精确推算正交视锥体（Frustum）的 Top/Bottom/Left/Right 边界，实现两种投影模式切换时视图缩放比例与观察焦点（OrbitControls Target）的平滑过渡，杜绝突变跳帧。

---

## Constraints

1. **架构与接口兼容性**
   - 必须保持现有 `Viewer3D.vue` 的外部暴露接口 `setCameraState` 兼容，确保双屏联动组件（`CompareView.vue`）在前后台矩阵同步时不发生崩溃。
   - 标注图层实体与新相机对象必须全面接入现有的 `clippingPlanes` 局部剖切剔除机制与 `resize` 视口自适应机制。

2. **性能与渲染稳定性**
   - 3D 标注文本须采用高性能 Canvas Texture Sprite 技术，禁止引入过重的 DOM/CSS2DRenderer 节点，确保在隧道长距离密集管网下帧率稳定维持在 60 FPS。
   - 动画暂停逻辑必须直接在 CPU 驱动端阻断 `uTime` 步进，不销毁或重建粒子 BufferGeometry，避免显存频繁分配释放。

3. **交付与编码规范**
   - 所有在方案中提出的代码修改与架构调整必须遵循工程开发规范：必须附带文件相对路径头注释（如 `// relative/path/file.ext`），严禁包含任何 TODO、伪代码或占位符。

---

## Architecture

### 1. 模块分解与职责映射

```
tunnel-drainage-platform/frontend/src/components/three/
├── Viewer3D.vue               # 3D 视口主控：UI 面板、相机模式切换、动画帧渲染循环 (scheduleRender)
├── DrainagePipeGenerator.ts   # 排水管网生成器：管网实体 Mesh + 新增 annotationGroup 标注 Sprites
└── Environment.ts            # 水文环境模块：水面与粒子 Shader + 新增 isAnimated 动态开关控制
```

- **Viewer3D.vue (视图与交互控制)**
  - 维护响应式状态 `layerVisibility.pipeAnnotations`（排水标注显显）与 `isWaterParticleAnimated`（粒子流动开关）；
  - 维护 `cameraMode` ('perspective' | 'orthographic') 状态，同时管理 `PerspectiveCamera` 与 `OrthographicCamera` 实例；
  - 负责事件触发与控制指令分发（如分发动画开关指令至 `Environment`，分发标注显显至 `DrainagePipeGenerator`）。

- **DrainagePipeGenerator.ts (管网与标注绘制)**
  - 新增 `public annotationGroup: THREE.Group` 容器；
  - 实现 `updateAnnotations(snapshot)` 方法，计算环向/纵向/横向/径向管网的三维特征点（如拱顶、边沟槽底、横管中点），创建包含数值文本的高对比度 Canvas Texture Sprite，并挂载至 `annotationGroup`。

- **Environment.ts (粒子与水文特效)**
  - 新增 `public setAnimationEnabled(enabled: boolean)` 接口与内部 `private isAnimated: boolean` 标志位；
  - 在 `update(delta)` 渲染循环中，若 `isAnimated === false`，则暂停累加 `clock.getElapsedTime()` 至 Shader Uniforms (`uTime`)，锁定粒子位置与水面波纹。

### 2. 状态与数据流拓扑

```
                         [ UI 交互控制 ]
                                │
   ┌────────────────────────────┼────────────────────────────┐
   ▼                            ▼                            ▼
[ 图层显隐/标注 Toggle ]    [ 粒子动画 Switch ]        [ 透视/正交 Button ]
   │                            │                            │
   ▼                            ▼                            ▼
layerVisibility.pipeAnnotations  isWaterParticleAnimated       cameraMode
   │                            │                            │
   ▼                            ▼                            ▼
pipeGen.annotationGroup.visible  env.setAnimationEnabled()   toggleCameraProjection()
   │                            │                            │
   └────────────────────────────┼────────────────────────────┘
                                │
                                ▼
                       [ scheduleRender() ]
                                │
                                ▼
                       [ WebGLRenderer.render ]
```

### 3. 数学模型与算法推导

#### 3.1 透视与正交相机视锥转换算法

设当前透视相机 `PerspectiveCamera` 参数为：
- 视场角 $\theta_{fov} = 45^\circ$
- 视口宽高比 $A = \frac{W}{H}$
- 相机世界坐标 $P_{cam} = (x_c, y_c, z_c)$
- OrbitControls 观察目标点 $P_{target} = (x_t, y_t, z_t)$

相机与目标点之间的欧氏距离 $D$ 计算公式为：
$$D = \| P_{cam} - P_{target} \|_2 = \sqrt{(x_c - x_t)^2 + (y_c - y_t)^2 + (z_c - z_t)^2}$$

在目标点 $P_{target}$ 所在平面上，透视相机的视场垂直高度 $H_{frustum}$ 与水平宽度 $W_{frustum}$ 为：
$$H_{frustum} = 2 \cdot D \cdot \tan\left( \frac{\theta_{fov} \cdot \pi}{360^\circ} \right)$$
$$W_{frustum} = H_{frustum} \cdot A$$

为了在切换至正交相机 `OrthographicCamera` 时画面保持完全等比例覆盖，正交相机的边界参数设为：
$$\text{top} = \frac{H_{frustum}}{2} = D \cdot \tan\left( \frac{\theta_{fov} \cdot \pi}{360^\circ} \right)$$
$$\text{bottom} = -\text{top}$$
$$\text{right} = \frac{W_{frustum}}{2} = \text{top} \cdot A$$
$$\text{left} = -\text{right}$$

坐标同步与控制器重绑定：
$$P_{ortho\_cam} = P_{cam}, \quad Q_{ortho\_cam} = Q_{cam}$$
$$\text{OrbitControls.object} = \text{activeCamera}$$

#### 3.2 排水管网标注锚点求解公式

1. **环向盲管标注锚点 ($P_{ring}$)**：
   锚定于隧道纵向起算点第一环盲管的拱顶上方 0.6m 处：
   $$P_{ring} = \left( x_{offset}, r_2 + 0.6, -start\_chainage \right)$$
   文本内容：`环向盲管: Φ${d_{ring}\times 1000}mm @ ${S_{ring}}m`

2. **纵向排水管标注锚点 ($P_{long}$)**：
   锚定于右侧边沟槽底管道上方 0.4m 处：
   $$P_{long} = \left( x_{offset} + sideDitchX, sideDitchBottomY + 0.4, -start\_chainage - \frac{L}{2} \right)$$
   文本内容：`纵向排水管: Φ${d_{long}\times 1000}mm`

3. **横向排水管标注锚点 ($P_{lat}$)**：
   锚定于隧道中部横向连通管中点上方 0.4m 处：
   $$P_{lat} = \left( x_{offset} + \frac{sideDitchX}{2}, \frac{sideDitchBottomY + ditchBottomY}{2} + 0.4, -start\_chainage - \frac{L}{3} \right)$$
   文本内容：`横向排水管: Φ${d_{lat}\times 1000}mm`

---

## Work Breakdown Structure

### WBS 1: 排水管网标注图层实现

#### 1.1 修改 `DrainagePipeGenerator.ts`
在 `DrainagePipeGenerator` 类中添加 `annotationGroup` 属性与标注更新逻辑。

```typescript
// tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts

// 1. 在类定义中新增属性
export class DrainagePipeGenerator {
  // ... 原有属性
  public annotationGroup: THREE.Group; // 排水管网标注图层组

  constructor(config: DrainagePipeConfig) {
    // ... 原有构造逻辑
    this.annotationGroup = new THREE.Group();
    this.annotationGroup.name = 'DrainagePipeAnnotations';
  }

  /**
   * 创建高性能 Canvas Texture Sprite 标注
   */
  private createTextSprite(text: string, color = '#38bdf8'): THREE.Sprite {
    const canvas = document.createElement('canvas');
    canvas.width = 512;
    canvas.height = 128;
    const ctx = canvas.getContext('2d')!;

    // 背景圆角框
    ctx.fillStyle = 'rgba(15, 23, 42, 0.85)';
    if (typeof (ctx as any).roundRect === 'function') {
      (ctx as any).roundRect(8, 8, 496, 112, 12);
    } else {
      ctx.rect(8, 8, 496, 112);
    }
    ctx.fill();
    ctx.strokeStyle = color;
    ctx.lineWidth = 4;
    ctx.stroke();

    // 绘制文本
    ctx.font = 'bold 36px "Segoe UI", sans-serif';
    ctx.fillStyle = '#ffffff';
    ctx.textAlign = 'center';
    ctx.textBaseline = 'middle';
    ctx.fillText(text, 256, 64);

    const texture = new THREE.CanvasTexture(canvas);
    texture.minFilter = THREE.LinearFilter;
    const spriteMaterial = new THREE.SpriteMaterial({
      map: texture,
      transparent: true,
      depthTest: false
    });
    const sprite = new THREE.Sprite(spriteMaterial);
    sprite.scale.set(6.0, 1.5, 1.0);
    return sprite;
  }

  /**
   * 依据快照数据动态更新管网标注
   */
  public updateAnnotations(snapshot: any): void {
    // 清空现有标注
    while (this.annotationGroup.children.length > 0) {
      const child = this.annotationGroup.children[0] as THREE.Sprite;
      this.annotationGroup.remove(child);
      if (child.material) {
        if (child.material.map) child.material.map.dispose();
        child.material.dispose();
      }
    }

    const ditchGeo = this.getDitchGeometry();
    const isDouble = this.config.tunnelType === 'double';
    const mainXOffset = isDouble ? (this.config.dSpacing ?? 30.0) / 2 : 0;
    const r2 = this.config.outerRadius ?? (this.config.tunnelRadius + 1.0);
    const length = Math.abs(this.config.endChainage - this.config.startChainage);

    // 1. 环向盲管标注
    const ringDiamMm = Math.round(this.config.ringDiam * 1000);
    const ringSpacingM = this.config.ringSpacing.toFixed(1);
    const ringText = `环向盲管: Φ${ringDiamMm}mm @ ${ringSpacingM}m`;
    const ringSprite = this.createTextSprite(ringText, '#38bdf8');
    ringSprite.position.set(mainXOffset, r2 + 0.6, -this.config.startChainage);
    this.annotationGroup.add(ringSprite);

    // 2. 纵向排水管标注
    const longDiamMm = Math.round(this.config.longDiam * 1000);
    const longText = `纵向排水管: Φ${longDiamMm}mm`;
    const longSprite = this.createTextSprite(longText, '#2ecc71');
    longSprite.position.set(mainXOffset + ditchGeo.sideDitchX, ditchGeo.sideDitchBottomY + 0.5, -this.config.startChainage - length * 0.4);
    this.annotationGroup.add(longSprite);

    // 3. 横向排水管标注
    const latDiamMm = Math.round(this.config.latDiam * 1000);
    const latText = `横向排水管: Φ${latDiamMm}mm`;
    const latSprite = this.createTextSprite(latText, '#e74c3c');
    latSprite.position.set(mainXOffset + ditchGeo.sideDitchX / 2, (ditchGeo.sideDitchBottomY + ditchGeo.ditchBottomY) / 2 + 0.5, -this.config.startChainage - length * 0.2);
    this.annotationGroup.add(latSprite);

    // 4. 径向排水管标注
    const radialText = `径向排水管: L=4.0m Φ50mm`;
    const radialSprite = this.createTextSprite(radialText, '#f39c12');
    radialSprite.position.set(mainXOffset - r2 - 1.2, 0, -this.config.startChainage - length * 0.6);
    this.annotationGroup.add(radialSprite);
  }
}
```

#### 1.2 修改 `Viewer3D.vue` 图层控制
在 `layerVisibility` 中增加 `pipeAnnotations: true`，并在模板中渲染子项控制。

```html
<!-- tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue -->

<!-- 图层显隐控制 Floating Toolbar -->
<label class="layer-item">
  <input type="checkbox" v-model="layerVisibility.pipes" @change="updateLayerVisibility" />
  <span class="layer-color-dot pipes-dot"></span>
  <span class="layer-label">排水管网</span>
</label>
<label class="layer-item sub-layer-item">
  <input type="checkbox" v-model="layerVisibility.pipeAnnotations" @change="updateLayerVisibility" />
  <span class="layer-color-dot annotation-dot"></span>
  <span class="layer-label">└ 排水管网参数标注</span>
</label>
```

---

### WBS 2: 地下水粒子特效动态变化开关

#### 2.1 修改 `Environment.ts`
为 `Environment` 类增加 `isAnimated` 机制与 `setAnimationEnabled` 接口。

```typescript
// tunnel-drainage-platform/frontend/src/components/three/Environment.ts

export class Environment {
  // ... 原有属性
  private isAnimated: boolean = true; // 粒子流动与水面波纹动画使能标志

  /**
   * 设置动态流速开关
   */
  public setAnimationEnabled(enabled: boolean): void {
    this.isAnimated = enabled;
  }

  /**
   * 动画帧驱动，水流波纹与粒子流动
   */
  public update(delta: number = 0.016): void {
    // 当动画处于暂停状态时，跳过 uTime 递增
    if (!this.isAnimated) return;

    const elapsedTime = this.clock.getElapsedTime();
    if (this.particleUniforms && this.particleUniforms.uTime) {
      this.particleUniforms.uTime.value = elapsedTime;
    }
    if (this.flowUniforms && this.flowUniforms.uTime) {
      this.flowUniforms.uTime.value = elapsedTime;
    }
  }
}
```

#### 2.2 修改 `Viewer3D.vue` 粒子控制 UI
在图层控制面板中为粒子特效增加独立的动态播放/暂停开关。

```html
<!-- tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue -->

<label class="layer-item sub-layer-item inline-between">
  <div class="left-group">
    <input type="checkbox" v-model="layerVisibility.waterParticles" @change="updateLayerVisibility" />
    <span class="layer-color-dot particle-dot"></span>
    <span class="layer-label">└ 地下水粒子特效</span>
  </div>
  <button 
    v-if="layerVisibility.waterParticles"
    class="mini-anim-btn" 
    :class="{ paused: !isWaterParticleAnimated }"
    @click.stop="toggleWaterParticleAnimation"
    :title="isWaterParticleAnimated ? '暂停粒子流动' : '启动粒子流动'"
  >
    {{ isWaterParticleAnimated ? '⏸ 动态' : '▶ 冻结' }}
  </button>
</label>
```

```typescript
// tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue

// 粒子动态控制状态
const isWaterParticleAnimated = ref(true);

const toggleWaterParticleAnimation = () => {
  isWaterParticleAnimated.value = !isWaterParticleAnimated.value;
  if (envInstance) {
    envInstance.setAnimationEnabled(isWaterParticleAnimated.value);
  }
  scheduleRender();
};
```

---

### WBS 3: 透视与正交相机无缝切换开关

#### 3.1 修改 `Viewer3D.vue` 相机初始化与切换逻辑

```typescript
// tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue

// 相机模式状态
const cameraMode = ref<'perspective' | 'orthographic'>('perspective');

let perspectiveCamera: THREE.PerspectiveCamera;
let orthographicCamera: THREE.OrthographicCamera;
let activeCamera: THREE.Camera;

/**
 * 初始化 WebGL 画布与双相机系统
 */
const initWebGL = () => {
  if (!canvasRef.value || !containerRef.value) return;

  const width = containerRef.value.clientWidth;
  const height = containerRef.value.clientHeight;

  renderer = new THREE.WebGLRenderer({
    canvas: canvasRef.value,
    antialias: true,
    logarithmicDepthBuffer: true,
    alpha: true
  });
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
  renderer.setSize(width, height);
  renderer.localClippingEnabled = true;

  scene = new THREE.Scene();
  scene.background = new THREE.Color(0x1a1d24);

  // 1. 初始化透视相机
  perspectiveCamera = new THREE.PerspectiveCamera(45, width / height, 0.1, 10000);
  perspectiveCamera.position.set(0, 25, 60);

  // 2. 初始化正交相机
  const aspect = width / height;
  const initD = perspectiveCamera.position.distanceTo(new THREE.Vector3(0, 0, -20));
  const halfH = initD * Math.tan((45 * Math.PI) / 360);
  const halfW = halfH * aspect;
  orthographicCamera = new THREE.OrthographicCamera(-halfW, halfW, halfH, -halfH, 0.1, 10000);
  orthographicCamera.position.copy(perspectiveCamera.position);

  // 默认启用透视相机
  activeCamera = perspectiveCamera;
  camera = perspectiveCamera; // 维持原有变量引用

  controls = new OrbitControls(activeCamera, renderer.domElement);
  controls.target.set(0, 0, -20);
  controls.enableDamping = true;
  controls.dampingFactor = 0.05;

  controls.addEventListener('change', () => {
    emit('cameraChange', {
      position: [activeCamera.position.x, activeCamera.position.y, activeCamera.position.z],
      target: [controls.target.x, controls.target.y, controls.target.z]
    });
    scheduleRender();
  });
  
  // ... 辅助灯光与模块挂载
};

/**
 * 切换透视 (Perspective) 与 正交/等轴 (Orthographic) 投影相机
 */
const setCameraProjectionMode = (mode: 'perspective' | 'orthographic') => {
  if (cameraMode.value === mode || !controls || !containerRef.value) return;

  const width = containerRef.value.clientWidth;
  const height = containerRef.value.clientHeight;
  const aspect = width / height;

  const currentPos = activeCamera.position.clone();
  const currentTarget = controls.target.clone();
  const distance = currentPos.distanceTo(currentTarget);

  if (mode === 'orthographic') {
    // 透视 -> 正交：根据观察距离精准算定 Frustum 视锥范围
    const halfH = distance * Math.tan((perspectiveCamera.fov * Math.PI) / 360);
    const halfW = halfH * aspect;

    orthographicCamera.left = -halfW;
    orthographicCamera.right = halfW;
    orthographicCamera.top = halfH;
    orthographicCamera.bottom = -halfH;
    orthographicCamera.position.copy(currentPos);
    orthographicCamera.quaternion.copy(perspectiveCamera.quaternion);
    orthographicCamera.updateProjectionMatrix();

    activeCamera = orthographicCamera;
    camera = orthographicCamera as any;
  } else {
    // 正交 -> 透视
    perspectiveCamera.aspect = aspect;
    perspectiveCamera.position.copy(currentPos);
    perspectiveCamera.quaternion.copy(orthographicCamera.quaternion);
    perspectiveCamera.updateProjectionMatrix();

    activeCamera = perspectiveCamera;
    camera = perspectiveCamera;
  }

  cameraMode.value = mode;
  controls.object = activeCamera;
  controls.update();
  scheduleRender();
};
```

#### 3.2 在视口工具栏增加切换按钮组

```html
<!-- tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue -->

<div class="toolbar-section">
  <span class="section-title">相机投影模式</span>
  <div class="projection-btn-row">
    <button 
      class="tool-btn proj-btn" 
      :class="{ active: cameraMode === 'perspective' }" 
      @click="setCameraProjectionMode('perspective')"
      title="透视投影 (3D 真实透视)"
    >
      📷 透视 3D
    </button>
    <button 
      class="tool-btn proj-btn" 
      :class="{ active: cameraMode === 'orthographic' }" 
      @click="setCameraProjectionMode('orthographic')"
      title="正交/等轴投影 (工程无变形测绘)"
    >
      📐 正交/等轴
    </button>
  </div>
</div>
```

---

## Acceptance Criteria

### 1. 排水管网标注图层验收标准
- **交互验证**：在右侧图层控制面板勾选 `└ 排水管网参数标注`，3D 视口内能清晰呈现 `环向盲管: Φ50mm @ 5.0m`、`纵向排水管: Φ100mm` 等悬浮 Label。
- **数据联动验证**：修改水文渗漏量输入使 $q_{drain} > 1.5 \text{ m}^3/(\text{m}\cdot\text{d})$，环向盲管标注文本自动同步变更为 `Φ50mm @ 2.5m`，且空间位置与盲管加密实体完美对齐。

### 2. 地下水粒子特效动态开关验收标准
- **冻结/播放验证**：勾选 `地下水粒子特效` 后，点击其右侧的 `▶ 冻结` 按钮，水流粒子与水面波纹动画立即静止停滞在当前帧；点击 `⏸ 动态` 后粒子恢复平滑下渗流动。
- **性能验证**：粒子动画冻结与恢复过程中，WebGL 渲染不发生界面卡顿、内存泄漏或 Mesh 重构。

### 3. 透视与等轴/正交相机切换验收标准
- **视锥转换验证**：点击工具栏中的 `📐 正交/等轴` 按钮，3D 模型平滑切换为无透视收缩的平行正交投影；点击 `📷 透视 3D` 恢复自然透视。
- **交互连续性验证**：模式切换时，模型在屏幕中心的位置与所占视野比例无突变骤跳；剖切工具、3D 交互测距、最不利探针悬浮框以及双屏分屏视角联动（CompareView）均能正常工作。
