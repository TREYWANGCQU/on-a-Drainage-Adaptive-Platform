# 阶段4-第八次-水文环境地表美化与埋深图层融合更新方案

## 1. 问题诊断与设计目标

### 1.1 现状问题分析
1. **图层受控失绑定**：在 3D 视图左侧控制面板中，【水文环境】次级图层【地面/地表】仅绑定了地表网格（`groundPlane`），而地下埋深标注图层（`depthIndicator`：包含高程测量虚线、高程刻度与 `埋深 c = XXm` 数值标签 Sprite）未收拢至【地面/地表】次级图层控制下，导致用户关掉【地面/地表】时埋深标注悬空残留。
2. **地表质感平淡**：现有 `groundPlane` 地表仅采用单一 2D Canvas 网格渐变贴图，缺乏高品质物理材质（PBR）的法线凹凸细节与环境光照 (HDRI IBL) 响应，且无法随视觉范式（赛博暗夜 `cyber` vs 高亮影棚 `studio`）动态切换地表色彩与标注对比度。

### 1.2 重构目标
1. **图层层级归一化**：将 `depthIndicator` 显隐完全收拢并归属于【地面/地表】次级图层（`layerVisibility.ground`）与【水文环境】主图层（`layerVisibility.environment`）的联合状态管控。
2. **地表美化与 PBR/程序化纹理管线**：
   - 建立 `/public/textures/ground/` 贴图资源目录与标准 PBR 异步加载机制（Diffuse, Normal, Roughness, AO）。
   - 建立具备无缝降级容灾能力的程序化 Canvas 纹理生成器，贴图未到位时平滑降级。
   - 为 `Environment` 模块实现 `setVisualParadigm(mode)` 范式响应，支持暗黑科技风与高亮影棚风的双重视觉表征。

---

## 2. 图层管控与数据流设计

```
【主图层: 水文环境】(environment: true/false)
       │
       ├── [子图层: 地下水流线] ────▶ flowLines.visible = environment && flowLines
       ├── [子图层: 地下水粒子] ────▶ waterParticles.visible = environment && waterParticles
       └── [子图层: 地面/地表]  ────▶ groundPlane.visible = environment && ground
                                    └─▶ depthIndicator.visible = environment && ground
```

### 关键接口变更
- `Environment.ts`：将 `depthIndicator` 放入 `getMeshes()` 导出列表，保证全局剖切与销毁钩子一致性。
- `Viewer3D.vue`：在 `updateLayerVisibility()` 中将 `depthIndicator` 显隐绑定为 `envVisible && layerVisibility.ground`；在 `switchVisualParadigm()` 中分发 `envInstances.forEach(env => env.setVisualParadigm(mode))`。

---

## 3. 地表 PBR 贴图资源规范与下载指南

### 3.1 贴图存放目录
贴图文件存放于前端公共资源目录：
`tunnel-drainage-platform/frontend/public/textures/ground/`

### 3.2 贴图命名规范与文件列表
- **`ground_diffuse.jpg`**：地表基础颜色贴图 (Base Color / Diffuse Map)
- **`ground_normal.jpg`**：地表面法线贴图 (Normal Map)
- **`ground_roughness.jpg`**：地表粗糙度贴图 (Roughness Map)
- **`ground_ao.jpg`**：地表环境光遮蔽贴图 (Ambient Occlusion Map)

### 3.3 推荐下载渠道 (免费 CC0 许可)
1. **Poly Haven**：[https://polyhaven.com/textures/ground](https://polyhaven.com/textures/ground)
   - 推荐款式：`rock_soil` (岩石土壤)、`dirt_ground` (泥土地表)、`aerial_ground_rock` (山体地表)。
   - 下载格式：**1K 或 2K 分辨率 JPG/PNG**。
2. **ambientCG**：[https://ambientcg.com/list?category=Ground](https://ambientcg.com/list?category=Ground)

---

## 4. 详细修改说明与验证计划

### 4.1 代码修改点
1. **`Environment.ts`**：
   - 补充 `setVisualParadigm(mode)` 方法，动态更新 `groundPlane` 材质与 `depthIndicator` 文本 Canvas 颜色的配色方案。
   - 增加异步 PBR 纹理加载管线与 Canvas 降级网格生成逻辑。
   - 将 `depthIndicator` 包含在 `getMeshes()` 返回数组中。
2. **`Viewer3D.vue`**：
   - 更新 `updateLayerVisibility` 中的 `depthIndicator.visible` 赋值分支。
   - 更新 `switchVisualParadigm` 中的水文环境实例范式同步逻辑。

### 4.2 验证步骤
1. 启动前端 `npm run dev`，打开 3D 视图。
2. 在左侧【图层显隐控制】面板中展开【水文环境】，取消勾选【地面/地表】，验证地表网格与埋深标注（虚线+文字框）是否一并消失。
3. 重新勾选【地面/地表】，点击视觉美化范式中的“高亮影棚”与“赛博暗夜”，验证地表网格贴图与埋深标注文字框对比度是否自适应调整。
