# 阶段4-第七次-排水管网坐标匹配与管网闭合优化方案

> **系统模块**：Three.js 3D 可视化渲染引擎 (`DrainagePipeGenerator.ts`, `TunnelGenerator.ts`, `math.ts`)  
> **制定日期**：2026-08-03  
> **方案性质**：系统分析与技术设计规范 (Analyzer Standard Specification)

---

## 一、 Objectives (目标定义)

针对当前 3D 视图中排水管网出现的蓝色伪影、排水管与排水沟空间位置错位以及管网断裂悬空问题，本方案确定以下三大核心研发目标：

### 1.1 环向盲管蓝色伪影彻底去化 (Blue Artifact Removal)
* **目标描述**：彻底消除附图左侧标红区域中环向盲管拱脚处横向/斜向向外异常刺出的蓝色线段伪影（即径向打孔排水管 `radialMesh` 的悬空外伸线条）。
* **交付标准**：环向盲管 (`ringMesh`) 保持纯净优雅的拱形体，无任何异常外凸蓝色刺线或游离线条干扰。

### 1.2 排水管与排水沟坐标精确对应 (Ditch-Pipe Precise Coordinate Alignment)
* **目标描述**：重构 `DrainagePipeGenerator.ts` 内的 `getDitchGeometry()` 数学解算逻辑，使其与 `TunnelGenerator.ts` 内的二衬与防渗混凝土水沟几何生成算法（`createInternalRoadAndDitchGeometry`）实现 **100% 零误差对齐**。
* **交付标准**：
  * **侧边纵向排水管**：中心轴线准确悬浮/卧铺于两侧边沟槽底中心（$X = \pm(x_{\text{lining, max}} - 0.25)$, $Y = y_{\text{Bot\_side}} + r_{\text{long}}$）。
  * **中央纵向排水管**：中心轴线准确卧铺于中央水沟槽底中心（$X = 0$, $Y = y_{\text{Bot\_central}} + r_{\text{long}}$）。
  * **横向连接排水管**：两端点精准连接侧边沟底与中央水沟底，保持 3% 的设计下倾横坡。

### 1.3 环向、径向、横向排水管闭合 (3D Pipe Network Topology Closure)
* **目标描述**：修复马蹄形环向盲管下端悬空断裂问题，将环向盲管末端延伸下落至侧边水沟内，与侧边纵向管形成物理对接；同时保证横向排水管由侧沟底精准连通至中沟底，实现 **“环向集水 $\rightarrow$ 侧沟纵向汇水 $\rightarrow$ 横向引水 $\rightarrow$ 中沟主排水”** 的全闭合三维管网拓扑。

---

## 二、 Constraints (约束条件)

1. **架构与性能约束**：
   * 必须延续 `InstancedMesh` 实例化渲染架构，严禁退化为逐个创建普通 `Mesh` 导致的 DrawCall 暴涨。
   * 管网材质需支持 `setVisualParadigm('studio' | 'cyber')` 影棚镀铬与赛博发光双范式平滑切换。
2. **数学模型严密约束**：
   * `DrainagePipeGenerator.ts` 与 `TunnelGenerator.ts` 必须共享相同的马蹄形三圆弧几何参数 ($R_1=1.05r, R_2=0.65r, R_3=1.80r$) 与路面槽底高程推导逻辑，禁止在两套代码中使用硬编码的不同偏移量。
3. **交付完整性约束**：
   * 遵循系统通用规范，不得留存 TODO、占位符或未验证的伪代码。

---

## 三、 Architecture (技术架构与数理逻辑建模)

```
                       [ 拱顶集水 ]
                            │
               ┌────────────┴────────────┐
               ▼                         ▼
      左侧环向盲管 (Ring)        右侧环向盲管 (Ring)
               │                         │
      (下落收口至侧沟)            (下落收口至侧沟)
               │                         │
               ▼                         ▼
      左侧纵向排水管 (Long)      右侧纵向排水管 (Long)
               │                         │
               └────────────┬────────────┘
                            │ (3% 横坡倾斜连通 Lat)
                            ▼
                   中央纵向主排水管 (Long)
                            │
                       [ 隧道洞外排出 ]
```

### 3.1 蓝色伪影根因剖析与隐去架构
* **根因分析**：
  在 `DrainagePipeGenerator.ts` 中，径向打孔排水管 `radialMesh` 赋予了 `0x3498db`（天蓝色）材质。在采样索引数组 `[4, 12, 20, 32, 44, 52, 60]` 中，索引 20 恰好处于马蹄形左侧直墙段（法向量为 $(-1, 0, 0)$），管长为 $L=4.0\text{m}$。当相机在透视视角下观察时，该管线直刺向围岩深部，在特定角度下表现为“从环向盲管左侧刺出一根蓝色悬空长线”（如附图红框所示）。
* **隐去架构**：
  将 `radialMesh` 的默认显示状态设为隐藏（`this.radialMesh.visible = false` 或 `count = 0`），提供 `showRadial: false` 配置控制，彻底消除环向盲管周围游离的蓝色刺线。

### 3.2 排水沟与排水管空间几何统一映射模型

为了保证管沟 100% 贴合，按 `TunnelGenerator.ts` 导出极值坐标推导统一几何方程：

$$\begin{aligned}
R_{1\_base} &= 1.05 \cdot r, \quad R_{2\_base} = 0.65 \cdot r, \quad R_{3\_base} = 1.80 \cdot r \\
dx &= R_{1\_base} - R_{2\_base} = 0.40 \cdot r \\
dy &= \sqrt{\max\left(0, (R_{3\_base} - R_{2\_base})^2 - dx^2\right)} \\
w &= 2.1 \cdot r, \quad h = w \cdot \text{aspect\_ratio} \\
H_{side} &= \max\left(0, h - R_{1\_base} + dy - R_{3\_base}\right) \\
y_{invertCenter} &= -H_{side} + dy \\
roadY &= y_{invertCenter} - (R_{3\_base} - 0.8) \\
y_{\text{Bot\_side}} &= roadY - 0.3 \quad (\text{侧边沟槽底高程}) \\
x_{\text{lining, max}} &= \sqrt{\max\left(0, R_{3\_base}^2 - (y_{invertCenter} - y_{\text{Bot\_side}})^2\right)} \\
x_{\text{sideDitchCenter}} &= x_{\text{lining, max}} - 0.25 \quad (\text{侧边沟槽底中心 X 坐标}) \\
y_{\text{Bot\_central}} &= y_{invertCenter} - R_{3\_base} + 0.05 \quad (\text{中央深水沟槽底高程}) \\
x_{\text{centralDitchCenter}} &= 0.0 \quad (\text{中央水沟中心 X 坐标})
\end{aligned}$$

#### 关键坐标修正矩阵表：
| 管线类型 | 修正前坐标/参数 | 修正后几何坐标 (与 TunnelGenerator 绝对对齐) |
| :--- | :--- | :--- |
| **侧边纵向管 X** | $halfRoadW - 0.15$ (错位 0.3m) | $\pm x_{\text{sideDitchCenter}} = \pm(x_{\text{lining, max}} - 0.25)$ |
| **侧边纵向管 Y** | $roadY - 0.4$ (下沉过深穿模) | $y_{\text{Bot\_side}} + r_{\text{long}} = roadY - 0.3 + r_{\text{long}}$ |
| **中央纵向管 Y** | $y_{invertCenter} - \sqrt{R_3^2 - w^2/4}$ | $y_{\text{Bot\_central}} + r_{\text{long}} = y_{invertCenter} - R_{3\_base} + 0.05 + r_{\text{long}}$ |
| **横向管起点/终点** | 旧侧沟底 $\rightarrow$ 旧中沟底 (断裂) | $(\pm x_{\text{sideDitchCenter}}, y_{\text{Bot\_side}}+r_{\text{lat}}) \rightarrow (0, y_{\text{Bot\_central}}+r_{\text{lat}})$ |

---

### 3.3 环-纵-横排水管网 3D 节点闭合解算拓扑

#### (1) 环向盲管脚端下落收口 (`HorseshoeArcCurve`)
在 `math.ts` 中扩展 `HorseshoeArcCurve` 离散点集：在左右两侧拱脚原 `aLeft` / `aRight` 终点处，增加垂直下落/圆弧过渡延伸段，使其切向点向下延伸至 $Y = y_{\text{Bot\_side}}$，且末端 $X$ 坐标精确收口于 $\pm x_{\text{sideDitchCenter}}$。

```
       /───────────────\   <-- 拱顶段 (R1)
      │                 │  <-- 直墙段
       \               /   <-- 拱脚段 (R2)
        └──┐       ┌──┘    <-- 新增：脚端下落收口段 (延伸至侧沟底)
           ▼       ▼
    [侧沟纵向管] [侧沟纵向管]
```

#### (2) 三维节点接头闭合方程
在每个环向里程断面 $Z = -i \cdot S_{\text{ring}}$ 处：
* **环向管末端**坐标：$(\pm x_{\text{sideDitchCenter}}, y_{\text{Bot\_side}} + r_{\text{ring}}, -i \cdot S_{\text{ring}})$
* **侧边纵向管**对应点：$(\pm x_{\text{sideDitchCenter}}, y_{\text{Bot\_side}} + r_{\text{long}}, -i \cdot S_{\text{ring}})$
* **横向连接管起点**：$(\pm x_{\text{sideDitchCenter}}, y_{\text{Bot\_side}} + r_{\text{lat}}, -i \cdot S_{\text{ring}})$
* **横向连接管终点**：$(0, y_{\text{Bot\_central}} + r_{\text{lat}}, -i \cdot S_{\text{ring}})$

三者在空间 3D 坐标上完全相交相切，不存在任何悬空空隙。

---

## 四、 Work Breakdown Structure (工作分解结构 WBS)

```
WBS 排水管网坐标匹配与管网闭合优化
├── 1. HorseshoeArcCurve 曲线下落收口重构 (math.ts)
│   ├── 1.1 引入 yBot_side 与 x_sideDitchCenter 参数计算
│   └── 1.2 增加拱脚下落收口离散点与法线补全
├── 2. DrainagePipeGenerator 几何坐标统一化 (DrainagePipeGenerator.ts)
│   ├── 2.1 重构 getDitchGeometry() 匹配 TunnelGenerator.ts
│   └── 2.2 彻底去化 radialMesh 蓝色伪影 (默认隐藏/刺线清理)
├── 3. 管网实例化矩阵生成与闭合对齐 (DrainagePipeGenerator.ts)
│   ├── 3.1 修正 updateRingPipes 矩阵使拱脚切入侧沟槽底
│   ├── 3.2 修正 updateLongPipes 精准卧铺边沟与中沟槽底
│   └── 3.3 修正 updateLatPipes 生成 3% 横坡闭合连通矢量
└── 4. 场景适配与三维闭合验收 (Viewer3D.vue & Visual Check)
    ├── 4.1 双范式 (Studio/Cyber) 材质与自发光调优
    └── 4.2 双洞/单洞工况管网空间拓扑校验
```

### 详细任务分解

#### Task 1: `math.ts` 中 `HorseshoeArcCurve` 曲线扩展
* 关联文件：[math.ts](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/utils/math.ts)
* 操作细节：
  * 更新 `HorseshoeArcCurve` 构造函数，接受 `yBot_side` 与 `sideDitchX` 参数。
  * 在 `buildPoints` 的段 1 (左侧墙) 和段 5 (右侧墙) 末尾，追加垂直/斜向延伸点，使曲线终点坐标从原拱脚弯曲延伸至 $(-x_{\text{sideDitchCenter}}, y_{\text{Bot\_side}})$ 与 $(x_{\text{sideDitchCenter}}, y_{\text{Bot\_side}})$。

#### Task 2: `DrainagePipeGenerator.ts` 坐标计算与蓝色伪影去化
* 关联文件：[DrainagePipeGenerator.ts](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts)
* 操作细节：
  * 重构 `getDitchGeometry()`，采用第三章 3.2 节给出的推导公式，算出精确的 `sideDitchX`, `sideDitchBottomY`, `ditchBottomY`。
  * 针对 `radialMesh`（蓝色径向管）：在 `constructor` 或 `updateFromSnapshot` 中设置 `this.radialMesh.visible = false`（或将其实例数量置 0），彻底消去悬空蓝色长刺线条。

#### Task 3: 环、纵、横管网实例化矩阵更新
* 关联文件：[DrainagePipeGenerator.ts](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts)
* 操作细节：
  * `updateLongPipes`：利用更新后的 `sideDitchX` 与 `sideDitchBottomY` 重新计算 Matrix4。
  * `updateLatPipes`：以 $(\pm sideDitchX, sideDitchBottomY)$ 为起点，$(0, ditchBottomY)$ 为终点，计算准确的倾斜方向四元数 $Q$ 与拉伸长度 $L$。

---

## 五、 Acceptance Criteria (验收标准)

| 序号 | 验收项目 | 检验标准 | 验证手段 |
| :--- | :--- | :--- | :--- |
| **AC-1** | **蓝色伪影去化** | 3D 视角中环向盲管四周无任何刺向围岩的蓝色线条，附图红框区域伪影 100% 消除 | 3D 视角环绕旋转检查 |
| **AC-2** | **纵向管卧铺精准度** | 两侧纵向排水管 100% 位于侧边沟槽底中心；中央纵向管 100% 位于中央水沟槽底中心，无悬空或穿透二衬现象 | 剖切面 (Clipping Plane) 局部放大观察 |
| **AC-3** | **横向管倾斜连通性** | 横向管呈现 3% 下倾坡度，两端精准对齐侧沟纵向管与中沟纵向管中心轴线 | 3D 网格节点坐标比对 |
| **AC-4** | **环向管拱脚闭合性** | 环向马蹄形盲管下端不再悬空终止于半山腰，而是顺畅延伸沉入侧边排水沟内 | 3D 管道端点拓扑检查 |
| **AC-5** | **双范式兼容性** | 切换至影棚风 (Studio) 时呈现金属镀铬质感；切换至赛博风 (Cyber) 时呈现高对比发光质感，无渲染层级错误 | 范式切换功能测试 |

---
*本方案为系统分析最终定稿文档，输出路径为根目录，待评审通过后即可交由 Builder 执行代码更新。*
