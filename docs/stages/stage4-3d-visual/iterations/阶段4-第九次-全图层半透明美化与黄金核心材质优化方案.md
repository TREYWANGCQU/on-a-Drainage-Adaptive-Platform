# 阶段4-第九次-全图层半透明美化与黄金核心材质优化方案

## Objectives (目标)

本方案旨在针对 `tunnel-drainage-platform/frontend/src/components/three` 及相关三维渲染模块，开展全图层 PBR 物理材质、自发光精细控制与光照环境的深度重构。参照工业级水晶透视视觉规范（水晶车壳 + 黄金机芯效果），实现以下核心技术目标：

1. **全图层半透明水晶质感重塑**：
   - 将围岩、地表、水文环境、二次衬砌、初期支护等包裹与承载图层统一重构为高透光率（High Transmission / Opacity $\le 0.35$）、低粗糙度（Low Roughness）、物理折射率（IOR $\approx 1.52$）的水晶玻璃 / 青冰半透明材质。
   - 保证外部包覆结构晶莹剔透，内部复杂空间拓扑清晰可见。

2. **黄金核心图层视觉焦点收敛与色彩和谐化**：
   - 确立**临界注浆圈（Critical Grouting Ring）**与**排水管网（Drainage Pipes）**为全场景的三维核心表达对象。
   - 彻底矫正既有代码中排水管网分散杂乱的红/绿/蓝 RGB 原色配色，统一升级为高饱满度**影棚抛光黄金 / 黄铜 PBR 金属材质**（Metallic Gold PBR: Color `#FFD700` / `#F59E0B` / `#D97706` / `#EAB308`，Metalness `0.90~0.95`，Roughness `0.12~0.18`）。
   - 通过黄金机芯般的金属质感与透明水晶罩形成强烈虚实对比（Contrast），实现高辨识度的工程受力与排水协同表达。

3. **双范式自发光（Emissive）精细解耦与物理光照激活**：
   - **高亮影棚范式 (`studio`)**：彻底清除全场景材质中的 `emissive` 非物理自发光污染（统一重置为 `0x000000` / `emissiveIntensity = 0.0`），完全依靠物理反射（BRDF）与 HDRI 环境贴图展示纯净高亮金属与玻璃质感。
   - **赛博暗夜范式 (`cyber`)**：允许保留微弱、精准受控的辅助自发光与轮廓微光（如 `emissive: 0x221800` ~ `0x332200`，`emissiveIntensity: 0.15~0.25`），防止在全暗背景（`#050b14`）下未直接受光面发生视觉失陷/黑洞沉没。
   - 引入物理 HDRI 基于图像的光照（Image-Based Lighting, IBL）与影棚级三点光源阵列（Key / Fill / Rim Lights），重点通过强轮廓光（Rim Light, Intensity 2.8）打亮金属管线与注浆圈的立体边缘。

4. **深度排序与透明混合治理**：
   - 优化渲染队列（`renderOrder`）与深度写入（`depthWrite`）策略，消除多层半透明物体叠加产生的 Z-fighting 闪烁与通道遮挡异常。

---

## Constraints (约束条件)

1. **WebGL 深度缓冲区与半透明渲染约束**：
   - 遵循 Three.js 物理材质 Alpha 混合规范，半透明水晶外壳必须禁用深度写入（`depthWrite: false`），而实心路面与重点管网保持 `depthWrite: true`，防止半透明面法向颠倒导致的排序混乱。

2. **双范式兼容与自发光受控约束**：
   - 必须无缝兼容 `Snapshot` 动态解算数据流（`original_state` / `critical_state`）。
   - `studio` 范式下强制 `emissive = 0x000000`；`cyber` 范式下允许低强度辅助自发光（`emissiveIntensity \le 0.25`），禁止出现高亮度过曝色彩乘子（禁止 `emissiveIntensity > 0.5`）。

3. **色彩一致性与防杂色约束**：
   - 全场景排水管网（环向盲管、纵向集水管、横向排水管）必须统一使用黄铜金/黄金 PBR 调色板，严禁在渲染器中使用分散的红/绿/蓝强对比原色。

4. **架构与工程规范约束**：
   - 本方案为系统分析与工程落地结合方案，严格遵循架构解构、模块分解、数据流建模与代码示例相对路径规范。

---

## Architecture (架构分解与物理流建模)

### 1. 图层视觉层级与色彩矩阵 (Visual Prominence Hierarchy & Color Matrix)

按照“水晶外壳 + 黄金机芯”的对比哲学，将场景中 8 大子图层划分为 3 级视觉梯队：

| 视觉层级 | 图层名称 | 代表组件 | 材质类型 | 核心 PBR 参数配置 | 视觉定位 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Level 1 (核心焦点)** | **临界注浆圈** | `Reinforcement.ts` | 晶透黄金 PBR 金属 | Color: `#FFD700`, Metalness: `0.92`, Roughness: `0.15`, Opacity: `0.85`, Studio Emissive: `0x000000` | 核心受力防渗边界，高亮黄金焦点 |
| | **排水管网 (环向盲管)** | `DrainagePipeGenerator.ts` | 抛光黄铜 PBR 金属 | Color: `#F59E0B`, Metalness: `0.92`, Roughness: `0.15`, Opacity: `0.95`, Studio Emissive: `0x000000` | 拱部与边墙集水盲管，黄铜金属线条 |
| | **排水管网 (纵向集水管)** | `DrainagePipeGenerator.ts` | 影棚深金 PBR 金属 | Color: `#D97706`, Metalness: `0.95`, Roughness: `0.12`, Opacity: `0.95`, Studio Emissive: `0x000000` | 槽底主集水干管，深金金属圆管 |
| | **排水管网 (横向连接管)** | `DrainagePipeGenerator.ts` | 亮黄金 PBR 金属 | Color: `#EAB308`, Metalness: `0.90`, Roughness: `0.18`, Opacity: `0.95`, Studio Emissive: `0x000000` | 水沟横坡连通管，亮金倾斜管道 |
| **Level 2 (次级结构)** | **原始注浆圈** | `Reinforcement.ts` | 水晶冰蓝 Glass | Color: `#38BDF8`, Metalness: `0.20`, Roughness: `0.20`, Opacity: `0.30`, Transmission: `0.65` | 基准对照层，半透明青冰辅衬 |
| | **系统锚杆 / 导管** | `Reinforcement.ts` | 亮银镀铬 Metal | Color: `#E2E8F0`, Metalness: `0.90`, Roughness: `0.20`, Opacity: `0.90` | 细微加固构件，亮银立体支承 |
| **Level 3 (透明外壳)** | **二次衬砌** | `TunnelGenerator.ts` | 磨砂珍珠水晶 Glass | Color: `#1E293B`, Metalness: `0.10`, Roughness: `0.25`, Opacity: `0.22`, Transmission: `0.75` | 主体外壳罩，极高透光率 |
| | **初期支护** | `TunnelGenerator.ts` | 深烟灰色水晶 Glass | Color: `#0F172A`, Metalness: `0.10`, Roughness: `0.30`, Opacity: `0.18`, Transmission: `0.70` | 洞壁外围包覆层 |
| | **路面与排水沟** | `TunnelGenerator.ts` | 结晶岩灰 Translucent | Color: `#0F172A` / `#1E3A5A`, Metalness: `0.20`, Roughness: `0.50`, Opacity: `0.55~0.65`, DepthWrite: `true/false` | 行车与开沟实底基座 |
| | **地表与水文环境** | `Environment.ts` | 澄澈青蓝液体/地质 Plane | Color: `#0284C7`, Metalness: `0.05`, Roughness: `0.10`, Opacity: `0.20~0.25`, Transmission: `0.80` | 空间环境地质边界 |

---

### 2. 双范式物理光照与自发光流模型 (Dual-Paradigm Lighting & Emissive Pipeline Model)

```
[HDRI Studio / Cyber Environment Map (PMREM)]
               │
               ▼
[Three-Point Studio Lighting Rig]
 ├── Key Light (主方向光: Intensity 2.5, Color #FFFFFF, Position 50, 80, 50)
 ├── Fill Light (辅方向光: Intensity 1.2, Color #93C5FD, Position -50, 40, -50)
 └── Rim Light (轮廓边缘光: Intensity 2.8, Color #FDE047, Position 0, -20, -100) ───► 打亮暗夜/高亮下黄金管道边缘
               │
               ▼
[Paradigm Material Selector]
 ├── Studio Mode : Emissive = 0x000000, 纯 PBR BRDF 反射 + Cook-Torrance 高光
 └── Cyber Mode  : Controlled Emissive (emissiveIntensity <= 0.25), 辅助轮廓防沉没
               │
               ▼
[Depth-Ordered Render Pass] -> [ToneMapping ACESFilmic (Exposure 1.1)] -> [Canvas Output]
```

---

### 3. 光学与物理材质建模 (Mathematical Modeling)

#### (1) 菲涅尔折射与玻璃透射公式 (Fresnel & Transmission Equations)
二衬与围岩透明水晶罩的反射与透射率由 Cook-Torrance 菲涅尔近似公式控制：

$$F(\theta) = F_0 + (1 - F_0) (1 - \cos\theta)^5$$

其中 $F_0 = \left(\frac{n_1 - n_2}{n_1 + n_2}\right)^2$，对于物理水晶玻璃，折射率 $n = 1.52$，$F_0 \approx 0.0428$。
在 ShaderMaterial（二衬/初支）与 MeshPhysicalMaterial（水文/地质）中，透射光强与入射角 $\theta$ 的耦合关系如下：

$$I_{\text{transmitted}} = I_{\text{incoming}} \cdot (1 - F(\theta)) \cdot e^{-\alpha \cdot d}$$

其中 $d$ 为光线穿透介质的几何厚度，$\alpha$ 为介质吸收系数。此模型保障了从正面视角看隧道二衬时透射率最高（能清晰看见内部黄金排水管），而在切向边缘处形成自然晶莹的冰蓝亮边。

#### (2) 黄金 PBR 金属高光 BRDF 模型 (Cook-Torrance BRDF for Metallic Gold)
临界注浆圈与排水管网采用纯物理 Cook-Torrance 金属 BRDF 计算公式：

$$f_{r} = \frac{D(\mathbf{h}) F(\mathbf{v}, \mathbf{h}) G(\mathbf{l}, \mathbf{v}, \mathbf{h})}{4 (\mathbf{n} \cdot \mathbf{l}) (\mathbf{n} \cdot \mathbf{v})}$$

- **法线分布函数 $D(\mathbf{h})$ (GGX Distribution)**：粗糙度 $\alpha_{\text{roughness}} = 0.15$，产生高度汇聚的高亮斑点。
- **菲涅尔项 $F$ (Gold Base Color)**：黄金基础反射率 $F_0 = (0.95, 0.71, 0.29)$（Hex `#FFD700`），赋予管网极致奢华的影棚金质感。

---

## Work Breakdown Structure (工作分解结构 WBS)

```
WBS 架构重构任务树:
├── 1. 核心黄金图层材质升级与颜色统一 (Level 1 Focal Cores)
│   ├── 1.1 Reinforcement.ts 临界注浆圈 PBR 黄金材质重构 (Studio/Cyber 双范式解耦)
│   └── 1.2 DrainagePipeGenerator.ts 排水管网全系黄铜/黄金色调统一与去杂色
├── 2. 外壳与环境半透明水晶化 (Level 2 & 3 Layers)
│   ├── 2.1 TunnelGenerator.ts 二衬/初支 ShaderMaterial 高透水晶着色器优化
│   ├── 2.2 TunnelGenerator.ts 路面与排水沟暗色半透明质感调整
│   └── 2.3 Environment.ts 地表与地下水文环境晶透水体材质优化
├── 3. 影棚级物理光照与 HDRI 环境配置 (Environment & Lighting Rig)
│   ├── 3.1 Viewer3D.vue 补齐三点影棚灯光阵列 (Key / Fill / 暖金 Rim Light)
│   └── 3.2 自发光精细控制 (Studio: 0.0, Cyber: emissiveIntensity <= 0.25)
└── 4. 深度渲染顺序与视效范式整合 (Render Pipeline Alignment)
    ├── 4.1 Viewer3D.vue 透明图层 renderOrder 链条定义
    └── 4.2 setVisualParadigm 默认主范式对齐“水晶外壳+黄金机芯”效果
```

---

### 模块详细变更设计与路径映射

#### 1. 核心图层：注浆圈与锚杆管理模块
`// tunnel-drainage-platform/frontend/src/components/three/Reinforcement.ts`

- **临界注浆圈 (`criticalMaterial`)**：
  - 材质类型：`MeshStandardMaterial` / `MeshPhysicalMaterial`
  - 颜色（Color）：`0xFFD700`（黄金）
  - 金属度（Metalness）：`0.92`
  - 粗糙度（Roughness）：`0.15`
  - 不透明度（Opacity）：`0.85`
  - **范式解耦 (`setVisualParadigm`)**：
    - `studio` 范式：`emissive: 0x000000`, `metalness: 0.95`, `roughness: 0.18`, `color: 0xffd700`（纯物理反射）。
    - `cyber` 范式：`emissive: 0x221800`, `emissiveIntensity: 0.15`, `metalness: 0.90`, `color: 0xd4af37`（暗夜微光辅助，防止视觉黑洞）。
- **原始注浆圈 (`groutingMaterial`)**：
  - 颜色（Color）：`0x38BDF8`（晶透天蓝）
  - 金属度（Metalness）：`0.20`
  - 粗糙度（Roughness）：`0.20`
  - 不透明度（Opacity）：`0.30`
  - `studio` 下 `emissive: 0x000000`；`cyber` 下 `emissive: 0x002244`, `emissiveIntensity: 0.10`。

#### 2. 核心图层：排水管网生成器模块
`// tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts`

- **全系排水管色调统一（消除杂乱 RGB 调色）**：
  - 环向盲管（Ring Pipe）：Color `#F59E0B`（黄铜金），Metalness `0.92`，Roughness `0.15`。
  - 纵向集水管（Longitudinal Pipe）：Color `#D97706`（深金），Metalness `0.95`，Roughness `0.12`。
  - 横向排水管（Lateral Pipe）：Color `#EAB308`（亮金），Metalness `0.90`，Roughness `0.18`。
- **动态范式更新接口 (`setVisualParadigm`)**：
  - `studio` 范式：`emissive: 0x000000`，靠强高光和 HDRI 环境反射表现金属质感。
  - `cyber` 范式：`emissive: 0x221500`, `emissiveIntensity: 0.15`，提供微弱暗夜管线轮廓响应。

#### 3. 外壳图层：隧道衬砌与水沟路面生成器模块
`// tunnel-drainage-platform/frontend/src/components/three/TunnelGenerator.ts`

- **二衬 / 初支 ShaderMaterial (liningVert & liningFrag)**：
  - `uBaseColor`：设置为 `#1E293B`（珍珠深蓝灰）。
  - `uFresnelColor`：设置为 `#38BDF8`（晶莹冰蓝高光边缘）。
  - `uOpacity`：降至 `0.22`，大幅提升透光率，使内部黄金注浆圈与排水管毫无遮挡。
  - `uFresnelPower`：调整为 `3.5`，使透射集中于中间区域，边缘保留精致的晶透反光。
- **路面与排水沟材质**：
  - `roadMat`：Color `#0F172A`，Opacity `0.55`，DepthWrite `true`（保持地基沉降实感）。
  - `ditchMat`：Color `#1E3A5A`，Studio Emissive `#000000`，Opacity `0.65`，DepthWrite `false`。

#### 4. 环境图层：水文与地表模块
`// tunnel-drainage-platform/frontend/src/components/three/Environment.ts`

- **地下水面 (`waterPlane`)**：
  - Shader 着色器中的反光项 `color += vec3(specular * 0.3)` 保持物理规范，透射浅蓝色 `uColorShallow` 为 `#0284C7`，透明度 `uOpacity` 设为 `0.25`。
- **地下水流线与粒子 (`flowLines` & `waterParticles`)**：
  - 移除流线中强烈的全白过曝脉冲，调整为极细柔和的冰蓝水流脉冲（Alpha `0.3~0.6`），避免抢夺黄金管网的视觉焦点。

#### 5. 全局视图入口与三点影棚光照阵列组装
`// tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue`

- **三点影棚光照阵列配置 (Studio Lighting Rig Configuration)**：
  - **主灯光 (Key Light)**：`DirectionalLight`，Position `(50, 80, 50)`，Color `#FFFFFF`，Intensity `2.5`，开启 Shadow Mapping。
  - **辅灯光 (Fill Light)**：`DirectionalLight`，Position `(-50, 40, -50)`，Color `#93C5FD`（柔和冷蓝），Intensity `1.2`。
  - **轮廓边缘光 (Rim Light / Back Light)**：`DirectionalLight`，Position `(0, -20, -100)`，Color `#FDE047`（暖金高光边缘），Intensity `2.8`，专门打亮黄金注浆圈与管网的下沿与后侧金属反光！
  - **环境光 (Ambient Light / Light Probe)**：Intensity `0.6`，搭配 HDRI 环境贴图提供高质量 IBL 全局光照。

---

## Code Implementation Reference (核心模块可落地代码参考)

### 1. `DrainagePipeGenerator.ts`
```typescript
// tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts
import * as THREE from 'three';

// 1. 环向盲管实例化网格生成 (统一黄铜金 PBR)
private createRingInstancedMesh(radius: number, count: number, _color: number): THREE.InstancedMesh {
  const pipeRadius = Math.max(0.01, radius / 2.0);
  const tubeGeometry = new THREE.TubeGeometry(this.horseshoeCurve, 64, pipeRadius, 8, false);
  
  const material = new THREE.MeshStandardMaterial({
    color: 0xf59e0b, // 黄铜金 (#F59E0B)
    roughness: 0.15,
    metalness: 0.92,
    emissive: new THREE.Color(0x000000),
    clippingPlanes: []
  });

  const mesh = new THREE.InstancedMesh(tubeGeometry, material, count);
  mesh.frustumCulled = false;
  mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
  mesh.count = 0;
  mesh.castShadow = true;
  mesh.receiveShadow = true;
  mesh.userData = { pipeCategory: 'ring', name: '环向排水盲管', diameter: Math.round(radius * 1000), spacing: this.config.ringSpacing };
  return mesh;
}

// 2. 双范式自发光解耦与物理金属反射
public setVisualParadigm(mode: 'studio' | 'cyber'): void {
  const meshes = this.getMeshes();
  meshes.forEach(mesh => {
    const mat = mesh.material as THREE.MeshStandardMaterial;
    if (!mat) return;
    if (mode === 'studio') {
      mat.metalness = 0.95;
      mat.roughness = 0.12;
      mat.emissive.setHex(0x000000); // 高亮影棚模式：纯物理反射，彻底去自发光
    } else {
      mat.metalness = 0.85;
      mat.roughness = 0.20;
      mat.emissive.setHex(0x221500); // 赛博暗夜模式：保持微弱辅助轮廓发光，防止黑色背景中沉没
    }
    mat.needsUpdate = true;
  });
}
```

### 2. `Viewer3D.vue`
```typescript
// tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue
// 初始化三点影棚灯光阵列 (Key Light + Fill Light + Rim Light)
const initSceneLighting = (scene: THREE.Scene) => {
  // Key Light 主方向光
  const keyLight = new THREE.DirectionalLight(0xffffff, 2.5);
  keyLight.position.set(50, 80, 50);
  keyLight.castShadow = true;
  scene.add(keyLight);

  // Fill Light 辅方向光 (柔和冷蓝)
  const fillLight = new THREE.DirectionalLight(0x93c5fd, 1.2);
  fillLight.position.set(-50, 40, -50);
  scene.add(fillLight);

  // Rim Light 轮廓边缘光 (暖金高光边缘，专门打亮黄金管网与注浆圈后侧)
  const rimLight = new THREE.DirectionalLight(0xfde047, 2.8);
  rimLight.position.set(0, -20, -100);
  scene.add(rimLight);

  // Ambient Light
  const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);
  scene.add(ambientLight);
};
```

---

## Acceptance Criteria (验收标准)

### 1. 视觉效果对齐验收矩阵 (Visual Alignment Criteria)

| 验收项 | 参考附图特征 | 系统实现标准 | 检验方法 |
| :--- | :--- | :--- | :--- |
| **外壳透明度** | 晶莹剔透的水晶车壳，全面透出内部机械拓扑 | 隧道二衬、初支与围岩呈高透水晶质感（Opacity $\le 0.25$），内部结构无视觉盲区 | 360° 旋转视角，验证隧道内部排水管与注浆圈全程清晰可见 |
| **核心焦点色度** | 内部发电机与发动机呈耀眼黄金金属材质 | 临界注浆圈与排水管网呈现抛光黄金/黄铜 PBR 质感（Color `#FFD700`/`#F59E0B`，Metalness $\ge 0.90$） | 放大微观视角，观察管网表面反射的影棚高光与环境反光 |
| **自发光受控** | 影棚纯物理反射，暗夜辅助轮廓微光 | 高亮模式 `emissive = 0x000000`；暗夜模式 `emissiveIntensity \le 0.25`，无过曝块 | 检查 Three.js Inspector / 材质 Uniforms 确认自发光数值符合规范 |
| **光照反射深度** | 依靠影棚灯光与 HDRI 产生丰富立体阴影与高光 | 边缘轮廓光（Rim Light, 2.8）精准打亮黄金管网边缘，水晶外壳在切向产生冰蓝菲涅尔边缘 | 切换透视图与剖切模式，确认无 Flat Shading 与对角线法向伪影 |

### 2. 性能与渲染指标 (Technical Performance Metrics)

1. **帧率稳定性 (FPS)**：
   - 包含 10,000+ 排水管网与双洞注浆圈全开状态下，在 1080P / 2K 分辨率下渲染帧率 $\ge 60$ FPS。
2. **Draw Calls 控制**：
   - 依托 `InstancedMesh` 实例化渲染，排水管网总 Draw Calls $\le 10$，注浆圈 Draw Calls $\le 4$。
3. **渲染排序无错乱 (Render Order Verification)**：
   - 剖切面开启或多重半透明图层重叠时，无 Alpha 深度穿透抖动，无闪烁 Z-fighting。

---

## Conclusion (结论)

本方案通过将视觉层级重置为“**高透水晶包覆壳 + 影棚抛光黄金核心机芯**”，在保留赛博暗夜模式下必要辅助自发光的同时，消除了盲目过曝自发光的廉价感。同时矫正了管网杂色，补齐了影棚级三点灯光阵列（尤其是暖金 Rim Light），为多维协同智能化排水自适应平台构建了稳固、优雅且符合顶级工程工业美学的三维可视化基石。
