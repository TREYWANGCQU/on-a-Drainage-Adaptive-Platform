<!-- 阶段4-第十一次-排水管标注响应收敛与两通三通放大镜方案.md -->
# 阶段4-第十一次-排水管标注响应收敛与环纵三通放大镜方案

## 一、问题背景与需求概述

在当前的 3D 隧道排水管网交互与微观局部放大视窗（`MagnifierPIP.vue`）功能中，经过实测诊断与核验，完成了以下两项核心技术治理与微观 3D 建模重构：

1. **点击与 Hover 响应精准收敛**：在 `Viewer3D.vue` 的 `handleCanvasClick` 与 `handleCanvasPointerMove` 拾取逻辑中，Raycaster 射线检测严格区分 3D 物理排水管几何体 Mesh（`InstancedMesh`）与 3D 悬浮标注文本框（`THREE.Sprite`）。收敛为**仅当拾取排水管标注文本框 (`isAnnotation === true`) 时才显示手型光标与弹出放大镜**，阻断物理 Mesh 的误触。
2. **放大镜二次打开空白防遗漏修复**：修复 `MagnifierPIP.vue` 在 `v-if="active"` 条件下由于 Canvas 节点卸载/重新挂载导致 `WebGLRenderer` 持久句柄失效及动画循环未重启的 Bug。增加 `disposePIPRenderer()` 函数，在 PIP 视窗关闭时彻底释放 WebGLRenderer 句柄与 GPU 显存，确保多次连续点选与反复弹窗无缝渲染且无内存泄露。
3. **环向盲管与纵向排水管三通节点微观示意**：精简管网节点标注，**只保留 1 个三通节点放大演示**，明确表达**环向盲管（拱形下落段）**与**纵向排水管（槽底主干）**的三通 T 型连接结构、琥珀金 PBR 金属汇流套壳及自上而下的水流汇入线段动画。

---

## 二、架构设计与修改边界

### 1. 涉及文件与模块

- `tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue`
  - **职责修改**：重构 `handleCanvasClick` 与 `handleCanvasPointerMove` 的 Raycaster 检测规则，增加 `hit.userData.isAnnotation === true` 拦截条件，排除物理 Mesh 的 Hover 与点击触发。
- `tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts`
  - **职责修改**：
    1. 为所有悬浮标注 Sprite 对象的 `userData` 注入 `isAnnotation: true` 显式属性标识；
    2. 计算并生成 1 个**环向盲管与纵向排水管三通连接节点**在 3D 管网侧边沟交汇处的空间坐标及悬浮标注 Sprite（`threeWaySprite`）。
- `tunnel-drainage-platform/frontend/src/components/three/MagnifierPIP.vue`
  - **职责修改**：
    1. 扩展 `pipeData` 接口与属性网格，支持 `nodeType: 'standard' | 'three-way'` 及 `pipeCategory: 'ring' | 'longitudinal' | 'lateral' | 'three_way'`；
    2. 新建程序化 3D 建模函数 `createThreeWayNodeGroup()`（纵向主管+环向盲管垂直汇入+琥珀金 T 型套管+多向汇流水流）；
    3. 增加 `disposePIPRenderer()` 资源解构与 WebGL 上下文彻底回收函数，重构 `watch([() => props.active, () => props.pipeData])` 监听，解决二次点选空白 Bug，确保连续点选不同标注框时 3D 视图平滑更新。

### 2. 交互与数据流转拓扑

```
[用户在 3D 画布中悬停 / 点击]
          │
          ▼
Viewer3D.vue (handleCanvasPointerMove / handleCanvasClick)
          │
          ▼
Raycaster 射线拾取 intersects
          │
          ├─► hit.userData.isAnnotation !== true ──► 忽略 (物理管道Mesh/衬砌/路面)
          │
          └─► hit.userData.isAnnotation === true
                  │
                  ├─► Hover: 设置 canvas.style.cursor = 'pointer'
                  │
                  ├─► Click: 提取 userData (包含 category, nodeType, name, diameter, status 等)
                  │
                  └─► set pipPipeData & isPipActive = true ──► 激活/更新 MagnifierPIP.vue
                                                                    │
                                                                    ▼
                                                 MagnifierPIP.vue 监听 active & pipeData:
                                                 ├─ active=false ──► disposePIPRenderer() 彻底销毁 WebGL 句柄与显存
                                                 └─ active=true  ──► 重新初始化/构建:
                                                     ├─ 'three-way' -> createThreeWayNodeGroup()
                                                     └─ 'standard'  -> createMicroPipeGroup()
```

---

## 三、详细设计方案

### 1. 标注框点击与 Hover 响应收敛 (Viewer3D.vue & DrainagePipeGenerator.ts)

#### 1.1 标注 Sprite 添加 `isAnnotation` 标识

在 `DrainagePipeGenerator.ts` 中，修改 `createTextSprite` 与 `updateAnnotations` 函数，确保生成的每一个 Sprite 对象的 `userData` 显式写入 `isAnnotation: true`。

```typescript
// DrainagePipeGenerator.ts
private createTextSprite(text: string, color = '#38bdf8'): THREE.Sprite {
  // ... canvas 绘图与材质构建 ...
  const sprite = new THREE.Sprite(spriteMaterial);
  sprite.renderOrder = 999;
  sprite.scale.set(6.0, 1.5, 1.0);
  sprite.userData = { isAnnotation: true }; // 默认写入标识
  return sprite;
}
```

#### 1.2 Raycaster 过滤器限制 (Click & Hover)

在 `Viewer3D.vue` 中，更新 `handleCanvasPointerMove` 与 `handleCanvasClick` 逻辑：

```typescript
// Viewer3D.vue
const handleCanvasPointerMove = (event: MouseEvent) => {
  if (!canvasRef.value || !camera) return;
  // ... mouseVec & setFromCamera ...
  const intersects = raycaster.intersectObjects(activeMeshes, true);

  if (intersects.length > 0) {
    const hit = intersects[0].object;
    if (hit && hit.userData && hit.userData.isAnnotation === true && (hit.userData.pipeCategory || hit.userData.name)) {
      canvasRef.value.style.cursor = 'pointer';
      return;
    }
  }
  if (!isMeasuring.value) {
    canvasRef.value.style.cursor = 'default';
  }
};

const handleCanvasClick = (event: MouseEvent) => {
  // ... mouseVec & setFromCamera ...
  const intersects = raycaster.intersectObjects(activeMeshes, true);

  if (isMeasuring.value) {
    // 测距模式逻辑保持不变
    return;
  }

  // 非测距模式：仅当点击 3D 悬浮文字标注框 (isAnnotation === true) 时才弹出/更新 PIP 局部放大镜
  if (intersects.length > 0) {
    const hit = intersects[0].object;
    if (hit && hit.userData && hit.userData.isAnnotation === true && (hit.userData.pipeCategory || hit.userData.name)) {
      pipPipeData.value = { ...hit.userData };
      isPipActive.value = true;
      return;
    }
  }

  // 点击 3D 画布空白区或非标注对象，自动收起 PIP 画中画镜头
  if (isPipActive.value) {
    isPipActive.value = false;
  }
};
```

---

## 四、环向盲管与纵向排水管三通节点拓扑 (DrainagePipeGenerator.ts)

在 `DrainagePipeGenerator.ts` 的 `updateAnnotations()` 函数中，仅保留 1 个三通节点标注：

- **定位**：位于侧边沟槽底环向盲管拱脚与纵向主排水管交汇处。
  `position = (mainXOffset + ditchGeo.sideDitchX, ditchGeo.sideDitchBottomY + 0.4, -startChainage - length * 0.25)`
- **Sprite 文本**：`${prefix}三通连接节点: 环向盲管-纵向排水管 🔍`
- **userData**：
  ```typescript
  {
    isAnnotation: true,
    pipeCategory: 'three_way',
    nodeType: 'three-way',
    name: '环向盲管与纵向排水管三通节点',
    diameter: 100,
    spacing: parseFloat(ringSpacingM),
    permeability: '三向汇排 3.5×10⁻² cm/s',
    flowRate: '0.60 L/s',
    status: '高效汇排'
  }
  ```

---

## 五、微观 3D PIP 视窗模型构建与生命周期回收 (MagnifierPIP.vue)

### 1. 建模拓展

1. **`createMicroPipeGroup()` (标准打孔直管模型)**：
   保留打孔槽结构与无纺土工布包覆网格。
2. **`createThreeWayNodeGroup()` (环向盲管与纵向排水管三通汇流模型)**：
   - 纵向主排水管：横向卧铺 `CylinderGeometry(0.075, 0.075, 0.9)`；
   - 环向盲管下落段：垂直方向自顶部汇入 `CylinderGeometry(0.06, 0.06, 0.45)`；
   - T 型三通包壳：交汇处添加琥珀金 PBR 金属质感强化包壳（`SphereGeometry(0.092)`）；
   - 汇流水流：包含纵向主管直通水流粒子与环向盲管自顶向下垂直汇入转向水流粒子。

### 2. WebGL 资源 Dispose 与生命周期修复

为解决 `v-if` DOM 重新挂载后 Canvas 句柄失效导致第二次放大空白的问题，实现 `disposePIPRenderer()` 函数：

```typescript
const disposePIPRenderer = () => {
  stopAnimation();
  if (scene && pipeMesh) {
    scene.remove(pipeMesh);
    pipeMesh.traverse((child) => {
      if ((child as THREE.Mesh).geometry) (child as THREE.Mesh).geometry.dispose();
      if ((child as THREE.Mesh).material) {
        const mat = (child as THREE.Mesh).material;
        if (Array.isArray(mat)) mat.forEach(m => m.dispose());
        else mat.dispose();
      }
    });
    pipeMesh = null;
    waterStream = null;
    branchStream = null;
  }
  if (renderer) {
    renderer.dispose();
    renderer = null;
  }
  scene = null;
  camera = null;
};

watch([() => props.active, () => props.pipeData], ([newActive, _newPipeData]) => {
  if (newActive) {
    if (!renderer) {
      setTimeout(() => initPIPRenderer(), 50);
    } else {
      rebuildPIPScene();
      startAnimation();
    }
  } else {
    disposePIPRenderer();
  }
}, { deep: true });
```

---

## 六、工作分解与验收标准 (WBS & Acceptance Criteria)

### Work Breakdown Structure (WBS)

1. **WBS 1: 拾取与 Hover 拦截逻辑收敛**
   - 给 `DrainagePipeGenerator.ts` 标注 Sprite 添加 `isAnnotation = true` 属性；
   - 在 `Viewer3D.vue` 中过滤物理管道 Mesh，仅对 Sprite 响应手型光标与点击。
2. **WBS 2: 三通节点拓扑与标注精简**
   - 精简 `DrainagePipeGenerator.ts` 标注，仅保留 1 个环向盲管与纵向排水管交汇的三通节点悬浮标注；
   - 设置精准的 3D 空间高程与坐标。
3. **WBS 3: 微观 3D 建模与水流汇入动画**
   - 在 `MagnifierPIP.vue` 中实现 `createThreeWayNodeGroup()` 与自顶向下的水流汇入线段；
   - 适配 UI Badge 与工程参数看板。
4. **WBS 4: WebGL 生命周期与内存回收**
   - 实现 `disposePIPRenderer()`，彻底解决第二次放大视窗空白的 Canvas/Renderer 状态错配 Bug。

### 验收标准 (Acceptance Criteria)

1. **点击与 Hover 精确定位**：在 3D 视图中悬停或点击物理管道 Mesh 时不改变光标形态亦不弹出放大镜；只有悬停或点击排水管标注文本框时才正确切换光标并弹出 PIP 视窗。
2. **二次放大正常渲染**：关闭放大镜后，再次点选任意标注框，放大镜 3D 视图能够正常初始化并平滑展现微观 3D 模型与动画，无空白/黑屏现象。
3. **三通连接表达精准**：放大镜内呈现环向盲管自顶部垂直汇入纵向主排水管的 T 型三通结构与双向汇流水流线段。
4. **稳定与无泄露**：放大镜频繁弹窗与反复切换，控制台无报错，GPU 显存及 WebGL 上下文稳定释放。
