# 阶段4-第十二次-排水管径倍数放大会显与一键还原控件方案

## Objectives

1. **解决管网尺度显隐冲突**：由于排水管物理管径（$\phi 50\text{mm} \sim 100\text{mm}$）相对隧道三维空间断面（跨度 $10\text{m} \sim 15\text{m}$）尺度极小（$\approx 1/100$），在远景或全景视角下难以清晰观察。本方案旨在提供三维管网倍数放大显示能力，大幅提升多维协同排水自适应平台的可视化观测与交互体验。
2. **构建高保真局部放缩算法**：在 `DrainagePipeGenerator.ts` 中建立管径局部放缩（Scaling）解耦机制。保持马蹄形衬砌贴合中轴线与管网拓扑坐标不动，仅拉伸管径横截面半径，避免出现管道脱离衬砌或管线错位。
3. **设计拟物化 Glassmorphism 界面控件**：在 `Viewer3D.vue` 左侧交互控制栈（`.left-controls-stack`）中集成【排水管径放大会显】控制卡片。提供连续滑块调节（$1.0\times \sim 10.0\times$）、预设快捷切片（$1.0\times / 3.0\times / 5.0\times / 8.0\times$）以及一键还原真实物理管径的极速响应按钮。

---

## Constraints

1. **几何拓扑保真约束**：管径放大仅改变管道截面粗细（Radius），不得改变管道长度（Length）、倾斜坡度（3% 横坡）以及环向管在衬砌背水面的弧线路径轨迹。
2. **性能零开销约束**：纵向管 (`longMesh`) 与横向管 (`latMesh`) 必须通过 GPU InstancedMesh 矩阵局部向量放缩（$\mathbf{S} = [S_{scale}, 1.0, S_{scale}]$）实现 $0\text{ms}$ 卡顿实时拉伸；环向管 (`ringMesh`) 必须轻量化重新烘焙 `TubeGeometry`，重构耗时需 $< 2\text{ms}$。
3. **UI 视觉与状态同步约束**：控件样式必须严格遵循系统 Glassmorphism 暗色水晶风格，控件显隐与【排水管网】图层勾选状态深度联动；场景切换或数据快照更新时，管径倍率状态需保持连续与一致。

---

## Architecture

### 1. 模块分解与架构拓扑 (Module Architecture)

```
+-----------------------------------------------------------------------------------+
| Viewer3D.vue (界面交互与状态调度层)                                                |
|                                                                                   |
|  +-----------------------------------------------------------------------------+  |
|  | .left-controls-stack (左侧控制面板栈)                                       |  |
|  |  +-----------------------------------------------------------------------+  |  |
|  |  | [排水管放大会显控制卡片] (.drainage-scale-card.glass-card)             |  |  |
|  |  |  - 当前倍率 Badge (e.g. "3.5x" / "1.0x 真实物理管径")                   |  |  |
|  |  |  - 连续倍率滑动条 (Slider: 1.0x ~ 10.0x, step=0.5)                     |  |  |
|  |  |  - 快捷预设 Pills ([1.0x 真实] [3.0x 适中] [5.0x 高显] [8.0x 特写])       |  |  |
|  |  |  - [↺ 一键还原真实管径] 极速重置按钮                                   |  |  |
|  |  +-----------------------------------------------------------------------+  |  |
|  +-----------------------------------------------------------------------------+  |
|                                     |                                             |
|                                (scaleFactor)                                      |
|                                     v                                             |
|  +-----------------------------------------------------------------------------+  |
|  | DrainagePipeGenerator.ts (排水管网三维几何生成器)                          |  |
|  |                                                                             |  |
|  |  - setPipeScaleFactor(scaleFactor: number)                                  |  |
|  |                                                                             |  |
|  |  +---------------------------+     +-------------------------------------+  |  |
|  |  | ringMesh (环向盲管)       |     | longMesh / latMesh (纵向与横向管)   |  |  |
|  |  | 重建 TubeGeometry(r*scale)|     | 矩阵组合 S = (scale, length, scale) |  |  |
|  |  +---------------------------+     +-------------------------------------+  |  |
|  +-----------------------------------------------------------------------------+  |
+-----------------------------------------------------------------------------------+
```

---

### 2. 数学模型与矩阵解算 (Mathematical Modeling)

#### (1) 环向马蹄形盲管 (`ringMesh`) 截面放缩

环向管基于 `TubeGeometry` 生成。给定原始推荐内径 $d_{ring}$（物理半径 $r_{ring} = d_{ring} / 2$）与放大倍率 $S \in [1.0, 10.0]$：
$$r_{tube}(S) = \max\left(0.01, \frac{d_{ring}}{2} \cdot S\right)$$

重新生成 Geometry 并替换：
$$\text{mesh.geometry.dispose}()$$
$$\text{mesh.geometry} = \text{new THREE.TubeGeometry}(\text{horseshoeCurve}, 64, r_{tube}(S), 8, \text{false})$$

#### (2) 纵向管 (`longMesh`) 与横向管 (`latMesh`) 矩阵 Scaling

纵向管与横向管为沿局部 Y 轴延展的柱体 `CylinderGeometry`。设原始半径 $r_{base}$，生成单位长度/指定半径柱体。在构建变换矩阵 $\mathbf{M}$ 时：

- **纵向管矩阵组合**：
  $$\mathbf{S}_{long} = \begin{bmatrix} S & 0 & 0 \\ 0 & L_{tunnel} & 0 \\ 0 & 0 & S \end{bmatrix}$$
  $$\mathbf{M}_{long} = \mathbf{T}(x_{ditch}, y_{ditch} + r_{base} \cdot S, z_{mid}) \cdot \mathbf{R}_{x}(\frac{\pi}{2}) \cdot \mathbf{S}_{long}$$

- **横向管矩阵组合**（包含 3% 横坡倾斜 Quaternion $\mathbf{Q}_{lat}$）：
  $$\mathbf{S}_{lat} = \begin{bmatrix} S & 0 & 0 \\ 0 & L_{pipe} & 0 \\ 0 & 0 & S \end{bmatrix}$$
  $$\mathbf{M}_{lat} = \mathbf{T}(x_{mid}, y_{mid} + r_{base} \cdot S, z_{pos}) \cdot \mathbf{Q}_{lat} \cdot \mathbf{S}_{lat}$$

利用局部 Scaling，纵横向管道仅改变横截面圆半径，长度 $L$ 与管道轴线姿态角度保持 100% 物理精准！

#### (3) 双洞模式副洞网格 (`leftRingMesh` / `leftLongMesh` / `leftLatMesh`) 缩放与 Geometry 安全回收

- **全图层覆盖**：在 `setPipeScaleFactor(S)` 与内部矩阵更新逻辑中，必须同步作用于主洞与双洞副洞（`leftRingMesh` / `leftLongMesh` / `leftLatMesh`）。
- **显存与指针安全**：当 `leftRingMesh` 与 `ringMesh` 共享同一个 `TubeGeometry` 时，在重建几何体时仅对旧 Geometry 执行一次 `.dispose()`，并同步更新 `leftRingMesh.geometry = newTubeGeo`，防止出现 WebGL null 指针或重复 dispose 异常。

#### (4) 状态持久化与快照刷新响应机制 (State Persistence)

- **私有状态绑定**：将 `scaleFactor` 绑定为 `DrainagePipeGenerator` 内部私有成员变量（默认 `1.0`）。当后端数据刷新或视图模式（`original` / `critical` / `all`）切换触发 `updateFromSnapshot()` 时，矩阵重算天然沿用该 `this.scaleFactor`，防止几何缩放被擦除。
- **重新实例化同步**：在 `Viewer3D.vue` 的 `update3DScene` 中，新建 `DrainagePipeGenerator` 实例后，立即调用 `pipeGen.setPipeScaleFactor(pipeScaleFactor.value)`，确保数据重新加载时 3D 管网粗细与 UI 界面滑块当前显示值 100% 连续一致。

#### (5) 与 MagnifierPIP 节点放大镜解耦机制

- **局部物理保真**：`MagnifierPIP.vue` 节点放大镜 PIP 视口内部的 3D 节点模型固定维持 $1.0\times$ 真实物理管径尺寸（或独立参数控制），确保局部节点细节观测的物理精度不受全景倍数放大会显的视觉拉伸干涉。

---

### 3. UI 控件位置与结构设计 (UI Control Design & Layout)

#### (1) 最佳摆放位置
位于 `Viewer3D.vue` 的 `<div class="left-controls-stack">` 中，紧跟在【图层显隐控制】(`layer-panel`) 面板下方。只有在【排水管网】图层启用（`layerVisibility.pipes === true`）时动态显示，保证界面整洁高效。

#### (2) HTML / Vue 结构规范
```html
<!-- 排水管径放大会显控制面板 -->
<div v-if="layerVisibility.pipes" class="pipe-scale-card glass-card">
  <div class="panel-header">
    <span class="panel-title">排水管径放大会显</span>
    <span class="scale-badge" :class="{ active: pipeScaleFactor > 1.0 }">
      {{ pipeScaleFactor === 1.0 ? '1.0x 真实' : `${pipeScaleFactor.toFixed(1)}x` }}
    </span>
  </div>
  <div class="panel-body">
    <!-- 连续倍率滑动条 -->
    <div class="control-row">
      <span class="control-label">管径倍率:</span>
      <input 
        type="range" 
        min="1.0" 
        max="10.0" 
        step="0.5" 
        v-model.number="pipeScaleFactor"
        @input="onPipeScaleInput"
        class="range-slider"
      />
    </div>

    <!-- 预设倍率 Pills 与一键还原按钮 -->
    <div class="scale-action-row">
      <div class="preset-btn-group">
        <button 
          v-for="preset in [1.0, 3.0, 5.0, 8.0]" 
          :key="preset"
          class="preset-pill-btn"
          :class="{ active: pipeScaleFactor === preset }"
          @click="setPipeScale(preset)"
        >
          {{ preset === 1.0 ? '真实' : `${preset}x` }}
        </button>
      </div>
      <button 
        class="reset-scale-btn" 
        :disabled="pipeScaleFactor === 1.0"
        @click="resetPipeScale"
        title="重置为真实物理管径 (1.0x)"
      >
        ↺ 一键还原
      </button>
    </div>
  </div>
</div>
```

---

## Work Breakdown Structure

### Phase 1: 3D 逻辑层代码扩充 (`DrainagePipeGenerator.ts`)
- [ ] **Task 1.1**: 添加 `scaleFactor: number = 1.0` 内部私有状态与 `public setPipeScaleFactor(scale: number)` 方法，支持双洞模式网格 (`leftRingMesh` / `leftLongMesh` / `leftLatMesh`) 100% 覆盖。
- [ ] **Task 1.2**: 改造 `createRingInstancedMesh` 与环向管更新逻辑，支持结合倍率动态生成 `TubeGeometry`，并处理显存 `.dispose()` 与双洞共享指针保护。
- [ ] **Task 1.3**: 改造 `updateLongPipes` 与 `updateLatPipes` 中的 `InstancedMesh` 矩阵合成逻辑，引入 X/Z 局部放缩向量 $\mathbf{S} = [S, 1.0, S]$，并结合 `this.scaleFactor` 保证 `updateFromSnapshot()` 触发时倍率不重置。
- [ ] **Task 1.4**: 提供管径缩放后的 `userData` 扩展，确保框选与标注提示显示正确的原始物理管径与放大倍数。

### Phase 2: Vue 视图与控件集成 (`Viewer3D.vue`)
- [ ] **Task 2.1**: 定义响应式变量 `const pipeScaleFactor = ref<number>(1.0)`。
- [ ] **Task 2.2**: 在 `left-controls-stack` 中新增 `.pipe-scale-card` 拟物化 Glassmorphism 界面卡片。
- [ ] **Task 2.3**: 实现 `onPipeScaleInput`、`setPipeScale` 和 `resetPipeScale` 调度逻辑，遍历 `pipeGenInstances` 实施即时缩放并触发 `scheduleRender()`；在 `update3DScene` 实例化后主动调用 `pipeGen.setPipeScaleFactor(pipeScaleFactor.value)` 保持状态连续。
- [ ] **Task 2.4**: 编写 CSS 样式，确保 Badge 高亮、Preset Pills 选中状态与一键还原按钮样式符合现代深色影棚/赛博双范式美学。

---

## Acceptance Criteria

1. **响应实时性**：拖动管径倍率滑动条（Slider）或点击预设按钮时，3D 视图中环向、纵向、横向排水管网粗细瞬间响应，无死锁、无顿挫（FPS $\ge 50$）。
2. **一键还原准确度**：点击【↺ 一键还原】按钮后，`pipeScaleFactor` 瞬间恢复至 `1.0`，排水管网 100% 恢复为后端传入的原始物理管径（如 $\phi 50\text{mm}$、$\phi 100\text{mm}$）。
3. **几何位姿稳定性**：管径在 1.0x ~ 10.0x 任意放大过程中，管道中心轴线姿态保持稳定，不发生空间漂移、离空或与水沟槽底错位脱节。
4. **UI 体验完备性**：仅在【排水管网】图层勾选时显示放大会显面板，控件在 1.0x 真实管径时“一键还原”按钮自动柔和置灰，交互语义明确、清晰。
5. **快照与模式切换连贯性**：在工况模式切换（原始/临界/全选）或数据快照刷新时，管网缩放倍数稳定维持用户当前选择的 UI 倍率（如 $5.0\times$），不发生降级重置。
6. **双洞拓扑同步性**：在双洞模式下，副洞管网与主洞管网缩放倍率及几何贴合 100% 同步，且无 GPU 显存泄漏风险。
