这是一个非常敏锐且关键的问题。我们前面编写了 `database.py`（路由定义）和 `init_db.py`（初始化事件），但**它们确实需要被后端的主入口点显式调用和注册，否则 FastAPI 根本不知道它们的存在。**

这个核心的调用位置就是后端的启动入口文件：**`backend/app/main.py`**。

为了确保整个本地数据库流转清晰落地，以下为您梳理从“0 到 1”的完整数据库集成与操作指南，包含具体挂载位置与排错策略：

---

### Phase 1: 数据库创建与配置 (Creation)

**目标**：配置底层驱动，让 Python 能够与 SQLite 建立异步连接。
**操作文件**：`backend/app/db/session.py`
**执行动作**：

1. 定义 `DATABASE_URL = "sqlite+aiosqlite:///./tunnel_params.db"`。
2. 实例化 `create_async_engine`。这里针对 SQLite 的特性，必须显式传入 `connect_args={"check_same_thread": False}`，以允许 FastAPI 的异步多线程安全访问。
3. 创建会话工厂 `AsyncSessionLocal`。

---

### Phase 2: 数据模型定义 (Definition)

**目标**：将 Python 类映射为数据库中的物理表结构（ORM）。
**操作文件**：`backend/app/models/domain.py`
**执行动作**：

1. 定义继承自 `Base` 的 `TunnelParameter` 类。
2. 设定 `__tablename__ = "tunnel_parameters"`。
3. 严格定义字段：`id`、`project_name`、`tunnel_type`、`parameters_json`。
4. 使用 `lambda: datetime.now(timezone.utc)` 动态生成标准时区的创建时间。

---

### Phase 3: 数据库初始化 (Initialization)

**目标**：在服务启动的瞬间，检查表是否存在；若不存在则建表，并写入“黄桷坪长江大桥”等基准演示数据。
**操作文件**：`backend/app/db/init_db.py`
**执行动作**：

1. 编写异步函数 `init_models()`，包含建表 (`create_all`) 与数据注入逻辑。
2. **核心机制**：利用 FastAPI 的 `lifespan` 异步上下文管理器将 `init_models()` 包装起来。这确保了初始化只在 App 启动前执行一次。

---

### Phase 4: API 路由定义 (Routing)

**目标**：向前端暴露 RESTful 接口以实现增删改查。
**操作文件**：`backend/app/api/v1/endpoints/database.py`
**执行动作**：

1. 实例化 `router = APIRouter()`。
2. 编写依赖注入函数 `get_db()`，确保每个请求获取独立的数据库 Session，并在请求结束后自动释放。
3. 定义具体的路由函数，如 `@router.post("/parameters")` 和 `@router.get("/parameters")`，并在这里调用 SQLAlchemy 执行具体的 SQL 动作。

---

### Phase 5: 全局注册与调用 (Invocation) - **关键闭环**

**目标**：把前面写好的生命周期事件（Lifespan）和路由（Router）挂载到 FastAPI 实例上。
**操作文件**：**`backend/app/main.py`**
**执行动作**：按照以下结构补充或重构您的 `main.py`：


---

### Phase 6: 联调与调试 (Debugging)

完成挂载后，本地数据库环境即插即用。遵循以下专业调试流程：

#### 1. 启动验证

* 终端运行 `uvicorn main:app --reload`。
* **看日志**：终端必须出现两行关键信息：`INFO: Application startup complete.` 以及您在 init_db 打印的 `Database Initialized and Seeded.`。
* **看文件**：根目录下必须出现一个名为 `tunnel_params.db` 的物理文件。

#### 2. 可视化查看物理库 (推荐工具)

作为开发者，不要仅仅依赖接口返回值，建议下载轻量级工具 **DB Browser for SQLite** 或使用 VS Code 插件 **SQLite Viewer**。

* 用工具直接打开 `tunnel_params.db`。
* 检查 `tunnel_parameters` 表是否成功创建，以及 `parameters_json` 字段内是否存入了规范的 JSON 字符串。

#### 3. 接口联调排错

打开 `http://localhost:8000/docs` 进行联调。

* **422 Unprocessable Entity**：前端传入的 JSON 格式与 `ParameterCreate` 模型不匹配。检查 `project_name` 或 `tunnel_type` 是否漏传，或者 `parameters_json` 的数据类型不对。
* **500 Internal Server Error (Database Locked)**：由于 SQLite 是单文件数据库，如果并发写入或异步 Session 没有正确关闭，会报锁死。确认接口中严格使用了 `Depends(get_db)` 来管理 Session。
* **时间偏移**：如果在表中看到的时间比北京时间晚了 8 小时，这是预期的标准行为（入库用 UTC 标准时间，前端展示时再由 Vue/JavaScript 自动转换为用户本地时间 GMT+8）。