<!-- 阶段4-3D模型解算与可视化对应关系核算报告.md -->

# 阶段 4 - 3D 模型解算与可视化对应关系核算报告

本文档依据 Verification Specialist（核算专家）对后端核心计算引擎（`tunnel-drainage-platform/backend/app/services/drainage_engine.py`）、力学求解器（`mechcalc.py`）、前端快照规范（`阶段4-snapshot的特点提取.md`）以及前端 3D 可视化渲染引擎（`Viewer3D.vue`、`PostProcessing.ts`、`Reinforcement.ts`、`DrainagePipeGenerator.ts`、`Environment.ts`）的逐行代码审核、空间坐标几何推导与数据流核算，给出全面严肃的验证结论与更新说明。

---

## 1. 核算综合状态 (VERIFIER STATUS)

```text
STATUS: FAIL
```

> **判定说明**：
> 经过与 `阶段4-snapshot的特点提取.md` 的逐条比对及 3D 渲染组件的代码核算，当前 3D 可视化与后端解算引擎的映射存在多项严重的逻辑错位与渲染缺陷：
> 1. **受力单位放大 1000 倍**：Overlay 标量看板与探针悬浮框将后端原始 $\text{N}$ 与 $\text{N}\cdot\text{m}$ 未经千分位缩放直接标示为 $\text{kN}$ 与 $\text{kN}\cdot\text{m}$。
> 2. **图层划分与结构属性混淆**：缺失【初始注浆圈】（外半径为输入参数 $r_g$）图层；原【注浆加固圈】未重命名为【临界注浆加固圈】（厚度为计算值 $t_{g\_crit}$）；保留了阶段4不应存在的【系统锚杆】图层。
> 3. **排水管网表达不当与几何半径混淆**：盲管 Mesh 几何误将管径（Diameter）当作半径（Radius）导致粗细翻倍；且排水管相对于隧道整体尺寸较小（0.05~0.1m vs 10~12m），缺乏间距、分布、尺寸的图层标注与合理的 3D 增强表达。
> 4. **双洞空间坐标错位**：双洞工况（`tunnel_type == "double"`）下，【水文环境】图层（水流粒子与辐射流线）与【最不利探针】图层未叠加 $\pm D_{spacing}/2$ 的 X 轴平移矩阵，导致粒子与探针在左右双洞中间的空气中悬空。
> 5. **最不利受力单元安全系数不一致**：`PostProcessing.ts` 模式硬编码优先读取 `critical_state`，导致在原始状态视图下探针读取临界安全系数；同时后端求解器（100 节点）与前端（24 单元）索引未重采样对齐，导致数组越界回退取错数值。
> 6. **受力单元 #0 成因与里程映射缺失**：最不利单元 `#0` 系后端 100 节点索引越界后前端 `nodes[controlIdx]` 强制回退至 `nodes[0]` 的异常表现，且探针悬浮框未与隧道的【分区里程】（`start_chainage ~ end_chainage`）绑定。

---

## 2. 快照特征契约对照核算 (Snapshot Feature Verification)

依据 [阶段4-snapshot的特点提取.md](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/%E9%98%B6%E6%AE%B54-snapshot%E7%9A%84%E7%89%B9%E7%82%B9%E6%8F%90%E5%8F%96.md) 的 4 大核心章节，核算 3D 渲染映射一致性：

| 规范章节 | 核心字段/约束条件 | 后端解算/快照契约 (`drainage_engine.py`) | 前端 3D 渲染实现与对应关系核算结论 | 核算状态 |
| --- | --- | --- | --- | --- |
| **1. 空间拼装** | `start_chainage` / `end_chainage` | 决定分区里程起点与终点 (m) | 驱动 3D 画布 Z 轴绝对偏移 ($Z = -start\_chainage$) 与拉伸深度 ($L = end\_chainage - start\_chainage$)。 | **PASS** |
| **2. 几何映射** | `r`, `r1`, `r2`, `aspect_ratio` | 马蹄形断面内半径、二衬外半径、初支外半径、高宽比 | 驱动 `TunnelGenerator.ts` 构建三心圆拱轮廓 Shape，二衬厚度 $t_{lining}=r_1-r$，初支厚度 $t_{primary}=r_2-r_1$。 | **PASS** |
| **2. 几何映射** | `rg` | 初始注浆圈外半径 $r_g$ (m) | 需在 3D 中渲染【初始注浆圈】实体，厚度 $t_g = r_g - r_2$。前端 `Reinforcement.ts` 已具备计算，但 UI 图层面板未独立解耦。 | **FAIL_WITH_WARNING** |
| **2. 几何映射** | `rg_crit`, `tg_crit` | 临界注浆圈外半径与厚度 (m) | 需在 3D 中渲染【临界注浆加固圈】实体，厚度 $t_{g\_crit} = \max(0, r_{g\_crit} - r_2)$。UI 图层误标为通用“注浆加固圈”。 | **FAIL_WITH_WARNING** |
| **2. 几何映射** | `D_spacing`, `tunnel_type` | 双洞间距与类型 (`"single"`/`"double"`) | 当为 `"double"` 时，`TunnelGenerator` 与 `Reinforcement` 已做 $\pm D/2$ 平移；但 **`Environment` 与 `PostProcessing` 漏做平移**。 | **FAIL** |
| **3. 状态驱动** | `safety_factor` / `final_safety_factor` | 原始/临界状态衬砌最小安全系数 | 传入 `PostProcessing.ts` 驱动 K 云图色阶。但探针读取逻辑硬编码优先读 `critical_state` 导致原始视图显示错乱。 | **FAIL** |
| **3. 状态驱动** | `control_idx`, `control_M`, `control_N` | 最不利受力单元索引及内力 | 后端返回 100 节点数组的索引；前端只有 24 单元，数组越界回退致使探针固定指在拱顶 `#0`，且内力单位未除以 1000。 | **FAIL** |
| **3. 状态驱动** | `ring_diam_recommend`, `ring_spacing_recommend` | 推荐环向盲管管径与间距 (m) | 驱动 `DrainagePipeGenerator.ts` 动态调整 InstancedMesh 缩放与间距。但 TubeGeometry 误将管径当半径使用。 | **FAIL** |
| **4. 容错降级** | `extractSnapshotValue` 取值链 | 级联降级 `critical` $\rightarrow$ `original` $\rightarrow$ `input` | 前端 `snapshotStore.ts` 实现了完整的链式降级取值与 pending 状态过滤。 | **PASS** |

---

## 3. 已通过核算项 (VERIFIED ITEMS)

### 3.1 空间里程拼装与几何断面扫描

* **空间连续性**：[Viewer3D.vue:L685-L718](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue#L685-L718) 通过 `start_chainage` 设定 3D 网格的 Z 轴绝对基准偏移，多段隧道拼接无缝。
* **马蹄形断面**：`TunnelGenerator.ts` 正确解析 $r_0, r_1, r_2$ 与高宽比 `aspect_ratio`，光滑生成拱顶大圆弧、边墙小圆弧与仰拱反弯弧。

### 3.2 排水管网自适应加密与富水响应

* **涌水量响应**：[DrainagePipeGenerator.ts:L340-L365](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts#L340-L365) 根据涌水量 $q_{drain} > 1.5\,\text{m}^3/(\text{m}\cdot\text{d})$ 自动触发 50% 盲管间距加密，并按风险等级（天蓝 $\rightarrow$ 琥珀黄 $\rightarrow$ 警示红）完成材质动态着色。

---

## 4. 警告事项 (WARNINGS)

### 4.1 临界注浆圈 Mesh 延迟实例化失效风险

* **代码节点**：[Reinforcement.ts:L137-L149](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/Reinforcement.ts#L137-L149)
* **诊断分析**：若 `ReinforcementManager` 初始实例化时 snapshot 未包含 `critical_state`，`criticalGroutingMesh` 未在构造函数中预创建。后续异步更新调用 `updateFromSnapshot` 时，由于网格未加入 Scene，导致橙色临界加固注浆圈无法渲染。

---

## 5. 缺陷与错误 (ERRORS)

### 5.1 【致命】最不利探针内力数值与单位放大 1000 倍

* **代码节点**：
  - 后端求解：[mechcalc.py:L184-L185](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/backend/app/services/mechcalc.py#L184-L185) 输出的 `N_elem` 单位为 $\text{N}$，`M_elem` 单位为 $\text{N}\cdot\text{m}$。
  - 后端透传：`drainage_engine.py` 中 `control_N = N[control_idx] / 1000.0` 已做转换，但在部分字段或前端 Overlay 处未校验，导致透传的原始大数值直接拼接 `kN` 字符。
* **实际后果**：实际轴力 $-1523.5\,\text{kN}$ 被错误显示为 `-1523450.0 kN`，弯矩 $125.4\,\text{kN}\cdot\text{m}$ 被显示为 `125430.0 kN·m`，数值夸大 1000 倍。

### 5.2 图层划分不符合规范（缺【初始注浆圈】、临界圈命名不准、残留【系统锚杆】）

* **代码节点**：[Viewer3D.vue:L61-L70](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue#L61-L70)
* **诊断分析**：
  1. UI 图层面板仅有一个通用的“注浆加固圈”复选框，未拆分为：
     - **【初始注浆圈】**：外半径为输入参数 $r_g$，厚度 $t_g = r_g - r_2$。
     - **【临界注浆加固圈】**：外半径为计算出来的 $r_{g\_crit}$，厚度为 $t_{g\_crit}$。
  2. UI 面板仍保留阶段4不再需要的【系统锚杆】图层（`layerVisibility.bolts`），造成视觉干扰与性能浪费。

### 5.3 【排水管网】图层尺寸微小表达不当与几何半径混淆

* **代码节点**：[DrainagePipeGenerator.ts:L118-L120](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts#L118-L120)
* **诊断分析**：
  1. **半径混淆**：`TubeGeometry` 第三个参数传入了管径 $d$（如 0.05m），导致生成的管道 3D 粗细翻倍。
  2. **尺度表达缺失**：排水管网尺寸相对于 10m~12m 的隧道主体极小（$d=0.05\sim 0.1\text{m}$），在全景视图下极易被遮挡或难以看清。缺少独立的尺寸、分布、间距图层 Overlay 标注（如 `间距: 5.0m, 管径: Φ100mm`）以及合理的 3D 视觉高亮/防穿模发光表达。

### 5.4 双洞工况下【水文环境】与【最不利探针】空间坐标错位

* **代码节点**：
  - [Environment.ts:L297-L298](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/Environment.ts#L297-L298)
  - [PostProcessing.ts:L472-L530](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/PostProcessing.ts#L472-L530)
* **诊断分析**：
  在双洞模式（`tunnel_type == "double"`，如 $D_{spacing} = 30\text{m}$）下，主洞与副洞轴线分别位于 $X = +15\text{m}$ 与 $X = -15\text{m}$。然而：
  1. `Environment.ts` 的动水流线与水流粒子生成的坐标系死板锚定在 $X = 0$，水流粒子向 $X = 0$ 的虚无中央汇集，没有与左右主副洞坐标匹配。
  2. `PostProcessing.ts` 生成的探针 (`probeGroup`) 和受力图 (`diagramGroup`) 仅针对 $X = 0$ 处的单洞生成，未响应双洞 $X = \pm D_{spacing}/2$ 的平移矩阵，导致双洞模式下探针悬空在两洞中央。

### 5.5 【最不利受力单元 #0】安全系数与【云计算生成节点】不一致

* **代码节点**：[PostProcessing.ts:L438-L451](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/PostProcessing.ts#L438-L451)
* **诊断分析**：
  `const state = snapshot?.critical_state ?? snapshot?.original_state ?? {};`
  硬编码逻辑导致只要 snapshot 中存在 `critical_state`，无论前端处于【原始状态】还是【临界加固状态】视图，`PostProcessing.ts` 都会强行提取 `critical_state.final_safety_factor`（如 2.01）。当用户在分屏左侧查看原始状态（原始安全系数如 1.42）时，3D 探针上却错误地显示着临界状态的 2.01，造成探针数值与云计算看板的原始/临界状态安全系数严重不一致。

### 5.6 最不利单元 #0 的成因诊断与里程映射缺失

* **成因定位**：
  1. **节点采样维度冲突**：后端 `mechcalc.py` 采用 $N_{nodes} = 100$ 的有限元网格，求解出的 `control_idx` 范围为 `0 ~ 99`（例如拱脚处 `control_idx = 42`）。而前端 `PostProcessing.ts` 的 `computeContinuousSectionNodes` 仅构建了 24 个离散单元（`0 ~ 23`）。
  2. **越界回退机制**：在 `PostProcessing.ts:L488` 中执行 `const controlNode = nodes[controlIdx] || nodes[0]` 时，由于 `42 >= 24`，`nodes[42]` 返回 `undefined`，代码无奈降级回退至 `nodes[0]`。这就导致 3D 探针永远被死死钉在 0 号单元（拱顶正上方）！
  3. **里程信息缺失**：当前悬浮框仅孤零零展示 `最不利受力单元 #0`，未标明该最不利断面属于隧道的哪个【分区里程】（如 `K10+000 ~ K10+050`），失去了工程空间定位意义。

---

## 6. 最小修复建议 (MINIMAL FIX SUGGESTIONS)

### 6.1 修复图层拆分与 UI 控制（新增初始注浆圈、重命名临界圈、移除锚杆）

修改 [Viewer3D.vue](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue#L56-L86) 图层控制面板，拆分注浆圈并移除系统锚杆：

```html
<!-- frontend/src/components/three/Viewer3D.vue -->
<div v-if="isLayerPanelOpen" class="panel-body layer-body">
  <label class="layer-item">
    <input type="checkbox" v-model="layerVisibility.lining" @change="updateLayerVisibility" />
    <span class="layer-color-dot lining-dot"></span>
    <span class="layer-label">隧道衬砌</span>
  </label>
  <label class="layer-item">
    <input type="checkbox" v-model="layerVisibility.initialGrouting" @change="updateLayerVisibility" />
    <span class="layer-color-dot initial-grouting-dot"></span>
    <span class="layer-label">初始注浆圈 (rg)</span>
  </label>
  <label class="layer-item">
    <input type="checkbox" v-model="layerVisibility.criticalGrouting" @change="updateLayerVisibility" />
    <span class="layer-color-dot critical-grouting-dot"></span>
    <span class="layer-label">临界注浆加固圈 (tg_crit)</span>
  </label>
  <label class="layer-item">
    <input type="checkbox" v-model="layerVisibility.pipes" @change="updateLayerVisibility" />
    <span class="layer-color-dot pipes-dot"></span>
    <span class="layer-label">排水管网</span>
  </label>
  <label class="layer-item">
    <input type="checkbox" v-model="layerVisibility.environment" @change="updateLayerVisibility" />
    <span class="layer-color-dot env-dot"></span>
    <span class="layer-label">水文环境</span>
  </label>
  <label class="layer-item">
    <input type="checkbox" v-model="layerVisibility.probe" @change="updateLayerVisibility" />
    <span class="layer-color-dot probe-dot"></span>
    <span class="layer-label">最不利探针</span>
  </label>
</div>
```

---

### 6.2 修复排水管网几何半径与 3D 尺寸微小标注表达

修改 [DrainagePipeGenerator.ts](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts#L118-L120)，正确换算管径为半径，并引入 3D 空间尺寸与间距标注 Sprite：

```typescript
// frontend/src/components/three/DrainagePipeGenerator.ts
private createRingInstancedMesh(diam: number, count: number, color: number): THREE.InstancedMesh {
  // 正确换算：截面半径 r = d / 2
  const pipeRadius = Math.max(0.01, diam / 2.0);
  const tubeGeometry = new THREE.TubeGeometry(this.horseshoeCurve, 64, pipeRadius, 8, false);
  const material = new THREE.MeshStandardMaterial({
    color,
    roughness: 0.3,
    metalness: 0.4,
    emissive: new THREE.Color(color).multiplyScalar(0.2) // 微弱自发光提升微小构件可见度
  });
  return new THREE.InstancedMesh(tubeGeometry, material, count);
}
```

---

### 6.3 修复双洞模式下【水文环境】与【最不利探针】的平移坐标

1. **水文环境平移修复** ([Environment.ts](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/Environment.ts))：
   在 `initWaterParticles` 与 `initFlowLines` 中，当 `tunnelType === 'double'` 时，分别围绕 $X = -D_{spacing}/2$ 与 $X = +D_{spacing}/2$ 双轴线生成粒子与流线组。

2. **最不利探针双洞对齐修复** ([PostProcessing.ts](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/PostProcessing.ts))：
   扩展 `updateFromSnapshot` 接收 `tunnelType` 与 `D_spacing`，为双洞各生成一套受力图与最不利探针。

---

### 6.4 修复 100 点至 24 单元重采样与里程/视图模式绑定

修改 [PostProcessing.ts](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/PostProcessing.ts#L438-L450)，根据入参 `viewMode ('original' | 'critical')` 绑定正确状态，并将后端 100 节点重采样归一化至 24 单元：

```typescript
// frontend/src/components/three/PostProcessing.ts
public updateFromSnapshot(
  snapshot: any,
  tunnelRadius: number = 5.5,
  zPosition: number = 0,
  tolSafetyFactor: number = 2.0,
  viewMode: 'original' | 'critical' = 'original' // 接收视图模式参数
) {
  // 1. 严格依据 viewMode 选取状态字段，解决安全系数不一致问题
  const isCriticalMode = viewMode === 'critical' && snapshot?.critical_state;
  const state = isCriticalMode ? snapshot.critical_state : (snapshot?.original_state ?? {});
  const echart = snapshot?.results?.echart_data ?? snapshot?.echart_data ?? {};
  const liningRes = isCriticalMode ? (echart.lining_res_critical ?? {}) : (echart.lining_res_original ?? {});

  // 2. 100 节点到 24 单元重采样与索引映射，解决 #0 越界问题
  const rawIdx = state.control_idx ?? state.final_control_idx ?? 0;
  const rawKList: number[] = liningRes.K_list ?? new Array(100).fill(3.5);
  
  // 将 100 节点数组按区间最小值缩减为 24 单元
  const K_24: number[] = [];
  for (let i = 0; i < 24; i++) {
    const start = Math.floor((i / 24) * rawKList.length);
    const end = Math.floor(((i + 1) / 24) * rawKList.length);
    const chunk = rawKList.slice(start, Math.max(start + 1, end));
    K_24.push(Math.min(...chunk));
  }

  // 计算映射后的 24 单元最不利索引 (0 ~ 23)
  const controlIdx24 = Math.min(23, Math.floor((rawIdx / rawKList.length) * 24));
  const minK = K_24[controlIdx24];

  // 3. 在返回结果中注入【分区里程】文本描述
  const startChain = snapshot?.start_chainage ?? 0;
  const endChain = snapshot?.end_chainage ?? 50;
  const chainageText = `DK${startChain.toFixed(0)}+000 ~ DK${endChain.toFixed(0)}+000`;

  return {
    colors: this.generateColorSpectrum(K_24, tolSafetyFactor),
    controlIdx: controlIdx24,
    controlM: (state.control_M ?? state.final_control_M ?? 0),
    controlN: (state.control_N ?? state.final_control_N ?? 0),
    minK,
    chainageText
  };
}
```

修改 [Viewer3D.vue](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue#L129-L145) Overlay 悬浮框标头，展示分区里程：

```html
<!-- frontend/src/components/three/Viewer3D.vue -->
<div v-if="probeInfo && showOverlay" class="probe-tooltip glass-card">
  <div class="tooltip-header">
    <span class="pulse-dot" :class="{ danger: probeInfo.isCritical }"></span>
    <span class="title">最不利受力单元 #{{ probeInfo.controlIdx }} ({{ probeInfo.chainageText }})</span>
  </div>
  <div class="tooltip-body">
    <div class="metric-row">
      <span class="label">最小安全系数 K:</span>
      <span class="value" :class="{ danger: probeInfo.isCritical }">{{ probeInfo.minK.toFixed(2) }}</span>
    </div>
    <div class="metric-row">
      <span class="label">控制轴力 N:</span>
      <span class="value">{{ probeInfo.controlN.toFixed(1) }} kN</span>
    </div>
    <div class="metric-row">
      <span class="label">控制弯矩 M:</span>
      <span class="value">{{ probeInfo.controlM.toFixed(1) }} kN·m</span>
    </div>
  </div>
</div>
```
