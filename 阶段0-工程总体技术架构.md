<!-- 阶段0-工程总体技术架构.md -->

# 【阶段0-工程方案】：工程总体技术架构设计书

### 一、 总体系统架构

隧道工程多维协同智能排水自适应平台采用前后端分离架构，确保计算逻辑与表现层解耦，支持跨端部署。

* **前端渲染层 (Web & 3D)**：采用 Vue 3 + TypeScript 框架。2D UI 组件库选用 Element Plus，保障表单重负载场景下的性能；3D 渲染引擎选用 Three.js，支持轻量化且高定制化的 WebGL 图形展示。
* **桌面端容器 (客户端套壳)**：选用 Tauri 框架。Tauri 基于 Rust 开发，打包体积更小、内存占用更低，符合轻量化客户端需求，完美封装 HTML5 前端页面。
* **后端 API 服务层**：采用 Python FastAPI 框架。底层计算模型由 Python 原生函数解算，FastAPI 实现原生无缝集成，具备极高的并发性能，并自动生成 OpenAPI (Swagger) 接口文档，满足向第三方输出 POST API 请求的要求。

---

### 二、 核心模块技术实现

#### 1. API 接口与数据流转 
* **请求规范**：计算请求统一通过 `POST /api/v1/calculate/drainage` 触发。
* **报文结构**：采用 JSON 格式。请求体包含 `tunnel_type`（单洞 `"single"` / 双洞 `"double"`）及完整的几何、水力与结构参数字典。
* **计算引擎对接 (`drainage_engine.py`)**：后端接收请求后调用 `run_calculation(data)` 主函数，实例化水力模型 `TunnelParamsHc` 与结构模型 `TunnelParamsMc`：
  - **4 工况判断**：根据 `tunnel_type` 及内半径与水头比值 $r_0/H \ge 0.062$ 判断低/高水位，划分为 `single_low`, `single_high`, `double_low`, `double_high` 4 类工况。
  - **结构内力解算**：基于拱顶坍塌荷载高度 $H_q$ 和地层外水压力解算全环 24 单元轴力 $N_{elem}$ 与弯矩 $M_{elem}$，求出最小安全系数 $nowK$。
  - **双分支数据契约**：
    1. **安全分支 ($nowK > tol\_safety\_factor$)**：仅返回 `input_parameter`, `original_state` 及 `echart_data`（含 `Hq` 与 `lining_res_original`）。
    2. **超限加固分支 ($nowK \le tol\_safety\_factor$)**：以步进 $0.05\text{m}$ 搜索容许水头 `final_waterHead`，反算临界注浆半径 $r_{g\_crit}$ 与厚度 $t_{g\_crit} = \max(0.0, r_{g\_crit} - r_2)$，输出 `critical_state` 与 `echart_data.lining_res_critical`。
* **第三方调用**：通过标准 RESTful API 及 Bearer Token 鉴权，支持外部系统（如综合管廊平台、BIM 平台）直接 POST 传参并获取推荐管间距、管径及加固厚度结果。

#### 2. 参数输入与 UI 交互设计 
针对前端 38/40 个高密度参数，核心设计逻辑为“分区分级、逐步披露”，降低用户的认知负荷。

* **分模块折叠面板 (Accordion)**：将参数按工程逻辑物理隔离，分为“基础参数区”、“复选参数区”和“高级默认参数区”。
* **参数分级与默认值封装**：
    * **核心输入区**：直接展示核心几何与地质参数（如 $r, r_1, r_2, r_g, c, ha, D_{spacing}$）。
    * **高级设置区**：将其余默认参数折叠隐藏，默认填入规范推荐值或经验值。用户如需修改，展开“高级设置”面板即可干预。
* **隧道输入参数数据库 (Parameter Database)**：建立独立的参数库管理界面（`ParameterDatabase.vue`）。支持按隧道类型（市政、公路、铁路）进行分类筛选与检索；系统预置标准隧道参数库及“示点工程项目”基准参数；提供当前表单参数的“另存为并命名”功能，实现工程计算参数的长期沉淀与跨项目复用。
* **主控台弹性布局 (Flexible Dashboard Layout)**：Dashboard 主界面增设视图控制按钮组，支持动态切换与折叠左侧（输入参数）、中部（3D 画布）、右侧（结果卡片）视图。左侧与右侧面板引入 SplitPane 机制，支持通过拖拽边界线自由分配屏幕空间。
* **一键主题切换 (Theme Toggle)**：建立全局状态的主题管理器（`themeStore.ts`），预设“明亮白”与“科技蓝”主题，支持一键切换并同步更新 2D 组件色彩变量与 3D 场景背景色/灯光色温。
* **极限工况快照 (Condition Snapshots)**：支持将当前参数组合及计算解算字典（含原始/临界状态）一键保存为“快照”。快照存储于 Pinia 及本地数据库，用户可快速回溯与比对不同工况。
* **批量数据 IO**：依托 `xlsx` 库支持下载标准 Excel 模板，用户在本地填好参数后一键导入；支持跨分区（起点/终点里程）Excel 模板批量导入，系统解析后自动生成快照序列（Snapshot Sequence）。

#### 3. 3D 模型构建与可视化交互 
3D 模块的核心是**数据驱动几何生成**与**状态机单向绑定**，无需导入外部庞大的 BIM 模型。

* **低参数隧道构建 (逐环生成与 ExtrudeGeometry)**：
    * 在 Three.js (`TunnelGenerator.ts`) 中解析 $r, r_1, aspect\_ratio$，构建马蹄形三心圆拱截面，计算总宽 $w = r_1 + r$ 与总高 $h = w \times aspect\_ratio$。
    * 使用 `THREE.InstancedMesh` (实例化网格) 技术按里程步进进行线性阵列复制，衬砌厚度 $t_{lining}=r_1-r$ 与 $t_{primary}=r_2-r_1$ 由 GPU 顶点着色器偏置挤出。当 $r > 5.0\text{m}$ 时自动开挖仰拱中心排水沟。
* **3D 交互控制 (旋转/剖切/图层管理)**：
    * **旋转与缩放**：引入 `OrbitControls` 实现鼠标拖拽旋转与滚轮缩放。
    * **剖切功能**：利用 Three.js 的 `ClippingPlanes` 特性，设置 X/Y/Z 剖切平面，通过 UI 滑块动态修改平面的常数项 `plane.constant`。
    * **图层控制**：利用 Three.js 的 `Object3D.layers` 机制划分为 Layer 0~5（环境、二衬、初支、注浆圈、锚杆小导管、排水管网），通过配置面板进行无开销显隐切换。
* **计算结果映射与拾取 (参数反显)**：
    * 优先从 `results.critical_state`（降级读取 `original_state`）获取推荐管间距 `ring_spacing_recommend` 与管径 `ring_diam_recommend`，驱动 `DrainagePipeGenerator.ts` 矩阵刷新。
    * 引入 `Raycaster` 射线拾取。点击特定排水管或衬砌顶点时，高亮该管段并弹出 HTML 浮窗 (Tooltip)，精准显示间距、管径及最不利单元应力标量。
* **多方案 3D 对比机制 (CompareView.vue)**：
    * 支持“分屏对比 (Split Screen)”模式，在同一个 WebGL 容器中挂载双路 `Viewer3D.vue` 实例，主画布展示 `original_state`（超限红区），副画布展示 `critical_state`（临界加固安全区，注浆厚度调整为 $t_{g\_crit}$）。
    * 双视角绑定同一相机控制器，实现旋转与缩放的无延迟同频联动。
* **智能求解模块与双分支渲染 (`drainage_engine.py`)**：
    * 当后端判定 $nowK \le tol\_safety\_factor$ 时，自动启动水头步进搜索求得 `final_waterHead` 与 `tg_crit`。
    * 3D 水位面 (`Environment.ts`) 动态调整至对应水头高度，同时动水流线粒子速度由总渗漏量 $Q$ 驱动。
* **安全系数云图与应力探针 (`PostProcessing.ts`)**：
    * 将 24 单元全环受力与安全系数数组 `lining_res_original.K_list` / `lining_res_critical.K_list` 注入着色器 `lining.vert`，驱动衬砌表面顶点的逐点色阶映射。
    * 解析 `control_idx` / `final_control_idx`，在三维空间中精确定位最不利点并挂载 3D 探针标注。

#### 4. 3D 模型美化与工程表现
* **注浆圈 (Grouting Ring)**：`Reinforcement.ts` 根据 $r_g$ 生成常规注浆圈（$t_g = r_g - r_2$），根据 `tg_crit` 生成临界注浆圈（$t_{g\_crit} = \max(0.0, r_{g\_crit} - r_2)$），配以半透明材质与非均匀扩散着色器。
* **锚杆与小导管 (Anchors & Conduits)**：利用 `InstancedMesh` 按照环向/纵向排距生成阵列，超前小导管依据外插角 `conduit_angle` 施加发散旋转矩阵。
* **后期处理管道 (Post-Processing)**：在 `EffectComposer` 中集成 SSAO 增强接缝接触暗部阴影，启用 Outline Pass 在拾取选中管段时提供发光描边反馈。

---

### 三、 工程文件目录架构

基于前述确定的 Vue 3 + Three.js (前端)、FastAPI (后端) 及 Tauri (桌面端) 技术栈，系统 Monorepo 单体仓库目录结构如下：

```text
tunnel-drainage-platform/
├── backend/                              # Python FastAPI 后端服务
│   ├── app/
│   │   ├── api/
│   │   │   └── v1/
│   │   │       └── endpoints/
│   │   │           ├── calculate.py      # POST API 路由 (/api/v1/calculate/drainage)
│   │   │           └── database.py       # 参数数据库 CRUD 接口路由 (/api/v1/database/parameters)
│   │   ├── db/
│   │   │   ├── session.py                # SQLite 异步引擎与会话工厂配置
│   │   │   └── init_db.py                # 建表逻辑与基础示点工程数据注入 (Seed)
│   │   ├── core/
│   │   │   └── config.py                 # 全局配置 (CORS, 环境变量)
│   │   ├── models/
│   │   │   ├── schemas.py                # Pydantic 数据验证模型 (严格定义38/40个参数及默认值)
│   │   │   └── domain.py                 # SQLAlchemy ORM 物理表结构模型 (TunnelParameter)
│   │   └── services/
│   │       ├── hydrocalc.py              # 水力学计算模块 (SCS-CN降雨, SCS透水计算)
│   │       ├── mechcalc.py               # 结构力学计算模块 (全环衬砌内力与安全系数)
│   │       └── drainage_engine.py        # 核心：封装智能排水与防排水联动解算引擎主函数 (run_calculation)
│   ├── requirements.txt                  # 后端依赖声明
│   └── main.py                           # FastAPI 启动入口
│
├── frontend/                             # Vue 3 + TS + Three.js 前端应用
│   ├── src/
│   │   ├── api/
│   │   │   └── index.ts                  # Axios 请求封装，对接后端计算 API
│   │   ├── assets/
│   │   │   └── shaders/                  # 自定义着色器代码 (lining.vert, lining.frag)
│   │   ├── components/
│   │   │   ├── ui/                       # 2D 交互组件库
│   │   │   │   ├── ParameterForm.vue     # 高密度参数表单 (分区分级折叠面板与脏数据拦截)
│   │   │   │   ├── SnapshotSidebar.vue   # 侧边栏快照列表组件
│   │   │   │   └── CaseSelector.vue      # 典型案例一键赋值组件
│   │   │   └── three/                    # 3D 渲染核心模块
│   │   │       ├── Viewer3D.vue          # 3D 画布容器与 Layers 图层控制
│   │   │       ├── TunnelGenerator.ts    # 隧道主体参数化生成 (InstancedMesh、马蹄形截面、中心水沟)
│   │   │       ├── DrainagePipeGenerator.ts # 排水盲管参数化生成与阵列矩阵更新
│   │   │       ├── Reinforcement.ts      # 注浆圈(常规/临界)、锚杆与小导管几何体生成
│   │   │       ├── Environment.ts        # 3D 水位面、渗漏量 Q 动水流线粒子场
│   │   │       └── PostProcessing.ts     # 后期处理管道 (SSAO, OutlinePass, 24单元安全系数云图与探针)
│   │   ├── store/
│   │   │   ├── parameterStore.ts         # Pinia 状态管理 (表单状态与 Dirty 标记)
│   │   │   ├── themeStore.ts             # 管理明亮白与科技蓝主题状态及切换逻辑
│   │   │   └── snapshotStore.ts          # 快照 CRUD、序列数据解包与双分支状态树挂载
│   │   ├── utils/
│   │   │   ├── excelIO.ts                # 跨分区 Excel 模板批量导入与快照生成
│   │   │   └── math.ts                   # 4x4 仿射变换矩阵、法向量及截面曲线推导
│   │   ├── views/
│   │   │   ├── Dashboard.vue             # 主控工作台界面
│   │   │   ├── ParameterDatabase.vue     # 独立的隧道输入参数数据库界面
│   │   │   └── CompareView.vue           # 3D 双视角分屏对比视图
│   │   ├── App.vue                       # 应用根组件
│   │   └── main.ts                       # Vue 启动入口
│   ├── package.json                      # 前端依赖 (vue, three, element-plus 等)
│   └── vite.config.ts                    # Vite 构建配置 (@ 别名)
│
└── desktop/                              # Tauri 桌面端套壳容器
    ├── src-tauri/
    │   ├── src/
    │   │   └── main.rs                   # Rust 入口
    │   ├── tauri.conf.json               # Tauri 配置文件
    │   └── Cargo.toml                    # Rust 依赖声明
    └── build.rs                          # Tauri 构建脚本
```

---

### 关键目录设计说明

1. **`backend/app/services/drainage_engine.py`**: 核心计算引擎服务，调用 `hydrocalc` 与 `mechcalc` 完成全环水力-结构耦合计算。根据全环最小安全系数 $nowK$ 与 $tol\_safety\_factor$ 的比对，自动选择输出单分支（安全）或双分支（超限加固）数据字典。
2. **`backend/app/models/schemas.py`**: Pydantic 输入输出校验模型，校验 38/40 个物理参数与默认值。
3. **`frontend/src/store/snapshotStore.ts`**: 快照状态树管理，提取并响应 `original_state` 与 `critical_state`，实现降级读取与 3D 可视化映射。
4. **`frontend/src/components/three/`**: 解耦的 3D 渲染构件层。`TunnelGenerator`、`DrainagePipeGenerator`、`Reinforcement` 与 `Environment` 严格依据 snapshot 数据更新 GPU 矩阵与材质，保证高帧率工业级渲染。