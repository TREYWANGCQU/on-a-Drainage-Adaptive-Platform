# 阶段4-路面与排水沟几何修复方案

## 1. Objectives (目标与范式定义)

本专项方案针对 3D 隧道模型可视化场景中，内部衍生几何体（路面、排水沟）存在的空间拓扑与渲染问题进行系统级修复。主要实现以下目标：
1. **彻底消除路面与排水沟的 Z-Fighting 空间重叠**，重构三沟（中心沟、双侧沟）为具有实际通水深度的 U 型凹槽空心结构，使其真正清晰可见。
2. **修复排水边沟与二衬（仰拱）的碰撞干涉错误**，引入二衬曲率极值约束，确保边沟不会穿透或暴露于二衬之外。
3. **修复中心排水沟悬空问题**，将中心排水沟的底面标高向下延伸至仰拱底部的真实物理位置。
4. **统一双范式透视美学**，赋予路面及水沟组件合理的半透明质感，匹配《阶段4-3D材质灯光美化与微观局部放大方案.md》的要求，使埋藏于路面下的排水盲管、中心管网及动水流线展现出强烈的赛博与影棚科技感。

---

## 2. Constraints (约束与设计边界)

1. **曲率极值推导约束**：所有边沟和水沟的 X 坐标横向偏移，绝不能使用相对于路面宽度的定值，必须使用基于水沟底部标高 `yBot` 与二衬仰拱方程 $y = \text{invertCenterY} - \sqrt{R_3^2 - x^2}$ 联立求得的 $x_{\text{lining}}$ 作为极值边界，并向隧道中心方向缩进。
2. **拓扑防自交约束**：水沟 Geometry 不能继续使用实心的 `BoxShape`（该写法会完全填满路面挖槽导致看不出水沟）。必须采用内嵌 `Hole` 路径的 U 型槽生成算法。
3. **渲染排序与透明度约束**：赋予路面/排水沟 `transparent: true`，但需通过合理的参数防止其与全息衬砌以及加固圈的半透明层级发生 Alpha Sorting 问题（例如开启 `depthWrite: true` 或严格调节 `opacity`）。

---

## 3. Architecture (系统架构与模块分解)

- **目标组件**：`frontend/src/components/three/TunnelGenerator.ts`
- **核心模块一**：内部衍生拓扑生成器 `createInternalRoadAndDitchGeometry()`
  - 负责利用 `THREE.Shape` 与 `THREE.Path` 切割路面实体，以及构造精确贴合二衬曲线、不重叠、无干涉的空心水沟。
- **核心模块二**：赛博双范式动态控制台 `setVisualParadigm()`
  - 负责对实例化网格 `roadMesh` 和 `ditchMesh` 进行全局材质刷新，实时匹配“高亮影棚（Studio）”与“暗夜赛博（Cyber）”的颜色及透明度基准。

---

## 4. Work Breakdown Structure (工作分解结构)

### WBS 1: 边沟曲率干涉修正 (Side Gutter Collision Fix)
- **分析**：原边沟 X 坐标 `[-halfRoadW, -halfRoadW + 0.4]` 强行绑定路面顶宽。由于二衬仰拱是圆弧，在深达 0.3m 处，二衬实际净宽已小于 `halfRoadW`，导致边沟网格刺穿二衬。
- **解法**：
  1. 定义侧沟底面标高 `yBot = roadY - 0.3`。
  2. 根据仰拱方程计算该深度处的二衬最大安全半径：`const max_x_lining = Math.sqrt(Math.max(0, R3_base * R3_base - Math.pow(invertCenterY - yBot, 2)))`（加入 `Math.max(0, ...)` 防下溢与 `NaN`）。
  3. 将左侧侧沟的区域收缩至安全范围内，例如：左侧沟槽区域重定义为 `[-max_x_lining + 0.05, -max_x_lining + 0.45]`。确保边沟 100% 容纳于二衬内部。

### WBS 2: 中心排水沟真实触底修正 (Central Ditch Depth Fix)
- **分析**：原代码 `ditchShapes.push(createBoxShape(-0.35, 0.35, roadY, roadY - 0.4))` 导致中心沟悬浮于路面中间（仅 0.4m 深），未触及仰拱底面。
- **解法**：
  1. 将中心沟的深度向下延展至二衬底部，即 `yBot_central = invertCenterY - R3_base + 0.05` (预留 5cm 防闪烁缝隙)。
  2. 路面的中央挖槽亦需同步加深，或路面自身形成分离跨接，包容极深的中央水沟。

### WBS 3: U 型空心水沟生成与重叠消除 (U-Shape Ditch & Z-Fighting Fix)
- **分析**：实心水沟网格和路面网格占据相同体积，使得路面虽然挖了槽，却被水沟填满。
- **解法**：
  1. 废弃 `createBoxShape`。
  2. 新增 `createUShape(xMin, xMax, yTop, yBot, wallThickness)` 方法。
  3. 利用 `THREE.Shape()` 勾勒水沟外壁，使用 `THREE.Path()` 从内部扣除流水空间（形成 `holes`）。内部 `Path` 的顶点绘制顺序需与外壁 `Shape` 相反（遵循非零环绕规则 Non-Zero Winding Rule），确保 Three.js Earcut 三角剖分正确定向，使得渲染出的水沟为真实的“槽”，内含空气。从而一举解决无法看清沟槽的视觉缺陷。

### WBS 4: 路面/水沟透视美学升级 (Translucency & Cybernetic Mat Upgrade)
- **分析**：为呼应《阶段4》要求，需要对沥青与混凝土赋予以赛博/影棚极客质感为主的半透明材质，展示内部复杂管网。
- **解法**：
  1. `createInternalRoadAndDitchGeometry()` 中：
     ```typescript
     const roadMat = new THREE.MeshStandardMaterial({
       color: 0x242830, roughness: 0.85, metalness: 0.1,
       side: THREE.DoubleSide, transparent: true, opacity: 0.85 // 默认初始微透
     });
     // 水沟同理开启透明
     ```
  2. `setVisualParadigm()` 动态适配：
     - **Cyber (赛博暗夜)**：`color: 0x0f172a`, `opacity: 0.6`（幽黑高透，隐约透出下部蓝色水流）。
     - **Studio (高亮影棚)**：`color: 0x2c3e50`, `opacity: 0.4`（玻璃磨砂高透质感）。

### WBS 5: ExtrudeGeometry 前后封顶面剥离 (End-Cap Removal & Channel Continuity)
- **分析**：`THREE.ExtrudeGeometry` 默认在 $Z=0$ 与 $Z=-depth$ 生成 Front Cap 与 Back Cap。在使用 `InstancedMesh` 沿 Z 轴按 1 米间隔重复排布时，每隔 1 米在隧道横截面上即叠加 2 张垂直封闭墙面。在半透明材质下，这些封顶面全部暴露，形成密密麻麻的横向线条（“梯子纹”），且将 U 型水沟隔断为几十个独立封闭方盒，严重破坏动水贯通视觉。
- **解法**：
  1. 新建 `removeExtrudeEndCaps(geometry: THREE.BufferGeometry)` 工具函数。
  2. 解析 ExtrudeGeometry 的法向 (Normals) 与 Position 顶点属性，判定法线平行于 Z 轴（`Math.abs(nz) > 0.99`）的 Front/Back Cap 三角形面片。
  3. 过滤 Index Buffer，剔除所有端面索引，仅保留沿纵向延伸的侧壁（Side Walls）索引。
  4. 使得排布后的路面与 U 型水沟在 Z 轴物理连成无缝长廊，100% 消除横向梯子条纹与水沟挡板。

### WBS 6: 透明深度写入与 Alpha 排序管线优化 (Depth Write & Alpha Sorting Optimization)
- **分析**：`InstancedMesh` 无法像独立 Mesh 那样逐个按视角相机距离 (View-Z) 动态重排序，在纯透明模式下易产生远近片元交错颠倒。
- **解法**：
  1. 为 `roadMat` 与 `ditchMat` 配置 `depthWrite: true`，结合 `alphaTest: 0.05` 与 `polygonOffset: true`（`polygonOffsetFactor: 1, polygonOffsetUnits: 1`），确保 WebGL 深度缓冲区正确阻挡远景颠倒片元。
  2. 针对高亮影棚 (Studio) 与赛博暗夜 (Cyber) 范式，优化材质 `opacity` 梯度及 Side 双面渲染参数，消除自遮挡黑斑。

---

## 5. Acceptance Criteria (验收标准与指标体系)

| 序号 | 修复维度 | 验收标准 | 对应 WBS |
| :--- | :--- | :--- | :--- |
| **AC-1** | 视觉重叠 (Z-Fighting) | 隧道路面挖槽部位呈现明显的凹陷与“空心槽”结构，不再填满实心网格。 | WBS 3 |
| **AC-2** | 边沟穿透 (Collision) | 在仰拱视角下，边沟完全内收于二衬外壁边界之内，无任何网格凸出。 | WBS 1 |
| **AC-3** | 中心沟悬空 (Suspend) | 中心排水沟垂直深度大幅增加，其底面直接接触隧道最底部的二衬结构上方。 | WBS 2 |
| **AC-4** | 材质范式 (Material) | 切换暗夜/高亮模式时，路面与水沟呈现（0.4~0.6）的半透明特性，能透视看见底部的管网及动水流线。 | WBS 4 |
| **AC-5** | 横向梯子纹消除 | 沿隧道纵向透视时，路面与水沟上**无任何每隔 1 米的垂直端面遮挡与横向隔条**。 | WBS 5 |
| **AC-6** | 水沟纵向贯通 | 视角深入水沟槽内，展现为**完全畅通的连续 U 型水廊**，动水粒子可无阻碍贯穿。 | WBS 5 |
| **AC-7** | 深度排序稳定 | 半透明渲染下片元深度关系正确，**无远处物体穿透覆盖近处物体的 Alpha Sorting 叠影**。 | WBS 6 |
