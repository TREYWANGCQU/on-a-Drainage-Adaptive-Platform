<!-- 阶段0-工程开发计划.md -->

# 【阶段0-工程方案】：工程开发计划路线图

隧道工程智能排水计算平台落地开发路径。遵循可控、可验证的工程实施原则，按开发先后逻辑拆解为以下五个核心执行阶段：

---

### 阶段一：工程基座初始化与环境隔离 (第 1 周)
此阶段目标为确立标准化的系统架构，确保前后端及跨端容器物理隔离，互不干扰。

1. **构建 Monorepo 目录：**
   新建根目录 `tunnel-drainage-platform` 并初始化 Git 仓库。
2. **初始化后端 (Python FastAPI)：**
   * 创建 `backend` 文件夹，配置虚拟环境：`python -m venv venv` 并激活。
   * 安装核心依赖：`pip install fastapi uvicorn pydantic pandas numpy pydantic-settings`。
   * 安装数据库依赖：`pip install sqlalchemy aiosqlite`。
3. **初始化前端与桌面端 (Vue 3 + Tauri)：**
   * 执行：`npm create tauri-app@latest`，按提示选择 `frontend` -> `Vue` -> `TypeScript`。
   * 进入 `frontend` 目录，安装 3D 与 UI 依赖：
     - 运行依赖：`npm install three element-plus @element-plus/icons-vue pinia vue-router@4 axios xlsx @tauri-apps/api`
     - 开发依赖：`npm install -D @types/three`

---

### 阶段二：后端计算逻辑服务化 (第 1-2 周)
此阶段目标为将 Python 计算引擎转化为标准化 API 服务，支持单/双分支数据契约。

1. **数据接口契约化 (Schemas 定义)：**
   在 `backend/app/models/schemas.py` 中使用 Pydantic 严格定义入参模型。显式区分单洞（38个参数）与双洞（40个参数），写死 13 个默认参数作为保底值，定义 $r, r_1, r_2, r_g, c, ha, D_{spacing}, aspect\_ratio$ 等字段。
2. **核心算法重构与集成 (`drainage_engine.py`)：**
   重构主解算入口 `drainage_engine.py`，调用 `hydrocalc` 与 `mechcalc` 模块：
   * **4 工况判断**：自动划分 `single_low`, `single_high`, `double_low`, `double_high` 水文工况。
   * **全环受力与安全系数**：计算拱顶坍塌荷载 $H_q$，求解 24 单元内力数组 $N_{elem}, M_{elem}$ 及安全系数数组 $K_{list}$，得到最小安全系数 $nowK$。
   * **双分支结构解算**：当 $nowK > tol\_safety\_factor$ 时输出 safe 分支；当 $nowK \le tol\_safety\_factor$ 时触发步进迭代搜索 `final_waterHead`，反算临界注浆半径 $r_{g\_crit}$ 与厚度 $t_{g\_crit} = \max(0.0, r_{g\_crit} - r_2)$，输出 `critical_state`。
3. **路由绑定与 API 暴露：**
   在 `backend/app/api/v1/endpoints/calculate.py` 中编写 `POST /api/v1/calculate/drainage` 接口，组装包含 `input_parameter`, `original_state`, `critical_state` (超限时) 及 `echart_data` 的透传字典。
4. **单元测试与 API 验证：**
   本地启动后端 `uvicorn main:app --reload`，访问 Swagger UI (`/docs`)，验证 38/40 个参数传入后能否正确返还结构受力与加固建议结果。

---

### 阶段三：前端台账式表单与数据流转 (第 2-3 周)
此阶段重点解决重度输入参数的交互体验与快照持久化流转。

1. **状态机搭建 (Store)：**
   配置 Pinia，建立 `parameterStore` 集中维护表单参数与脏数据标识 (`isDirty`)；建立 `snapshotStore` 管理快照序列与双分支计算结果树。
2. **分区分级 UI 构建：**
   * 使用 Element Plus `el-collapse` 将表单分为：基础输入区、复选区与高级默认区。
   * 预设典型案例数据 (`CaseSelector.vue`)，实现下拉框一键覆盖 `parameterStore` 中的数值。
3. **Excel 批量 IO 与快照流水线：**
   * 引入 `xlsx` 库开发跨分区里程 Excel 模板批量解析功能，自动根据分区参数推演生成快照序列（Snapshot Sequence）。
   * 提供示例 Excel 模板下载功能，引导用户规范录入。
4. **快照持久化与一键调度：**
   * 开发快照捕获函数，将 Pinia 参数及解算字典镜像导出为 JSON 对象并保存。
   * 开发“一键批量计算”功能，针对处于待计算 (`pending`) 状态的快照执行异步并发调度。
5. **弹性视图与主题框架：**
   引入 SplitPane 实现 Dashboard 左右面板的拖拽拉伸；开发顶部视图控制栏调度面板显隐；配置全局 Theme 变量支持“明亮白”与“科技蓝”双主题切换。
6. **参数数据库与持久化开发：**
   开发 `ParameterDatabase.vue` 界面，建立市政、公路、铁路标签检索体系，录入示点工程数据；实现表单参数“另存为”写入 SQLite 数据库。
7. **前后端联调与序列卡片优化：**
   * 封装 Axios 实例，联调 `POST /api/v1/calculate/drainage`。
   * 优化序列库卡片，增加状态指示器（🟢 已计算、🟡 待计算、🔴 计算失败），展示核心指标（如 `waterHead` / `final_waterHead`、`safety_factor`）。

---

### 阶段四：3D 可视化映射与交互开发 (第 3-4 周)
此阶段为核心难点，重点保障大体量工程构件的渲染性能与数据物理映射。

1. **交互状态机与脏数据拦截：**
   配置 `parameterStore.isDirty` 机制。修改参数时同步冻结 3D 画布色谱并覆盖预警横幅，切断陈旧结果与 3D 几何的物理映射。
2. **三维几何生成器 (`TunnelGenerator.ts`)：**
   * 解析 $r, r_1, aspect\_ratio$ 构建马蹄形截面 Shape（总宽 $w=r_1+r$，总高 $h=w \times aspect\_ratio$）。
   * 衬砌厚度 $t_{lining}=r_1-r$ 与 $t_{primary}=r_2-r_1$ 交由 Shader (`lining.vert`) 偏置挤出；当 $r > 5.0\text{m}$ 时自动相减开挖中心水沟。
3. **注浆加固与支护建模 (`Reinforcement.ts`)：**
   构建常规注浆圈（$t_g = r_g - r_2$），当存在 `critical_state` 时提取 `tg_crit` 动态构建临界注浆圈；使用 `InstancedMesh` 生成锚杆与超前小导管。
4. **排水管网阵列推演 (`DrainagePipeGenerator.ts`)：**
   优先从 `critical_state`（降级读取 `original_state`）读取 `ring_spacing_recommend` 与 `ring_diam_recommend`，推演 4x4 仿射变换矩阵 $M = T \cdot R \cdot S$ 更新 InstancedMesh。
5. **水水文环境随动 (`Environment.ts`)：**
   根据 `waterHead` / `final_waterHead` 驱动 3D 地下水位平面 Y 轴标高，依据总渗漏量 $Q$ 驱动动水流线粒子速度。
6. **多快照空间组装与分屏对比 (`CompareView.vue`)：**
   * 基于 `start_chainage` 与 `end_chainage` 在 3D 画布中按真实里程拼接多分区网格。
   * `CompareView.vue` 挂载双路画布，主屏展示 `original_state`（超限红区），副屏展示 `critical_state`（临界加固安全区），同步双视角 Camera 矩阵。
7. **图层管理与后处理 (`Viewer3D.vue` & `PostProcessing.ts`)：**
   * 应用 Three.js Layers 通道（Layer 0~5）实现图层秒级显显隐控制。
   * 接入 SSAO Pass 与 Outline Pass；在 Shader 中注入 24 单元 $K_{list}$ 实现全环云图；根据 `control_idx` 挂载 3D 探针。

---

### 阶段五：系统编译与打包交付 (第 4 周)
此阶段目标为完成独立桌面安装包的输出与全平台性能验收。

1. **桌面端容器融合配置：**
   配置 Tauri 代理后端服务请求，或配置 Tauri 在启动时异步拉起 Python 后端进程。
2. **智能求解算法闭环验证：**
   验证后端 `drainage_engine.py` 在超限状态下自动搜索 `final_waterHead` 与 `tg_crit` 的收敛性与前端 3D 临界状态随动表现。
3. **高频交互稳定性测试：**
   在长里程、高密度场景下测试高频拖拽参数时的渲染帧率（保持 60FPS），确保内存无泄漏、WebGL 上下文不丢失。
4. **多平台构建打包：**
   执行 `npm run tauri build`，在 Windows 环境下编译生成独立可执行程序 (`.exe` / `.msi`)。
5. **工程验收交付：**
   在桌面端安装包中测试满载示范工程数据（双洞、长里程分区），全面验收 2D 表单交互、3D 云图映射与分屏比对功能。
