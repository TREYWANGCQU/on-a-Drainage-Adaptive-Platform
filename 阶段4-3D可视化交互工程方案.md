# 【阶段4-工程方案】：3D 可视化映射与交互开发深化设计 (第 3-4 周)

## 一、 总体工程架构与设计基准

此阶段核心任务在于确立前端状态机、UI 交互与底层 WebGL 渲染管线的物理映射关系。系统全面摒弃传统 DOM 驱动下的“销毁-重建”渲染模式，转向“数据驱动显存缓冲（Data-Driven Buffer）”的原生架构。系统依据单一数据源（Single Source of Truth）原则，将后端解算快照精确分发至对应的渲染着色器与几何体缓冲中。

### 1.1 核心文件物理拓扑定位
为保障代码库的长期可维护性与职责单一原则，三维模块的文件层级结构作如下硬性规定，各项建模参数与逻辑严格归口：

*   **三维画布根节点**：`frontend/src/components/three/Viewer3D.vue`。负责 Vue 响应式生命周期与 Three.js 渲染管线的桥接。
*   **隧道主体与管网建模**：`frontend/src/components/three/TunnelGenerator.ts`。归集衬砌几何体生成、多截面类型（马蹄形/圆形）算法，及排水管网阵列的矩阵推演。
*   **支护体系建模**：`frontend/src/components/three/Reinforcement.ts`。负责注浆圈、系统锚杆及超前小导管等控制参数的几何体生成与空间位姿解算。
*   **水文环境建模**：`frontend/src/components/three/Environment.ts`。专职处理 3D 水位面表达、动水流线生成及地下水头高度的随动逻辑。
*   **数据交换与状态拦截**：frontend/src/store/parameterStore.ts 联合 snapshotStore.ts。负责前端表单状态机管理、脏数据拦截（Dirty Flag）机制的精准触发，以及执行计算结果向下游渲染器的单向数据流分发。
*   **视觉后处理**：`frontend/src/components/three/PostProcessing.ts`。接管安全系数云图着色、边缘泛光及屏幕空间环境光遮蔽（SSAO）。
*   **底层数学库**：`frontend/src/utils/math.ts`。处理所有非线性空间变换、法向量推导及复杂截面曲线方程。

---

## 二、 Three.js 参数动态建模与控制详细规程

本规程详述如何将前端接收到的工程参数（如间距、孔径、厚度等）无损、低延迟地转化为三维空间中的几何实体，所有操作均需在显存预分配的框架下执行。

### 步骤一：状态隔离与渲染上下文接管

**文件位置：** 主要在 `frontend/src/components/three/Viewer3D.vue` 与 `frontend/src/store/snapshotStore.ts` 中执行。

1.  **画布初始化与硬件加速声明**：在 Vue 的 `onMounted` 生命周期钩子中，实例化 `WebGLRenderer`。配置开启抗锯齿（`antialias: true`），并根据客户端设备像素比调用 `setPixelRatio(window.devicePixelRatio)` 以防止高分屏模糊。配置渲染器支持对数深度缓冲（`logarithmicDepthBuffer: true`），以解决隧道细长结构带来的 Z-Fighting（深度冲突）问题。
2.  **数据单向监听与解包**：在 `Viewer3D.vue` 中引入 `snapshotStore`。采用 `watch` 深度监听当前激活的计算快照对象（`activeSnapshot`）。为阻断 Vue Proxy 响应式系统对 Three.js 底层大型对象的性能反噬，提取数据时需调用 `toRaw()` 方法获取纯粹的 JavaScript 对象。
3.  **防抖节流下发管道**：构建微秒级节流阀。当用户连续拖拽滑块调整参数时，利用 `requestAnimationFrame` 将多次状态变更合并为单次渲染调用。提取后的裸数据被打包为标准接口对象（如 `ITunnelParams`），下发至底层 `TunnelGenerator.ts` 与 `Reinforcement.ts` 中。

### 步骤二：预分配显存与基准几何体建立

**文件位置：** 主导逻辑位于 `frontend/src/components/three/TunnelGenerator.ts` 及 `Reinforcement.ts`。

基于前端不宜进行高频内存垃圾回收的原则，所有工程构件采用实例化网格技术。

1.  **极值计算与显存申请**：在工程加载之初，系统读取当前区段的最大设计纵深里程（$L_{max}$）以及支持的最小环向/纵向排布间距（$\Delta l_{min}$）。据此推算各构件的最大可能数量。例如，排水管的最大实例数 $N_{max}$ 计算公式为：
    $$ N_{max} = \left\lceil \frac{L_{max}}{\Delta l_{min}} \right\rceil \times C_{ring} $$
    其中 $C_{ring}$ 为单环最大布管数量。
2.  **基准几何体（计算单元）构建**：
    *   **有中心水沟隧道（典型案例-晏家隧道）**：针对该类非对称马蹄形断面，在 `TunnelGenerator.ts` 中引入分段函数。调用 `THREE.Shape` 定义截面。核心控制参数设定为：上部圆心距拱顶半径 $R_1 = 7.9$ 米，侧墙过渡曲线计算，隧道脚（仰拱接合处）半径 $R_2 = 5.2$ 米，最大开挖宽度限制为 $15.7$ 米。底部布设矩形布尔扣减区域，预留中心深埋水沟结构。通过 `THREE.ExtrudeGeometry` 沿 Z 轴拉伸 $1$ 个标准单位长度形成基准几何体。
    *   **无中心水沟隧道（典型大断面盾构隧道）**：截面逻辑切换为纯圆形。通过 `THREE.CylinderGeometry` 或 `THREE.TubeGeometry` 构建，核心外径参数硬编码或参数化为 $12$ 米。内部底板与侧边沟的几何特征通过叠加次级实例化网格实现。
3.  **实例化对象生成**：调用 `new THREE.InstancedMesh(geometry, material, N_{max})` 完成实例化。该操作一次性在 GPU 显存中开辟连续的矩阵存储空间。日常渲染时，依据当前参数计算的实际需求量 $N_{current}$，动态赋值 `mesh.count = N_{current}`，使得冗余实例在渲染管线中被直接抛弃，不占用计算资源。

### 步骤三：仿射变换与矩阵更新逻辑

**文件位置：** 核心计算回路位于 `frontend/src/components/three/TunnelGenerator.ts` 与 `frontend/src/utils/math.ts`。

当用户调整了“排水管间距”、“衬砌厚度”或由后端推回新的设计结果时，触发此步骤。

1.  **活跃实例遍历**：初始化一个针对 $N_{current}$ 的 `for` 循环。对于每一个实例索引 $i$，计算其在三维空间中的世界坐标。
2.  **空间矩阵推演**：利用 `math.ts` 提供的辅助计算工具，求解当前实例的空间状态方程。构建一个 $4 \times 4$ 的仿射变换矩阵 $M$。
    $$ M = T(t_x, t_y, t_z) \cdot R(\theta_x, \theta_y, \theta_z) \cdot S(s_x, s_y, s_z) $$
    *   **平移向量 (Translate)**：根据当前索引推算其所处的里程数（Z轴坐标），结合极坐标系下的角度推算 X、Y 坐标。
    *   **旋转四元数 (Rotate)**：为保证排水管或锚杆垂直于隧道壁，需计算该点在曲面上的法向量。利用向量叉乘求取法线方向，并将其转换为四元数（`Quaternion`）以规避万向节死锁。
    *   **缩放因子 (Scale)**：依据输入的“排水孔径”或“衬砌厚度”参数动态调整缩放比例。
3.  **矩阵覆写注入**：将计算所得的变换矩阵通过 `mesh.setMatrixAt(i, M)` 接口，直接覆盖显存缓冲中的历史数据。

### 步骤四：显式脏标记提交与渲染挂载

**文件位置：** 操作于 `frontend/src/components/three/TunnelGenerator.ts` 及各构件生成类中。

1.  **触发管线重绘**：在上述循环体结束、所有构件的空间矩阵参数更新完毕后，系统调用单条指令：
    `mesh.instanceMatrix.needsUpdate = true;`
    此操作作为脏标记（Dirty Flag），通知 WebGL 在下一帧绘制前，将主存中的 Float32Array 数组批量传输至 GPU 的 VRAM 中。
2.  **色彩与状态挂载**：若当前参数调整涉及应力状态或安全系数的变化，系统同步更新颜色缓冲。调用 `mesh.setColorAt(i, new THREE.Color(r, g, b))`，并随后执行 `mesh.instanceColor.needsUpdate = true`。

### 步骤五：顶点着色器动态形变映射与水位表达

**文件位置：** 核心逻辑分布于 `frontend/src/assets/shaders/`、`frontend/src/components/three/Environment.ts` 以及通过 `onBeforeCompile` 注入的材质钩子中。

针对标准矩阵变换无法处理的非线性几何变形（如围岩非均匀挤压造成的截面收敛，或地下水头高度的动态升降），引入 GLSL 着色器编程。

1.  **Uniforms 参数暴露**：在实例化网格的材质编译前阶段，拦截着色器代码，将前端 UI 传入的“水平收敛率”、“竖向沉降量”以及“地下水头标高”作为 `uniform` 变量暴露给显卡。
2.  **3D 水位平面参数建模**：
    *   **几何体生成**：在 `Environment.ts` 中创建一个横跨整个场景边界框的大型 `THREE.PlaneGeometry`，作为地下水位的可视基准面。
    *   **坐标绑定**：其纵向（Z轴）覆盖隧道的起始至终止里程，垂直向（Y轴）坐标直接绑定后端返回或前端滑块控制的地下水位参数。
    *   **着色器处理**：在水面材质的片段着色器（Fragment Shader）中，引入根据时间变量 `uTime` 滚动的噪点纹理以模拟水波纹理，同时计算其与隧道主体的深度交叉，使用不同颜色的 Alpha 混合来实现淹没区域的警示效果。
3.  **GPU 顶点偏移解算**：对于隧道管片受压变形的呈现，在 Vertex Shader 中编写偏移算法。读取管片顶点的原始坐标与世界中心轴的距离向量，依据注入的 `uniform` 变形参数库，利用 GPU 的并行计算能力实时更新顶点新位置。此举将极大降低 CPU 在处理多达数十万个顶点时的计算负荷。

---

## 三、 数据交换与双向绑定控制逻辑机制

为实现“计算参数-后端求解-前端可视化”的闭环，需构建严密的交换控制流。

**文件位置重点管控：**
*   交换控制器：`frontend/src/store/snapshotStore.ts`
*   输入输出拦截器：`frontend/src/components/ui/ParameterForm.vue`

1.  **表单状态与脏数据拦截 (Dirty Data Interception)**：
    * 约束条件：用户对任意输入参数（如注浆厚度、间距）进行滑动或修改后，必须立即判定当前 3D 画布中已映射的“安全系数云图”与“管网排布密度”失效。
    * 实现方式：在 ParameterForm.vue 的 onChange 事件中触发状态机 parameterStore 的更新逻辑，同步将 isDirty 状态标识置为 true 并主动销毁结果缓存。3D 渲染容器（Viewer3D.vue）订阅该状态，激活 CSS 后处理滤镜（如高斯模糊与灰度遮罩）冻结底层图表，并在画布顶层覆盖预警横幅
    * 影响范围：从数据总线层面切断陈旧结果与空间几何的视觉映射链路，避免工程误判，全程无需销毁底层显存的 Buffer 数据。
2.  **数据清洗与重新计算调度**：触发脏数据拦截后，用户的下一次计算指令将启动有效性清洗（如拦截管径负值）。清洗后的数据构造为 JSON 载荷，通过 Axios POST 请求发往后端 FastAPI 引擎进行重算。
3.  **结果状态回填**：后端基于物理方程解算完毕后，返回诸如“合理管间距：$2.5m$”、“最优化孔径：$110mm$”、“局部安全系数云图数据列表”等结果。`snapshotStore.ts` 接收该响应负载，并更新其内部的 `currentComputedResult` 状态树。
4.  **反应式级联触发**：由于 `Viewer3D.vue` 已预先通过 `watch` 监听了 Store 中的该状态树，数据的更替将瞬时触发**步骤一至步骤四**的重绘流程。三维场景中的排水管网将自动调整其分布密度（矩阵平移）与粗细（矩阵缩放），实现从数据到视觉的零延迟物理映射。

---

## 四、 其他附加 3D 模型与场景表现控制

为达到工程级数字孪生平台的专业视觉标准，需补充环境遮蔽与标量场可视化。

### 1. 虚拟应力探针与安全系数云图
**文件位置：** `frontend/src/components/three/PostProcessing.ts`

*   **安全系数云图（Heatmap）**：不再依赖外部预渲染贴图。系统读取后端返回的各节点主应力与孔隙水压力数值数组。在生成衬砌与围岩的 BufferGeometry 时，利用数值区间映射函数，生成对应的 RGB 顶点颜色（Vertex Color）数组（例如，低安全系数区域映射为红色，高系数区映射为蓝色），直接注入到几何体的 color 属性中。同时，该管线的更新权限受 parameterStore.isDirty 严格管控；若检测到脏数据标记，系统将挂起（Suspend）云图着色器的更新队列，防止错误应力数据被映射至新的几何参数上。
*   **空间拾取探针**：实例化 `THREE.Raycaster`。将鼠标的 2D 屏幕坐标投影至三维世界。利用 BVH（Bounding Volume Hierarchy）加速包围盒进行射线求交检测。当触碰有效实体（如某段异常衬砌）时，从数据缓冲中提取该顶点的具体应力值，并驱动 DOM 层面的 HTML 浮动标签（Tooltip）进行跟随显示。

### 2. 后期处理管道（Post-Processing Pipeline）
**文件位置：** `frontend/src/components/three/PostProcessing.ts`

*   在 `WebGLRenderer` 基础之上封装 `EffectComposer`。
*   **SSAO 层叠注入**：引入屏幕空间环境光遮蔽（SSAO Pass）。由于隧道场景内部光源复杂且结构遮挡严重（如管网与壁面接缝），SSAO 能通过分析深度缓冲动态生成接触阴影，使结构体具备真实的工程厚重感。
*   **Outline 交互提示**：启用轮廓描边着色器（Outline Pass）。当通过射线拾取（Raycaster）选中特定的排水管段或锚杆时，将该实例化网格的索引加入 Outline Pass 的选中列表，使其边缘散发特定颜色的光晕，替代传统的网格变色或材质替换方案，降低状态管理的复杂度。

本工程方案通过严格规划文件职能边界与底层显存调度机制，将确保在高密度参数化隧道模型场景下，系统仍能维持稳定流畅的工业级渲染与交互表现。