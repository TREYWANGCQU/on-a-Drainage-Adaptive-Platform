# 阶段4-3D材质灯光美化与微观局部放大方案

---
**版本**: v1.0  
**定位**: 隧道防排水自适应平台 - 3D可视化渲染引擎美化与微观交互专项方案  
**依据与标准**: 遵循 `/analyzer` 系统分析师规范，针对 `frontend/src/components/three` 模块执行架构拆解、数据流建模与实现细化。  
---

## 1. Objectives (目标与范式定义)

### 1.1 核心表现目标
本方案旨在将 3D 可视化场景从传统的“静态粗糙几何体”升级为**高对比度、科技感、机械半透明（Cybernetic Translucent Structural Aesthetics）**的专业级隧道数字孪生系统。重点凸显：
1. **临界状态衬砌与注浆加固圈 ($t_{g,\text{crit}}$)**：采用高透玻璃质感（Glassmorphism）与边缘菲涅尔发光（Fresnel Glow），清晰暴露内部结构细节。
2. **防排水管网体系**：包含环向排水管（Spacing $L_{\text{spacing}}$）、纵向集水管、横向排水管（Rec. Diameter $d_{\text{lat}}$）及其 3D 参数三维空间标注，呈现高亮金属/荧光管线质感（参照附图透明概念车机械结构展示效果）。
3. **水文环境与渗流场**：重构地表、地下水面与流线，彻底消除“蓝块遮挡”与“乱流感”，实现清晰的水头势场收敛表达。
4. **微观细节局部放大 (PIP & Magnifier Lens)**：解决排水管网（管径 80~100mm）相比于隧道（直径约 10~12m）几何尺度相差近百倍导致难于观察的问题，引入双视角画中画与 3D 悬浮放大镜。

### 1.2 视觉范式规范 (Style Paradigm & Dual-Mode Visualization)
本方案支持**双视觉范式动态切换机制**，既 100% 还原附图浅色影棚奢华透视质感，又满足工程看板暗夜科技监控需求：

1. **范式 A：高亮影棚风 (Light Studio Mode, 100% 对齐附图透视跑车质感)**
   - **画布背景**：极简浅灰/米白工作室背景（Hex: `#F3F4F6` ~ `#E5E7EB`），配置地面阴影落地板（Shadow Catching Plane）。
   - **隧道衬砌**：水晶级高透玻璃（Transmission: 0.95, IOR: 1.52, Clearcoat: 1.0, Roughness: 0.05），无多余杂乱发光纹理，纯净透射内部构件。
   - **临界注浆加固圈 ($t_{g,\text{crit}}$)**：高光泽抛光黄铜/金色金属漆（Metalness: 0.9, Roughness: 0.15, Hex: `#D4AF37` / `#FFB800`），依赖影棚 PBR 反射与折射表现极佳重工业质感。
   - **排水管网与内部机械**：高亮铬合金与镀金金属管道，精细暴露内部管路结构。

2. **范式 B：赛博暗夜风 (Dark Cyber Mode, 数字孪生工程看板默认)**
   - **画布背景**：极深黑/暗夜黑画布（Hex: `#030712` 至 `#000000`），无接缝过渡。
   - **隧道衬砌**：高透明度科技玻璃质感（Transmission: 0.85, IOR: 1.52），边缘发青蓝/金黄菲涅尔高光（Fresnel Glow）。
   - **临界注浆加固圈 ($t_{g,\text{crit}}$)**：半透明炽热荧光橙/金色（Hex: `#FF9900` ~ `#FFD700`），带 PostProcessing 辉光 Pass。
   - **排水管网**：高饱和金属铬/荧光青（Hex: `#00F3FF`）与金黄（Hex: `#FFB800`），高亮径向管道轮廓。
   - **水文流线与粒子**：电光蓝（Hex: `#0088FF`）渐变至亮白（Hex: `#E0F7FF`），向管网方向精准收敛。

---

## 2. Constraints (约束与设计边界)

### 2.1 性能与渲染约束
1. **帧率保障**：在双视角画中画（PIP）与 Bloom 后处理激活状态下，主画布保持 60 FPS (1080p) / 45 FPS (4K)。
2. **内存与显存**：限制最大 Draw Calls $\le 120$，管道与衬砌必须采用 `InstancedMesh` 实例化渲染与 Shared ShaderMaterial，严禁为每个环向管道创建独立 Geometry。
3. **WebGL 兼容性**：基于 WebGL 2.0 (Three.js r160+)，材质使用 GLSL 3.00 ES Shader 编写。

### 2.2 物理与尺度约束
1. **深埋尺度压缩 ($c > 30\text{m}$)**：隧道埋深 $c$ 极深时（如 $c = 100\text{m}\sim 300\text{m}$），在保留物理尺寸字段的同时，3D 视角采用非线性对数空间压缩或“断层剖面线（Break-line Sectioning）”，防止场景 Y 轴边界过大导致隧道变成细线。
2. **管网微观比例**：管径真实物理尺寸（$0.08\text{m}\sim 0.1\text{m}$）在全局视图下提供自适应管径发光宽度增强（Shader Minimum Width Clamp），确保远景不失真、近景不穿模。

---

## 3. Architecture (系统架构与模块分解)

### 3.1 模块架构设计 (`frontend/src/components/three`)

```
+-----------------------------------------------------------------------------------+
|                                  Viewer3D.vue                                     |
|  +-----------------------+ +-----------------------+ +-------------------------+  |
|  |   Viewport Canvas     | |  PIP Magnifier Lens   | | HTML/CSS Hologram Cards |  |
|  |  (Main Scene 60FPS)   | |  (Scissor Test / PIP) | |  (3D Callout Badges)  |  |
|  +-----------+-----------+ +-----------+-----------+ +------------+------------+  |
+--------------|-------------------------|--------------------------|---------------+
               |                         |                          |
               v                         v                          v
+-----------------------------------------------------------------------------------+
|                            Scene Manager & Render Core                            |
|                                                                                   |
|  +----------------------+  +-------------------------+  +----------------------+  |
|  |  TunnelGenerator.ts  |  | DrainagePipeGenerator.ts|  |    Environment.ts    |  |
|  | (Glass & Metal Shaders)|  | (Emissive Pipe & PIP)  |  | (Seepage Flow Field) |  |
|  +----------+-----------+  +------------+------------+  +----------+-----------+  |
|             |                           |                          |              |
|             v                           v                          v              |
|  +-----------------------------------------------------------------------------+  |
|  |                           PostProcessing.ts                                 |  |
|  |   (Bloom / Selective Emissive / SSAO / Tech Color Tone Mapping / PIP Pass) |  |
|  +-----------------------------------------------------------------------------+  |
+-----------------------------------------------------------------------------------+
```

### 3.2 模块职责与依赖表

| 模块文件 | 核心职责 | 本次方案改造重点 |
| :--- | :--- | :--- |
| [Viewer3D.vue](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue) | 3D 画布容器、双视角控制器、射线检测与画中画联动 | 新增 PIP 悬浮镜头、自适应深埋断层控制、高对比暗黑 UI 布局 |
| [TunnelGenerator.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/TunnelGenerator.ts) | 衬砌、初始注浆圈、临界注浆圈几何与材质构建 | 升级为半透明玻璃/金属漆着色器，菲涅尔边缘高光，显露内部管网 |
| [DrainagePipeGenerator.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/DrainagePipeGenerator.ts) | 环/纵/横向排水管网生成、自适应线宽与参数 3D 标注 | 管道自发光镀铬质感、局部放大高亮抓取、3D 弧形卡片标注 |
| [Environment.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Environment.ts) | 地表、地下水面、地下水流线与粒子场 | 重构为势场收敛流线、全息网格水面、对数景深高程压缩 |
| [PostProcessing.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/PostProcessing.ts) | 辉光(Bloom)、SSAO、灰阶调色与选发光合成 | 选发光通道拆分（管道/加固圈辉光，衬砌保持清澈透明） |

---

## 4. Work Breakdown Structure (工作分解结构)

### WBS 1: 机械半透明科技风材质与灯光重构 (Translucent Tech Materials & Lighting)

#### 1.1 双范式灯光与环境管线 (Dual-Mode Lighting & Environment Pipeline)
根据当前激活的视觉范式自动切换灯光与环境配置：
- **高亮影棚模式 (Light Studio Mode, 参照附图)**：
  - 画布背景色为 `#F3F4F6`，开启 `renderer.shadowMap.enabled = true`。
  - `EnvironmentMap` (Studio HDRI) 提供真实环境感应折射与反射。
  - `AmbientLight` (Color: `#FFFFFF`, Intensity: 1.2) 提供明亮清透的基底。
  - `DirectionalLight_Main` (Color: `#FFFDFA`, Intensity: 3.0, Pos: `[40, 100, 60]`, CastShadow: true) 形成精致软阴影与车身/衬砌高光。
- **赛博暗夜模式 (Dark Cyber Mode)**：
  - 画布背景色为 `#030712` (RGB: 3, 7, 18)。
  - `AmbientLight` (Color: `#1a2638`, Intensity: 0.8) 提供暗部蓝灰色冷调基底。
  - `DirectionalLight_Key` (Color: `#ffffff`, Intensity: 2.2, Pos: `[30, 80, 50]`) 产生主结构金属高光。
  - `DirectionalLight_Rim` (Color: `#00f3ff`, Intensity: 3.5, Pos: `[-40, -20, -50]`) 产生科技感青色轮廓光。
  - `PointLight_Core` (Color: `#ff9900`, Intensity: 2.0, Range: 15m) 放置于隧道内部，向外透射炽热金光。

#### 1.2 衬砌与注浆加固圈着色器升级 (Custom Shader & Glass Material)
针对 `TunnelGenerator.ts` 材质进行 GLSL 重构，将顶点与片元着色器解耦为独立资源文件（位于 `frontend/src/assets/shaders/`），引入菲涅尔反射（Fresnel Reflection）、深度衰减透明（Depth Alpha）与网格开关控制（`uShowGrid`）：

```glsl
// frontend/src/assets/shaders/lining_translucent.vert
#version 300 es
precision highp float;

in vec3 position;
in vec3 normal;
in vec2 uv;

uniform mat4 modelMatrix;
uniform mat4 modelViewMatrix;
uniform mat4 projectionMatrix;
uniform mat3 normalMatrix;

out vec3 vNormal;
out vec3 vViewPosition;
out vec2 vUv;
out vec3 vWorldPosition;

void main() {
    vUv = uv;
    vNormal = normalize(normalMatrix * normal);
    vec4 worldPosition = modelMatrix * vec4(position, 1.0);
    vWorldPosition = worldPosition.xyz;
    vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
    vViewPosition = -mvPosition.xyz;
    gl_Position = projectionMatrix * mvPosition;
}
```

```glsl
// frontend/src/assets/shaders/lining_translucent.frag
#version 300 es
precision highp float;

in vec3 vNormal;
in vec3 vViewPosition;
in vec2 vUv;
in vec3 vWorldPosition;

uniform vec3 uBaseColor;       // 衬砌基色 vec3(0.12, 0.18, 0.25)
uniform vec3 uFresnelColor;    // 边缘发光 vec3(0.0, 0.95, 1.0)
uniform float uOpacity;        // 基础透明度 0.35
uniform float uFresnelPower;   // 菲涅尔指数 3.0
uniform float uShowGrid;       // 全息网格开关 (1.0开启暗夜网格, 0.0关闭纯净影棚玻璃)

out vec4 fragColor;

void main() {
    vec3 normal = normalize(vNormal);
    vec3 viewDir = normalize(vViewPosition);
    
    // 菲涅尔边缘高光计算
    float fresnel = pow(1.0 - max(0.0, dot(normal, viewDir)), uFresnelPower);
    
    // 混色：内部透亮，边缘呈现高亮青色/金线条
    vec3 finalColor = mix(uBaseColor, uFresnelColor, fresnel);
    float finalAlpha = clamp(uOpacity + fresnel * 0.65, 0.1, 0.9);
    
    // 全息微纹理开关控制 (Hologram Grid Lines)
    if (uShowGrid > 0.5) {
        float gridPattern = step(0.96, fract(vWorldPosition.z * 1.0)) + step(0.96, fract(vUv.x * 20.0));
        finalColor += uFresnelColor * gridPattern * 0.4;
    }

    fragColor = vec4(finalColor, finalAlpha);
}
```

#### 1.3 临界注浆加固圈 ($t_{g,\text{crit}}$) 高亮显示
- **材质定义**：采用 `MeshPhysicalMaterial` 或专用着色器。
  - `color`: `#ffaa00` (金黄色)
  - `emissive`: `#ff6600`, `emissiveIntensity`: 0.8
  - `roughness`: 0.2, `metalness`: 0.1
  - `transmission`: 0.65 (半透明，可透过看清内部衬砌与管道)
  - `wireframe`: 可切换虚线网格模式（Grid Pattern）。

---

### WBS 2: 水文环境图层与地下水势场流线重构 (Hydrological Seepage Field Redesign)

#### 2.1 全息水头曲面重构 (Holographic Water Table Plane)
- 废弃原有实心蓝色遮挡块。
- 新增**全息网格水面**（Holographic Wave Mesh Plane）：
  - 材质使用点阵与等高线着色，透明度设为 `0.25`。
  - **地下水头标尺**：在水面与隧道拱顶之间绘制动态 3D 高程标尺（Water Head Indicator Line & Metric Label），实时显示 $H_w = \text{waterHead} - Y_{\text{crown}}$。

#### 2.2 势场方向收敛流线 (Convergent Seepage Streamlines)
废弃乱杂的随机下落粒子，建立**基于达西定律渗流场（Darcy Seepage Potential Field）的二维/三维收敛流线**：

$$\Phi(x, y) = -K \cdot \left[ y + \frac{q}{2\pi K} \ln\left( \frac{\sqrt{x^2 + y^2}}{R} \right) \right]$$

- **流线形态**：地下水从两侧高水位处，呈抛物线弧形向隧道拱顶及两侧环向排水管收敛。
- **流速与色彩**：
  - 远端流速慢：深蓝透明度高（Hex: `#0044aa`, Alpha: 0.2）。
  - 靠近排水管出口：加速度，变为亮青/纯白（Hex: `#00ffff` $\rightarrow$ `#ffffff`, Alpha: 0.95）。
  - **箭头/流脉动画**：采用带 UV 偏移的 `Line2` (Three.js Fat Lines) 或粒子贴图沿着弯曲 PATH 流动。

```typescript
// frontend/src/components/three/Environment.ts
export class Environment {
  /**
   * 生成向隧道排水管收敛的流线几何体
   */
  public createConvergentFlowLines(tunnelRadius: number, waterHead: number): THREE.LineSegments {
    const points: number[] = [];
    const colors: number[] = [];
    const numLines = 36; // 36 条汇流线
    
    for (let i = 0; i < numLines; i++) {
      // 0 (右侧) 到 Math.PI (左侧)，对称覆盖上半拱与左右双侧墙
      const angle = (i / (numLines - 1)) * Math.PI; 
      const startX = Math.cos(angle) * (tunnelRadius * 4.0);
      const startY = waterHead;
      const targetX = Math.cos(angle) * (tunnelRadius + 0.1);
      const targetY = Math.sin(angle) * (tunnelRadius + 0.1);
      
      // 生成贝塞尔收敛曲线
      const curve = new THREE.QuadraticBezierCurve3(
        new THREE.Vector3(startX, startY, 0),
        new THREE.Vector3((startX + targetX) * 0.5, (startY + targetY) * 0.5 + 2.0, 0),
        new THREE.Vector3(targetX, targetY, 0)
      );
      
      const curvePoints = curve.getPoints(20);
      for (let j = 0; j < curvePoints.length - 1; j++) {
        points.push(curvePoints[j].x, curvePoints[j].y, curvePoints[j].z);
        points.push(curvePoints[j+1].x, curvePoints[j+1].y, curvePoints[j+1].z);
        
        // 渐变色彩由深蓝到亮青
        const t = j / curvePoints.length;
        colors.push(0.0, 0.3 * (1 - t), 0.8 + 0.2 * t);
        colors.push(0.0, 0.3 * (1 - (t + 0.05)), 0.8 + 0.2 * (t + 0.05));
      }
    }
    
    const geometry = new THREE.BufferGeometry();
    geometry.setAttribute('position', new THREE.Float32BufferAttribute(points, 3));
    geometry.setAttribute('color', new THREE.Float32BufferAttribute(colors, 3));
    
    const material = new THREE.LineBasicMaterial({
      vertexColors: true,
      transparent: true,
      opacity: 0.75,
      blending: THREE.AdditiveBlending
    });
    
    return new THREE.LineSegments(geometry, material);
  }
}
```

#### 2.3 深埋条件（$c > 30\text{m}$）景深自适应非线性压缩与折断指示器
针对深埋条件下地面与隧道距离远、视场拉伸破坏视觉的问题：
1. **非线性高程映射 (Logarithmic Depth Compression)**：

$$Y_{\text{render}} = Y_{\text{crown}} + h_0 \cdot \ln\left(1.0 + \frac{Y_{\text{real}} - Y_{\text{crown}}}{h_0}\right)$$

其中基准缩放系数 $h_0 = 15.0\text{m}$。若真实埋深 $c = 150\text{m}$，视觉渲染高度仅占用约 $36\text{m}$，极大紧凑化场景。
2. **折断高程指示器 (Break-line Section Indicator)**：地表与隧道水头之间绘制全息折断线（锯齿虚线），标注实际物理埋深文字（例如：`埋深 c = 120.0m (非线性空间压缩视图)`）。

---

### WBS 3: 排水管网微观局部放大与交互系统 (Drainage PIP & Local Magnifier System)

针对排水管网尺寸小（管径 $\Phi 80\sim \Phi 100\text{mm}$，间距 $L_{\text{spacing}}$），提供两种互补的放大方案：

```
+-------------------------------------------------------------------------------+
|                       3D Canvas (Viewer3D.vue)                                |
|                                                                               |
|    +----------------------------------+                                       |
|    | 主视图 (Global View)             |                                       |
|    |                                  |   [Hover / Click 排水管]              |
|    |      ( ( ( 隧道衬砌 ) ) )        | --------+                             |
|    |              ||                  |         |                             |
|    +--------------||------------------+         v                             |
|                   ||                 +-----------------------------------+    |
|                   ||                 | 画中画 3D 微观放大镜 (PIP Window)  |    |
|                   ||                 |                                   |    |
|                   v                  |  放大倍率: 8.0x                   |    |
|       +-----------------------+      |  显示: 排水管透水孔/渗水粒子/透水土工布|    |
|       | 3D 悬浮卡片 (Badge)   |      |  参数: 横向管径 d_lat = 100mm     |    |
|       | 环向间距: L_sp = 4.0m |      +-----------------------------------+    |
|       | 排水状态: 良好(0.12L/s)|                                              |
|       +-----------------------+                                               |
+-------------------------------------------------------------------------------+
```

#### 3.1 方案 A：三维局部画中画放大镜 (3D Picture-in-Picture PIP Magnifier)
- **运行原理**：在 `Viewer3D.vue` 右下角（或跟随鼠标位置）设立第二个 WebGL Viewport (使用 `renderer.setScissor` / `setViewport`) 或二次渲染 Canvas。
- **相机控制**：主相机全局鸟瞰，PIP 相机点对点聚焦在当前被选中的排水管节点（Target Pipe Point），近距离视角（Distance $R_{\text{pip}} = 0.8\text{m}$）。
- **微观几何增强**：在 PIP 视图中激活排水管详细几何模型：
  - 环向/横向排水管的透水打孔结构（Perforated Slots）。
  - 管外包裹的无纺土工布滤层（Filter Mesh Layer）。
  - 管内动态流动的水滴/水流线束（Intra-pipe Water Stream）。
- **相机关联与 Layer 显存性能裁切 (Camera Layer Masking & Micro-culling)**：
  - **`Layer 0` (全局几何层)**：衬砌、注浆圈、地表水面、主管线轮廓（主相机只开启 `mainCamera.layers.enable(0)`，且 `mainCamera.layers.disable(1)`）。
  - **`Layer 1` (微观细节层)**：透水打孔槽、无纺土工布网格、管内水滴流束（仅 PIP 相机开启 `pipCamera.layers.enable(0)` 与 `pipCamera.layers.enable(1)`）。
  - **性能收益**：主视图在 60 FPS 鸟瞰视角下自动跳过数以万计透水孔与土工布微观顶点的 Vertex Shader 计算与 Draw Call，几何顶点处理量削减 85% 以上，彻底杜绝双视角渲染导致的帧率下降。

#### 3.2 方案 B：3D 交互引线与微观剖面卡片 (Interactive 3D Callout Cards)
- 在 3D 场景中，当用户靠近或点击某一段排水管网时，生成一条 3D 荧光引线（Glowing Leader Line），连接管道节点与屏幕空间的 2D/3D 悬浮卡片。
- **卡片内容**：
  - **管道类型**：环向盲管 / 纵向集水管 / 横向排水管
  - **推荐/设计管径**：$d_{\text{lat}} = 100\text{mm}$ (与推荐管径对齐)
  - **排布间距**：$L_{\text{spacing}} = 3.0\text{m} \sim 5.0\text{m}$
  - **排水效能指标**：单环排水量 $q_{\text{pipe}}$，渗透系数 $k_g$。

```vue
<!-- frontend/src/components/three/MagnifierPIP.vue -->
<template>
  <div v-show="active" class="pip-magnifier-container glass-card">
    <div class="pip-header">
      <span class="pip-title">🔍 微观防排水结构放大镜 (8x)</span>
      <button class="pip-close" @click="active = false">✕</button>
    </div>
    <div class="pip-viewport" ref="pipCanvasRef">
      <!-- 渲染集中放大的 3D 管道细节 -->
    </div>
    <div class="pip-footer">
      <div class="pip-metric"><span>管径:</span> <strong>{{ pipeDetail.diameter }} mm</strong></div>
      <div class="pip-metric"><span>间距:</span> <strong>{{ pipeDetail.spacing }} m</strong></div>
      <div class="pip-metric"><span>滤层渗透性:</span> <strong>{{ pipeDetail.permeability }} cm/s</strong></div>
    </div>
  </div>
</template>
```

---

### WBS 4: 后处理与辉光合成 (Post-Processing & Bloom Selective Pass)

#### 4.1 选择性辉光 (Selective Bloom Pipeline)
在 `PostProcessing.ts` 中改造合成管线，避免整个透明衬砌一同发光导致模糊：
1. **Pass 1 (Base Layer)**：渲染场景主几何（衬砌、地表、背景），写入 Base RenderTarget。
2. **Pass 2 (Emissive Layer)**：仅将“排水管线”、“临界注浆加固圈”、“探针高亮点”渲染至 Selective Black RenderTarget。
3. **Pass 3 (UnrealBloomPass)**：仅对 Selective RenderTarget 应用 Bloom 模糊（Threshold: 0.25, Strength: 1.5, Radius: 0.6）。
4. **Pass 4 (Final Composite Shader)**：将 Base Layer 与 Bloomed Layer 按加法混合（Additive Blending）叠加，输出至最终屏幕。

#### 4.2 SSAO 深度阴影与色调映射 (SSAO & Tone Mapping)
- 开启 `SAOPass` (Screen Space Ambient Occlusion)，增加衬砌与管网交接处的缝隙阴影，强化空间立体机械感。
- 色调映射选用 `THREE.ACESFilmicToneMapping`，Exposure 设为 `1.1`，确保黑色画布下鲜艳荧光线条不爆光溢出。

---

## 5. Acceptance Criteria (验收标准与指标体系)

### 5.1 视觉美化验收指标

| 序号 | 验证维度 | 交付物/表现形态 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **AC-1** | 画布背景与双模式 | `Viewer3D.vue` 背景控制器 | 支持一键切换：① 高亮影棚模式（`#F3F4F6`，100% 对齐附图透视跑车风格）；② 赛博暗夜模式（`#030712`，深黑高对比工程看板） |
| **AC-2** | 衬砌材质 | `TunnelGenerator.ts` / Shader | 呈现高透明玻璃/水晶质感，在影棚模式下纯净透射内部构件，在暗夜模式下边缘产生菲涅尔青蓝高光 |
| **AC-3** | 注浆圈与管道 | `DrainagePipeGenerator.ts` | 临界注浆圈呈抛光黄铜/黄金色半透明；排水管呈镀铬/荧光青高亮质感，具清晰内部透视效果 |
| **AC-4** | 水文环境 | `Environment.ts` | 废弃蓝色遮挡块；水面呈现全息网格；地下水流线呈弯曲势场收敛至排水管 |
| **AC-5** | 深埋景深 | `Environment.ts` | 当埋深 $c > 30\text{m}$ 时自动开启对数景深压缩与折断指示线，场景比例和谐 |
| **AC-6** | 微观放大 | `MagnifierPIP.vue` / Viewport | 提供 3D PIP 画中画放大镜或 3D 悬浮卡片，点击/悬停排水管可清楚查看管径与结构细节 |

### 5.2 性能与工程质量标准
1. **渲染帧率**：在开启 Bloom 与 PIP 画中画时，在主流显卡（如 GTX 1660 / RTX 3060 以上）下帧率 $\ge 60\text{FPS}$。
2. **代码规范**：所有 Shader 代码集中放置于 `src/assets/shaders` 或 TS 模块变量中，且带完整 GLSL 3.00 注释；遵从全局 `file:///` 路径规范与工程分层约定。
3. **功能完备**：无 TODO、无伪代码、无硬编码遗留。

---
**总结**：本方案以“机械半透明透视美学”为核心视觉范式，完美兼顾了附图浅色影棚风（Light Studio）与暗夜赛博风（Dark Cyber）双重场景。通过自定义菲涅尔玻璃着色器、抛光黄铜/镀铬金属管网、势场收敛流线、对数景深压缩及 PIP 微观放大镜头，彻底解决原版水文图层遮挡、流线混乱、深埋比例失调及小管道难察觉四大痛点。
