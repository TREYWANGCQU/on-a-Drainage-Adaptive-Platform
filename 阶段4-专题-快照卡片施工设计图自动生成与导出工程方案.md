# 【阶段4-工程方案】：工况快照标准 A3 施工设计图参数化自动生成与 PDF 导出技术方案

## 一、 Objectives (目标定义)

### 1.1 业务与功能目标
1. **快照卡片一键导出**：在前端快照边栏 `SnapshotSidebar.vue` 的每个历史/计算工况卡片（`snapshot-item`）上提供“施工图导出”功能，支持用户无需回溯或加载 3D 场景，即可毫秒级下载对应工况的标准工程施工图。
2. **标准 A3 横向工程 CAD 级高保真呈现**：严格参照标准工程制图规范与行业标杆图样，自动绘制标准 A3（$420\,\text{mm} \times 297\,\text{mm}$，横向 Landscape）矢量工程施工图，载明：
   - **拓扑几何形态**：自适应区分单洞（Single Tube）与双洞（Double Tube）形态；自适应区分三沟（含中心深排水沟）与双侧沟（无中心排水沟）构造；准确绘制初支、二衬、仰拱、沥青路面及排水沟槽。
   - **工程尺寸与轴线标注**：精确标注主洞与副洞轴线中线（点划线“隧道中线”）、双洞中心距尺寸线（如“隧道中心间距 30.00m”）、衬砌内净空与开挖轮廓半径。
   - **多维自适应排水参数引出标注**：
     - **环向打孔盲管**：多段折线引出标注拱顶/拱腰波纹管参数（如 `Φ50打孔波纹管 纵向间距10.0m`，与 `ring_diam_recommend` 及 `ring_spacing_recommend` 强绑定）。
     - **墙背纵向排水管**：引线标注左右拱脚墙背盲管（如 `HDPE DN100 双壁打孔波纹管 墙背纵向排水管 (全线贯通)`）。
     - **横向排水支管**：引线标注穿衬坡度汇流管（如 `横向排水支管 DN100双壁波纹管 (3%坡度) 纵向间距 20.0m`）。
   - **标准施工图必要制图要素**：
     - **工程图框与装订边**：标准 A3 内外图框（左侧装订边 $25\,\text{mm}$，上下右边距 $10\,\text{mm}$，内框线宽 $0.7\,\text{mm}$）。
     - **工程比例尺（Scale）**：标准比例尺条与比例标注（自适应 $1:50$ / $1:100$）。
     - **工程图名标注框与标题栏（Title Block）**：位于图纸右下角，规范载明：
       - 图名：`隧道衬砌与防排水系统横断面施工设计图`
       - 工程里程与工况：如 `DK0+000 ~ DK0+047`、工况备注及状态标示（🟢 原始计算工况 / 🔴 临界加固工况）
       - 出图时间戳（精确到秒，如 `2026-08-29 22:30:00`）
       - 依据平台：`隧道工程多维协同智能排水自适应平台 (V4.0)`
       - 默认版权标注：`智能排水协同研发团队 版权所有`
       - 工程审核与会签栏：设计、校核、审核、审定栏。
3. **输出格式**：以标准矢量 **PDF（A3 Landscape，300 DPI 矢量级打印品质）** 为主，并支持一键另存为超高清 PNG 图像。

---

## 二、 Constraints (边界与约束)

### 2.1 性能与依赖约束
1. **轻量纯前端矢量渲染与 PDF 生成引擎**：严禁引入重量级服务端依赖（如后端绘图微服务）或臃肿 CAD 运行时。采用原生 HTML5 Canvas 2D 结合轻量纯前端 PDF 封装库（`jspdf`），纯 TypeScript 实现，零后端网络 I/O 开销，支持纯离线与 Tauri 桌面环境秒级渲染导出。
2. **3D 渲染管线彻底解耦**：绘图引擎必须与 WebGL 3D 视口（`Viewer3D.vue`）彻底解耦，直接读取 Snapshot 数据模型（`snap.params` 与 `snap.results`），侧边栏可在后台无感知极速批量生成，不占用 WebGL 上下文、显存及相机状态。
3. **纯净工程要素原则**：严格剔除与本项目排水模型无关的辐射状发散射线，专注呈现真实的马蹄形衬砌截面、路面结构、三沟/双侧沟凹槽与环向/纵向/横向三套排水管网流路。

### 2.2 数据向下兼容与鲁棒性约束
1. **多级链式降级提取**：严格复用 `extractSnapshotValue` 规范，对历史遗留快照、部分缺损字段具备兜底机制（`critical_state` -> `original_state` -> `input_parameter` -> `params` 逐级降级），严禁因字段缺失抛出异常导致导出崩溃。
2. **设计图标准化工程色彩**：
   - 隧道轮廓与衬砌结构：工程深黑/高对比工程青蓝（`#0F172A` / `#0284C7`）
   - 隧道中线与基准轴线：工程灰色细点划线（`#475569`，`dash: [12, 4, 2, 4]`）
   - 排水盲管与水流流路：高对比工程深蓝/琥珀金（`#1D4ED8` / `#D97706`）
   - 标注文字与引出线：工程深黑（`#0F172A`）
   - 图框与标题栏边框：标准黑色加粗线（`#000000`）

---

## 三、 Architecture (架构设计与核心技术实现)

### 3.1 技术选型（最优方案：前端原生 2D 参数化矢量绘图 + jsPDF 导出引擎）

采用 **独立工具模块 `@/utils/blueprintGenerator.ts`**，以纯函数式输入/输出方式构建标准 A3 施工设计图渲染与 PDF 生成管线：
- **物理与矢量尺寸基准**：严格遵循 ISO 216 标准 A3 横向规格（$420\,\text{mm} \times 297\,\text{mm}$）。底层基于 $4200 \times 2970\,\text{px}$（对应 300 DPI 矢量级打印母版画布）进行精确像素与毫米映射。
- **执行效率**：单图生成时间 $< 30\,\text{ms}$，内存占用 $< 5\,\text{MB}$，生成后自动封装为标准 PDF / PNG Blob 并触发浏览器/Tauri 原生保存并释放内存。

```
   ┌────────────────────────────────────────────────────────┐
   │                  SnapshotSidebar.vue                   │
   │  (快照卡片列表 UI: snap-item -> [导出施工图] 按钮)        │
   └───────────────────────────┬────────────────────────────┘
                               │ 触发 click: exportSnapshotBlueprint(snap, 'pdf')
                               ▼
   ┌────────────────────────────────────────────────────────┐
   │             @/utils/blueprintGenerator.ts              │
   │  (ConstructionBlueprintGenerator 参数化出图引擎)        │
   ├────────────────────────────────────────────────────────┤
   │ 1. 结构化解构 Snapshot 参数 (单双洞/三沟/管径/间距)     │
   │ 2. 坐标转换系 (工程米制世界坐标 -> A3 毫米/像素坐标映射)   │
   │ 3. 几何绘制流水线:                                     │
   │    ├── drawA3FrameAndTitleBlock (绘制A3图框/标题栏/版权)│
   │    ├── drawTunnelLining (马蹄形初支/二衬几何轮廓)        │
   │    ├── drawInvertRoadAndDitches (仰拱/路面/三沟双侧沟)   │
   │    ├── drawDrainageSystem (环向/纵向/横向排水管路)       │
   │    ├── drawCenterlinesAndDimensions (中线/双洞间距标注)  │
   │    ├── drawPipeAnnotations (CAD级折线引出标注系统)      │
   │    └── drawScaleBar (自适应工程比例尺)                   │
   │ 4. 封装输出 jsPDF A3 Landscape 矢量 PDF / PNG 数据流   │
   └───────────────────────────┬────────────────────────────┘
                               │ 返回 PDF Blob / File
                               ▼
   ┌────────────────────────────────────────────────────────┐
   │                  浏览器 / Tauri 文件下载               │
   │  (命名: 施工设计图_DK0+000-DK0+047_原始工况_A3.pdf)     │
   └────────────────────────────────────────────────────────┘
```

---

### 3.2 核心数学与几何建模规范（严格与 3D 几何管线对齐）

#### 1. 马蹄形三心圆轮廓几何算法（与 `TunnelGenerator.ts` 及 `math.ts` 100% 对齐）
* **输入基准参数**：内净空半径 $r$，二衬外半径 $r_1 = r_s$（厚度 $t_{\text{lining}} = r_1 - r$），初支外半径 $r_2 = r_p$（厚度 $t_{\text{primary}} = r_2 - r_1$），高宽比 $\text{aspect\_ratio} = 0.7$。
* **几何偏置与核心参数推导**：
  $$R_{1\text{\_base}} = 1.05 \times r, \quad R_{2\text{\_base}} = 0.65 \times r, \quad R_{3\text{\_base}} = 1.80 \times r$$
  $$dx = R_{1\text{\_base}} - R_{2\text{\_base}} = 0.40 \times r$$
  $$dy = \sqrt{(R_{3\text{\_base}} - R_{2\text{\_base}})^2 - dx^2} = \sqrt{(1.15r)^2 - (0.40r)^2} = \sqrt{1.1625} \times r \approx 1.0782 \times r$$
  $$w = 2.1 \times r, \quad h = w \times \text{aspect\_ratio}$$
  $$H_{\text{side}} = \max(0.0, h - R_{1\text{\_base}} + dy - R_{3\text{\_base}})$$
  $$\text{invertCenterY} = -H_{\text{side}} + dy$$
* **分层轮廓生成（二衬内壁 $t=0$、二衬外壁 $t=t_{\text{lining}}$、初支外壁 $t=t_{\text{lining}}+t_{\text{primary}}$）**：
  - 拱顶大圆弧：半径 $R_1 = R_{1\text{\_base}} + t$，圆心 $(0, 0)$，角度区间 $[0, \pi]$。
  - 侧壁直墙过渡段（若 $H_{\text{side}} > 0$）：连接 $(\pm R_1, 0)$ 至 $(\pm R_1, -H_{\text{side}})$ 的垂直线段。
  - 拱脚侧圆弧：半径 $R_2 = R_{2\text{\_base}} + t$，圆心 $(\pm dx, -H_{\text{side}})$，自侧墙相切点连接至仰拱交界。
  - 仰拱底圆弧：半径 $R_3 = R_{3\text{\_base}} + t$，圆心 $(0, \text{invertCenterY})$。

#### 2. 仰拱路面与水沟凹槽几何
* **沥青路面定位**：
  $$\text{ditchH} = 0.8\,\text{m}, \quad dy_{\text{road}} = R_{3\text{\_base}} - \text{ditchH}$$
  $$\text{halfRoadW} = \sqrt{\max(0, R_{3\text{\_base}}^2 - dy_{\text{road}}^2)}, \quad \text{roadY} = \text{invertCenterY} - dy_{\text{road}}$$
* **侧边排水沟（双侧对称）**：
  - 侧沟设计底标高：$y_{\text{Bot\_side\_nominal}} = \text{roadY} - 0.3\,\text{m}$
  - 防穿模二衬极值外边界：$\max\_x\_lining = \sqrt{\max(0, R_{3\text{\_base}}^2 - (\text{invertCenterY} - y_{\text{Bot\_side\_nominal}})^2)}$
  - 侧沟槽位：外壁 $X_{\text{side\_outer}} = \max\_x\_lining - 0.05\,\text{m}$，内壁 $X_{\text{side\_inner}} = \max\_x\_lining - 0.45\,\text{m}$（槽宽 $0.40\,\text{m}$）。
  - 侧沟底安全标高：$y_{\text{Bot\_side}} = \max(\text{roadY} - 0.3, \text{invertCenterY} - \sqrt{R_{3\text{\_base}}^2 - X_{\text{side\_outer}}^2} + 0.05)$。
* **中央深排水沟（仅在 `has_central_ditch = true` 时绘制）**：
  - 水平范围：$X \in [-0.35, +0.35]\,\text{m}$（槽宽 $0.70\,\text{m}$）。
  - 沟底标高：$y_{\text{Bot\_central}} = \text{invertCenterY} - R_{3\text{\_base}} + 0.05\,\text{m}$。

#### 3. 排水管路几何布设与流路
* **环向打孔盲管**：沿二衬外壁（半径 $R_{1\text{\_ext}}, R_{2\text{\_ext}}$）敷设，下延终止于左右拱脚纵向管交汇点 $( \pm x_{\text{LongFoot}}, y_{\text{LongFoot}} )$。
* **拱脚纵向排水管**：左右各一根（主洞 $X = \pm x_{\text{LongFoot}}, Y = y_{\text{LongFoot}}$），全线贯通。
* **穿衬横向排水管**：以 $3\%$ 汇流下倾坡度（$k_{\text{slope}} = 0.03$）自拱脚纵向管引出：
  - 三沟模式：汇入中央深水沟侧壁（$X = \pm 0.35\,\text{m}, Y = y_{\text{LongFoot}} - 0.03 \times |x_{\text{LongFoot}} - 0.35|$）。
  - 双侧沟模式：直接汇入同侧边沟外壁。

#### 4. 双洞布局与自适应比例尺（Scale）
* **双洞模式**：主洞轴线位于 $X = +D_{\text{spacing}} / 2$，副洞轴线位于 $X = -D_{\text{spacing}} / 2$。
* **自适应工程比例尺计算**：
  - 工程总包络宽度：$W_{\text{world}} = \text{isDouble} ? (D_{\text{spacing}} + 2 \times r_2) : (2 \times r_2 + 2.0)$。
  - A3 绘图有效区域：宽 $360\,\text{mm}$，高 $220\,\text{mm}$。
  - 计算比例尺 $S = \min(360 / (W_{\text{world}} \times 1000), 220 / (H_{\text{world}} \times 1000))$，归一化取标准工程比例（$1:50$、$1:75$、$1:100$、$1:150$）。

---

## 四、 Work Breakdown Structure (工作分解结构 WBS)

```
阶段4-标准A3施工设计图导出方案
├── 1. 数据层与参数归一化
│   ├── 1.1 Snapshot 结构化参数提取器 (extractBlueprintParams)
│   ├── 1.2 单/双洞与三沟/双侧沟拓扑开关解构
│   └── 1.3 排水推荐值格式化 (m -> mm, 间距格式化, 状态前缀)
├── 2. A3 矢量参数化工程制图引擎 (@/utils/blueprintGenerator.ts)
│   ├── 2.1 A3 标准图框与装订线生成器 (装订边25mm, 边距10mm)
│   ├── 2.2 右下角标准工程标题栏 (Title Block & Engineering Metadata)
│   │   ├── 图名、工况里程区间 (DK0+000 ~ DK0+047)
│   │   ├── 出图时间戳与状态角标 (🟢 原始 / 🔴 临界加固)
│   │   ├── 依据平台: 隧道工程多维协同智能排水自适应平台
│   │   └── 默认版权: 西南交通大学 / 重庆大学 智能排水协同研发团队
│   ├── 2.3 自适应比例尺条与比例标注 (如 1:50 / 1:100)
│   ├── 2.4 马蹄形衬砌多层轮廓绘制 (二衬、初支、直墙段过渡)
│   ├── 2.5 仰拱填充、路面层与水沟凹槽 (三沟 / 双侧沟)
│   ├── 2.6 排水系统流路绘制 (环向盲管、纵向管、横向3%坡度管)
│   ├── 2.7 隧道中线点划线、双洞中心连线及跨度尺寸标注
│   └── 2.8 CAD 级折线引出线与多行参数排版标注
├── 3. PDF 矢量封装与下载触发
│   ├── 3.1 集成 jsPDF 生成 A3 Landscape 标准矢量 PDF 文件
│   ├── 3.2 支持超高清 PNG 备选格式导出
│   └── 3.3 自动生成工程规范命名 (如: 施工设计图_DK0+000-DK0+047_原始工况_A3.pdf)
└── 4. UI 交互与集成
    ├── 4.1 在 SnapshotSidebar.vue 快照卡片操作栏集成 [导出施工图] 按钮
    ├── 4.2 导出加载中反馈 (ElLoading / ElMessage 提示)
    └── 4.3 跨工况出图兼容性测试 (单洞/双洞/三沟/双侧沟/临界加固)
```

---

## 五、 Acceptance Criteria (验收指标)

| 编号 | 验收项 | 验收标准 | 验证手段 |
| :--- | :--- | :--- | :--- |
| **AC-01** | **UI 交互与秒级响应** | 快照侧边栏每一个快照卡片均展示“导出施工图”按钮；点击后 $100\,\text{ms}$ 内触发标准 A3 PDF 下载，不卡顿 3D 主视口。 | 点击不同快照卡片测试下载流程。 |
| **AC-02** | **A3 标准图框与标题栏** | 输出文件严格符合 A3 横向规格；具备完整内外图框、比例尺条、标题栏（载明图名、里程区间、状态、生成时间、依据平台、默认版权及会签栏）。 | 打开导出的 PDF，核对图纸版面与标题栏要素。 |
| **AC-03** | **拓扑形态与尺寸标注** | 单洞快照输出单洞截面；双洞快照准确输出对称双洞截面并精准标注中线间距；剔除无关辐射线；准确标注隧道中线点划线。 | 核对导出的单洞/双洞图纸尺寸标注。 |
| **AC-04** | **水沟与排水管网准确性** | 三沟工况载明中央深沟与横向坡度管；双侧沟工况准确呈现双侧边沟；环向/纵向/横向管网标注文字与快照参数完全一致。 | 分别导出有无中心水沟的快照核对管路与水沟。 |
| **AC-05** | **矢量打印品质** | 导出的 PDF 文件支持无限级放大不失真，线条清晰锐利，无文字重叠，具备 CAD 级出图品质。 | 放大 PDF 至 400% 检验矢量文字与引线抗锯齿。 |

