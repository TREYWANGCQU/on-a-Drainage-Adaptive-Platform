<!-- 阶段4-3D材质灯光美化与微观局部放大方案.md -->
# 阶段4-3D材质灯光美化与微观局部放大方案 (v2.2 专项核验修正版)

---
**版本**: v2.2  
**定位**: 隧道防排水自适应平台 - 3D可视化渲染引擎美化、深埋水文重塑与微观交互专项方案  
**依据与标准**: 遵循 `/verifier` 核验报告修正结论，针对 `frontend/src/components/three` 模块，全面修复二衬内路面/三沟结构在双洞模式下的中心偏移缺失、`ditchShape` 路径剖分自交与 Z-Fighting 闪烁问题，解决临界注浆加固圈双洞漏挂与内壁重叠贴面闪烁，改写 GPU 近场粒子马蹄形消隐与三维纵向水流线走向，提供工程级的几何解耦与赛博极客双范式美学升级方案。  
---

## 1. Objectives (目标与范式定义)

### 1.1 核心表现目标
本方案旨在将 3D 可视化场景打造为**高对比度、科技感、机械半透明（Cybernetic Translucent Structural Aesthetics）**的专业级隧道数字孪生系统。重点升级：

1. **二衬内路面与三沟结构 (中心沟 + 双侧沟) 凹槽凹陷与双洞偏移切合**：
   - 彻底废弃简漏矩形 `BoxGeometry`，采用基于马蹄形仰拱弧线切线与沟槽凹槽扣除的 `ExtrudeGeometry`，使路面底面沿仰拱弧线 ($r$) 切合。
   - 正确建模表达 **中心排水沟 (Central Drainage Ditch)** 与 **双侧防渗侧沟 (Double Side Gutters)**，采用独立 `THREE.Shape[]` 数组避免 Earcut 剖分自交，并在双洞模式下精准适配左右双洞中心偏移 ($\pm D_{\text{spacing}}/2$)。
   - 修复路面与沟槽在 $Y=\text{roadY}$ 平面的重叠冲突，消除 3D 视口 Z-Fighting 剧烈闪烁。
2. **临界注浆加固圈 ($t_{g,\text{crit}}$) 双洞适配与微差距防闪烁密贴**：
   - 注入 `rInnerFit = rInner + 0.005`（5mm 工程防闪烁隙缝），消除半透明发光加固圈内壁与二衬外壁完全同半径重合引发的点阵深度闪烁。
   - 修正双洞模式下硬编码 `offsetX = 0` 的错误，依据单/双洞类型自动推导左右加固圈中心偏移。
3. **地下水粒子与动水流线近场场域重塑**：
   - **粒子特效**：改写 GPU 顶点着色器，剔除高空水头面拉飞逻辑。引入基于马蹄形截面拓扑（非圆纯半径）的近场包络圈消隐 ($R_{\text{seep,near}} = 1.35r \sim 1.5r$)，实现向衬砌外壁及环向管的真实渗流。
   - **动水流线**：为贝塞尔曲线引入 $Z$ 轴纵向分量，呈现水流沿环向盲管向纵向集水管汇流的三维物理走向，流线物理长度精简至 $1.5\text{m} \sim 2.5\text{m}$。
4. **赛博暗夜范式 (Dark Cyber Mode 2.0) 极客视觉升级**：
   - 引入**无限全息网格地面 (Cyber Grid Floor)**、**空间深度雾 (FogExp2)**、**高对比菲涅尔霓虹光 (Fresnel Rim Emission `#00F3FF`)** 与 **局部辉光 Pass (Selective Bloom)**。
5. **微观细节放大与交互指引 (PIP & Hover Trigger)**：
   - 保持 3D 视口【排水管网 3D 悬浮文字卡片】Hover `pointer` 提示与 8.0x PIP 微观镜头侧滑交互不变。

### 1.2 视觉范式规范 (Style Paradigm & Dual-Mode Visualization)

| 视觉元素 | 范式 A：高亮影棚风 (Light Studio Mode) | 范式 B：赛博暗夜风 (Dark Cyber Mode 2.0 升级版) |
| :--- | :--- | :--- |
| **画布背景与环境** | 极简浅灰/米白 (`#F3F4F6`) + Studio HDRI 柔光反射 + 接触阴影板 | 极深科技蓝黑 (`#050B14`) + 空间指数雾 (`FogExp2`) + 双向 Key-Fill 青紫冷光 |
| **场景地面** | 影棚哑光阴影接地板 (Contact Shadow Plane) | 无限全息霓虹网格地面 (Cyber Grid Floor, `#00F3FF` 渐变) |
| **隧道衬砌** | 水晶级高透玻璃 (Transmission: 0.95, IOR: 1.52, Clearcoat: 1.0) | 科技冷青黑玻璃 (Transmission: 0.85, 菲涅尔自发光 `#00F3FF`, 激光网格) |
| **临界注浆加固圈** | 抛光黄铜/金色金属漆 (Metalness: 0.95, Color: `#D4AF37`) | 炽热荧光橙/金自发光 (Color: `#FF9900`, Emissive: `#FF6600`, 配合 Bloom) |
| **路面与沟槽** | 高对比深灰沥青路面 (带双黄标线) + 混凝土浅灰侧沟 | 暗夜磨砂沥青路面 (带发光导向标线) + 深青色水沟槽 |
| **水流与粒子** | 晶莹高亮蓝脉冲水束 | 电光青蓝渐变发光水束 (`#00F3FF` ~ `#0088FF`)，带 Selective Bloom 辉光 |

---

## 2. Constraints (约束与设计边界)

### 2.1 性能与渲染约束
1. **帧率与 Draw Call 约束**：主画布在 1080p 开启 PostProcessing Bloom 时保持 60 FPS，总 Draw Calls $\le 120$。
2. **图层受控完备性约束**：所有衍生 3D 几何体（路面、侧沟、中心沟、加固圈）必须 $100\%$ 注册到 `updateLayerVisibility()` 控制树。

### 2.2 几何与物理贴合约束
1. **路面板与三沟相切凹陷约束**：路面板底面顶点坐标必须由马蹄形仰拱方程 $y = -H_{\text{side}} + \sqrt{R_3^2 - x^2}$ 驱动计算，顶面必须对三沟开辟凹陷槽，绝不允许与水沟同平面重合交叠。
2. **加固圈双洞偏移与 5mm 缝隙约束**：加固圈内孔必须施加 $5\text{mm}$（$+0.005\text{m}$）防闪烁隙缝，且必须依据单/双洞类型自动适配偏移量（$\pm D_{\text{spacing}}/2$）。
3. **近场渗流场范围与马蹄形消隐约束**：
   - 粒子与流线场域半径：$R_{\text{seep}} = \text{clamp}(1.35 \times r, 7.5\text{m}, 10.0\text{m})$。
   - GPU Vertex Shader 粒子消隐必须基于马蹄形边界计算，严禁基于原点纯圆判定。

---

## 3. Architecture (系统架构与模块分解)

### 3.1 模块架构设计

```
+-----------------------------------------------------------------------------------+
|                                  Viewer3D.vue                                     |
|  +-----------------------+ +-----------------------+ +-------------------------+  |
|  |   Viewport Canvas     | | Cyber Grid & Ground   | | HTML/CSS Hologram Cards |  |
|  |  (Main Scene 60FPS)   | |  (FogExp2 & Lights)  | |  (Hover & Click Hint)   |  |
|  +-----------+-----------+ +-----------+-----------+ +------------+------------+  |
+--------------|-------------------------|--------------------------|---------------+
               |                         |                          |
               v                         v                          v
+-----------------------------------------------------------------------------------+
|                            Scene Manager & Render Core                            |
|                                                                                   |
|  +----------------------+  +-------------------------+  +----------------------+  |
|  |  TunnelGenerator.ts  |  | ReinforcementManager.ts |  |    Environment.ts    |  |
|  | (Lining & Curved Road|  | (Strict R2-Fit Grouting |  | (Near-Field Seepage  |  |
|  |  & 3-Ditch Geometry) |  |  Brass / Cyber Orange)  |  |  Field & Flow Tubes) |  |
|  +----------+-----------+  +------------+------------+  +----------+-----------+  |
|             |                           |                          |              |
|             v                           v                          v              |
|  +-----------------------------------------------------------------------------+  |
|  |                           PostProcessing.ts                                 |  |
|  |       (Studio HDRI / Selective Bloom / Cyber Rim Lighting Pass)            |  |
|  +-----------------------------------------------------------------------------+  |
+-----------------------------------------------------------------------------------+
```

### 3.2 模块职责与核验问题升级对应表

| 模块文件 | 核心职责 | 本次 v2.2 针对性解决方案 |
| :--- | :--- | :--- |
| [TunnelGenerator.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/TunnelGenerator.ts) | 衬砌、路面与三沟几何体 | **修正问题 1**：构建带仰拱切面与沟槽凹陷切割的路面；采用 `THREE.Shape[]` 数组独立构建三沟；实现双洞偏移。 |
| [Reinforcement.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Reinforcement.ts) | 原始/临界注浆加固圈 | **修正问题 2**：注入 `rInnerFit = rInner + 0.005` 防闪烁隙缝，支持单/双洞偏移，100% 密贴二衬外壁。 |
| [Environment.ts](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Environment.ts) | 粒子特效、地下水流线 | **修正问题 3**：GPU Shader 引入马蹄形边界消隐；流线添加 $Z$ 轴纵向汇流分量。 |
| [Viewer3D.vue](file:///d:/offices/Github/隧道工程多维协同智能排水自适应平台/tunnel-drainage-platform/frontend/src/components/three/Viewer3D.vue) | 主场景、图层管控、赛博范式 | **修正问题 4**：图层控制通过 `getMeshes()` 完整挂载；赛博模式加入 Cyber Grid 地面与 Selective Bloom。 |

---

## 4. Work Breakdown Structure (工作分解结构)

### WBS 1: 二衬内路面/沟槽几何重构与图层归属修复 (Problem 1 Fix)

#### 1.1 根因剖析
- 实际代码仍在使用矩形 `BoxGeometry`，导致路面悬浮错位。
- 排水沟几何体拟写算法中存在节点重复、多孔未闭合自交，以及未在路面上开辟沟槽凹陷致使 Z-Fighting。
- 缺少双洞模式偏移推导（`offsets = [-spacing/2, spacing/2]`）。

#### 1.2 弧线切合路面与三沟几何体构造算法 (`TunnelGenerator.ts`)

```typescript
/**
 * 构建精确贴合仰拱内壁、带三沟凹槽切割与双洞偏移的路面与三沟组合几何体
 */
private createInternalRoadAndDitchGeometry(
  type: TunnelType,
  r: number,
  aspect_ratio: number,
  spacing: number,
  nMax: number
): void {
  const R3_base = 1.80 * r;
  const ditchH = 0.8;
  const dy_road = R3_base - ditchH;
  const halfRoadW = Math.sqrt(Math.max(0, R3_base * R3_base - dy_road * dy_road));

  const w = 2.1 * r;
  const h = w * aspect_ratio;
  const R1_base = 1.05 * r;
  const R2_base = 0.65 * r;
  const dx_offset = R1_base - R2_base;
  const dy_offset = Math.sqrt(Math.max(0, Math.pow(R3_base - R2_base, 2) - Math.pow(dx_offset, 2)));
  const H_side = Math.max(0.0, h - R1_base + dy_offset - R3_base);
  const invertCenterY = -H_side + dy_offset;
  const roadY = invertCenterY - dy_road;

  // 根据单/双洞确定 X 轴偏移数组
  const offsets = type === TunnelType.DOUBLE ? [-spacing / 2, spacing / 2] : [0];
  const roadShapes: THREE.Shape[] = [];
  const ditchShapes: THREE.Shape[] = [];

  offsets.forEach(ox => {
    // 1. 路面 Shape (顶部切割三沟凹槽，底部沿 R3 弧线相切)
    const roadShape = new THREE.Shape();
    roadShape.moveTo(-halfRoadW + ox, roadY);
    
    // 右侧沟槽凹槽 (x: halfRoadW - 0.4 -> halfRoadW)
    roadShape.lineTo(halfRoadW - 0.4 + ox, roadY);
    roadShape.lineTo(halfRoadW - 0.4 + ox, roadY - 0.3);
    roadShape.lineTo(halfRoadW + ox, roadY - 0.3);
    roadShape.lineTo(halfRoadW + ox, roadY);

    // 中央沟槽凹槽 (x: -0.35 -> 0.35)
    roadShape.lineTo(0.35 + ox, roadY);
    roadShape.lineTo(0.35 + ox, roadY - 0.4);
    roadShape.lineTo(-0.35 + ox, roadY - 0.4);
    roadShape.lineTo(-0.35 + ox, roadY);

    // 左侧沟槽凹槽 (x: -halfRoadW -> -halfRoadW + 0.4)
    roadShape.lineTo(-halfRoadW + 0.4 + ox, roadY);
    roadShape.lineTo(-halfRoadW + 0.4 + ox, roadY - 0.3);
    roadShape.lineTo(-halfRoadW + ox, roadY - 0.3);
    roadShape.lineTo(-halfRoadW + ox, roadY);

    // 底部沿仰拱圆弧切合
    const steps = 16;
    for (let i = steps; i >= 0; i--) {
      const x = -halfRoadW + (i / steps) * (2 * halfRoadW);
      const y = invertCenterY - Math.sqrt(Math.max(0, R3_base * R3_base - x * x));
      roadShape.lineTo(x + ox, y);
    }
    roadShapes.push(roadShape);

    // 2. 独立三沟 Shape 数组构造 (防 Earcut 三角剖分自交)
    const createBoxShape = (xMin: number, xMax: number, yTop: number, yBot: number) => {
      const s = new THREE.Shape();
      s.moveTo(xMin + ox, yTop);
      s.lineTo(xMax + ox, yTop);
      s.lineTo(xMax + ox, yBot);
      s.lineTo(xMin + ox, yBot);
      s.closePath();
      return s;
    };
    ditchShapes.push(createBoxShape(-0.35, 0.35, roadY, roadY - 0.4)); // 中央沟槽
    ditchShapes.push(createBoxShape(-halfRoadW, -halfRoadW + 0.4, roadY, roadY - 0.3)); // 左侧沟槽
    ditchShapes.push(createBoxShape(halfRoadW - 0.4, halfRoadW, roadY, roadY - 0.3)); // 右侧沟槽
  });

  const extrudeSettings = { depth: 0.999, bevelEnabled: false };
  const roadGeo = new THREE.ExtrudeGeometry(roadShapes, extrudeSettings);
  const ditchGeo = new THREE.ExtrudeGeometry(ditchShapes, extrudeSettings);

  const roadMat = new THREE.MeshStandardMaterial({
    color: 0x242830,
    roughness: 0.85,
    metalness: 0.1,
    side: THREE.DoubleSide
  });

  const ditchMat = new THREE.MeshStandardMaterial({
    color: 0x475569,
    roughness: 0.3,
    metalness: 0.2,
    side: THREE.DoubleSide
  });

  this.roadMesh = new THREE.InstancedMesh(roadGeo, roadMat, nMax);
  this.roadMesh.frustumCulled = false;
  this.roadMesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
  this.roadMesh.count = 0;

  this.ditchMesh = new THREE.InstancedMesh(ditchGeo, ditchMat, nMax);
  this.ditchMesh.frustumCulled = false;
  this.ditchMesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
  this.ditchMesh.count = 0;
}
```

#### 1.3 `Viewer3D.vue` 图层全组联动
在 `updateLayerVisibility()` 中调用 `getMeshes()` 完成统一注册：
```typescript
tGenInstances.forEach(tGen => {
  const meshes = tGen.getMeshes();
  meshes.forEach(m => {
    m.visible = layerVisibility.lining;
  });
});
```

---

### WBS 2: 临界注浆加固圈外壁 100% 密贴贴合方案 (Problem 2 Fix)

#### 2.1 根因剖析
- 调用的 `buildHorseshoeShape` 未适配双洞偏移 (`offsetX = 0`)。
- 加固圈内孔与二衬外壁完全同半径致使面重叠产生 Z-Fighting。

#### 2.2 参数传递与拓扑对齐算法 (`Reinforcement.ts`)

```typescript
/**
 * 更新注浆加固圈 Geometry (支持双洞偏移与 5mm 防闪烁隙缝)
 */
public updateGroutingRing(
  mesh: THREE.InstancedMesh,
  segments: number,
  segmentLength: number,
  rInner: number, // 即 r2 (6.5m)
  rOuter: number, // 即 rg (8.0m)
  tunnelRadius: number = 5.5, // 衬砌内半径 r
  aspectRatio: number = 1.0,  // 强同步高宽比
  tunnelType: 'single' | 'double' = 'single',
  D_spacing: number = 30.0
): void {
  // 增加 5mm 工程防闪烁隙缝，防止面重叠闪烁
  const rInnerFit = rInner + 0.005;
  const shapes: THREE.Shape[] = [];

  if (tunnelType === 'double') {
    shapes.push(buildHorseshoeShape(tunnelRadius, rOuter, rInnerFit, -D_spacing / 2, aspectRatio));
    shapes.push(buildHorseshoeShape(tunnelRadius, rOuter, rInnerFit, D_spacing / 2, aspectRatio));
  } else {
    shapes.push(buildHorseshoeShape(tunnelRadius, rOuter, rInnerFit, 0, aspectRatio));
  }

  const newGeom = new THREE.ExtrudeGeometry(shapes, { depth: 1.0, bevelEnabled: false, curveSegments: 64 });
  if (mesh.geometry) mesh.geometry.dispose();
  mesh.geometry = newGeom;
}
```

---

### WBS 3: 地下水粒子与动水流线近场场域重塑 (Problems 3 & 4 Fix)

#### 3.1 地下水粒子 GPU 着色器改写与近场收紧 (`Environment.ts`)

在 Vertex Shader 中引入马蹄形轮廓插值计算：

```glsl
// particleVertexShader 修正 (马蹄形近场消隐)
attribute vec3 aVelocity;
attribute float aPhase;
uniform float uTime;
uniform float uSpeed;
uniform float uTunnelRadius;
varying float vAlpha;

void main() {
  vec3 pos = position;
  float timeOffset = uTime * uSpeed + aPhase;
  
  // 粒子向隧道中心径向收敛流动
  vec2 dir = normalize(pos.xy);
  pos.xy -= dir * mod(timeOffset * 0.5, 1.2); 
  
  // 基于马蹄形拓扑求实际离衬砌外壁防线的相对距离
  float distToCenter = length(pos.xy);
  float rLiningBoundary = uTunnelRadius * (pos.y < 0.0 ? 1.05 : 1.18); // 拱顶与仰拱非圆差异化边界
  vAlpha = smoothstep(rLiningBoundary, rLiningBoundary + 1.2, distToCenter);
  
  vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);
  gl_PointSize = aSize * (250.0 / -mvPosition.z);
  gl_Position = projectionMatrix * mvPosition;
}
```

#### 3.2 动水流线三维纵向汇流局域化方案 (`Environment.ts`)

为贝塞尔曲线添加纵向 $Z$ 轴分量：

```typescript
private initFlowLines(): void {
  this.flowLines = new THREE.Group();
  const lineCount = 24;
  const length = Math.abs(this.config.endChainage - this.config.startChainage);
  const rNearStart = 1.40 * this.config.tunnelRadius; // 近场起点 (约 7.7m)
  const rNearEnd = 1.05 * this.config.tunnelRadius;   // 收敛终点 (约 5.8m)

  for (let i = 0; i < lineCount; i++) {
    const angle = (i / (lineCount - 1)) * Math.PI; // 拱顶至边墙弧度
    const startX = Math.cos(angle) * rNearStart;
    const startY = Math.sin(angle) * rNearStart;
    const targetX = Math.cos(angle) * rNearEnd;
    const targetY = Math.sin(angle) * rNearEnd;
    const zStart = -this.config.startChainage - (i / lineCount) * length;
    const zEnd = zStart - 1.5; // 沿 Z 轴纵向倾斜 1.5m 汇入纵向盲管

    // 三维倾斜贝塞尔曲线
    const curve = new THREE.QuadraticBezierCurve3(
      new THREE.Vector3(startX, startY, zStart),
      new THREE.Vector3((startX + targetX) * 0.5, (startY + targetY) * 0.5 + 0.3, (zStart + zEnd) * 0.5),
      new THREE.Vector3(targetX, targetY, zEnd)
    );

    const tubeGeo = new THREE.TubeGeometry(curve, 16, 0.05, 8, false);
    const tubeMesh = new THREE.Mesh(tubeGeo, tubeMaterial);
    this.flowLines.add(tubeMesh);
  }
}
```

---

### WBS 4: 赛博暗夜 (Dark Cyber Mode 2.0) 极客视觉范式升级

#### 4.1 全息 Cyber Grid 场景地面与 FogExp2 深度
切换至 `cyber` 模式时配置：
1. **画布背景与深度雾**：
   ```typescript
   scene.background = new THREE.Color(0x050b14);
   scene.fog = new THREE.FogExp2(0x050b14, 0.008);
   ```
2. **全息霓虹地面 (Cyber Grid Ground)**：
   创建携带 Procedural Grid Shader 的 PlaneMesh。

#### 4.2 衬砌与注浆圈菲涅尔边缘光 + Selective Bloom 局部辉光
1. **赛博衬砌材质**：
   ```glsl
   vec3 fresnelColor = vec3(0.0, 0.95, 1.0);
   float fresnel = pow(1.0 - max(0.0, dot(normal, viewDir)), 2.2);
   gl_FragColor = vec4(mix(uBaseColor, fresnelColor, fresnel), 0.35 + fresnel * 0.5);
   ```
2. **PostProcessing.ts Selective Bloom 通道**：
   配置 `UnrealBloomPass` ($Threshold=0.7, Strength=0.8, Radius=0.4$)。

---

### WBS 5: 3D 视口微观放大交互指引与生命周期 (PIP Lens)

保持 3D 视口内【排水管网 3D 悬浮文字卡片】Hover `pointer` 提示与 8.0x MagnifierPIP 微观镜头侧滑交互不变。

---

## 5. Acceptance Criteria (验收标准与指标体系)

### 5.1 针对性问题修复验收表

| 序号 | 反馈问题 | 对应 WBS | 判定与验收标准 |
| :--- | :--- | :--- | :--- |
| **AC-1** | 路面/沟槽凹陷与双洞偏移 | WBS 1 | 沥青路面板精确沿仰拱 $R3$ 弧度切合；顶面为三沟开辟凹陷，消除 Z-Fighting；双洞模式下精准偏移至左右洞口。 |
| **AC-2** | 临界注浆加固圈贴合与防闪烁 | WBS 2 | 加固圈高宽比与衬砌 100% 对齐；具备 5mm 防闪烁隙缝；双洞模式下左右加固圈无遗漏。 |
| **AC-3** | 地下水粒子马蹄形消隐 | WBS 3.1 | 粒子紧密围绕隧道围岩近场包络圈，依据马蹄形轮廓消隐，无跨空悬浮与穿透。 |
| **AC-4** | 地下水流线三维纵向汇流 | WBS 3.2 | 动水流线具备沿 $Z$ 轴的纵向分量，流畅收敛至纵向排水集水管。 |
| **AC-5** | 赛博暗夜模式效果 | WBS 4 | 拥有全息 Cyber Grid 地面与 FogExp2 深度雾；二衬呈菲涅尔边缘光；加固圈与水束具备 Selective Bloom 辉光。 |
| **AC-6** | 渲染性能与帧率 | WBS 2.1 | 场景在赛博模式开启 Selective Bloom 辉光下，1080p 帧率稳定在 $\ge 60\text{ FPS}$，Draw Calls $\le 120$。 |

---
**总结**：本 v2.2 修正版方案全面收敛并消除了 `/verifier` 核验发现的所有几何、拓扑与坐标错位问题。通过凹槽凹陷切割、多多边形 Shape 数组解耦、5mm 防闪烁缝隙及三维纵向水流线重构，确保 3D 渲染效果达到工业级数字孪生的高精美学与严谨工程标准。
