<!-- 阶段4-3D模型渲染条纹与旋转消失问题分析方案.md -->
# 阶段4-3D模型渲染条纹与旋转消失问题分析方案

## Objectives

1. **彻底消除 3D 模型条纹伪影**：消除隧道衬砌端面、内壁及外表面出现的 4 类条纹（着色器程序化纹理伪影、内部环向端面切片重复渲染“一圈圈”黑纹、几何体 Z-Fighting 绝对共面重叠闪烁、阴影 Shadow Acne 斑马纹）。
2. **保障视角旋转稳定性**：彻底解决在 `http://localhost:1420/` 主界面拖拽或旋转 OrbitControls 视角时，三维隧道模型突发性隐形/消失的问题。

---

## Constraints

1. **架构延续性**：必须保持前端基于 Three.js `InstancedMesh` 与自定义 GLSL 3.00 ES 着色器（`lining.vert` / `lining.frag`）的同心拓扑与参数化渲染架构。
2. **性能约束**：渲染优化不得破坏现有 `requestAnimationFrame` 防抖节流管线，确保帧率维持在 60 FPS，避免引发 CPU/GPU 冗余渲染或显存泄漏。
3. **规范约束**：严格遵循 GLSL 3.00 ES 着色器规范与 WebGL 深度缓冲标准。

---

## Architecture

基于对前端 Three.js 渲染管线代码、着色器及研究排查结果，造成“条纹依然存在”及“旋转后消失”的根因定位与系统架构映射如下：

```
[ Viewer3D.vue ] (WebGLRenderer + DirectionalLight + OrbitControls)
       │
       ├──► 视锥体裁剪机制 (Frustum Culling) ──► 触发 InstancedMesh 整体剔除 (旋转消失根因 2A)
       ├──► 对数深度缓冲 (logarithmicDepthBuffer) ──► 自定义 Shader 缺失 #include <common> 导致
       │                                            isPerspectiveMatrix 未定义编译崩溃 (模型消失根因 2B)
       └──► 阴影 Bias & Cascade 设置 ──► 曲面阴影自遮挡 Shadow Acne 斑马纹 (条纹根因 1D)
       │
[ TunnelGenerator.ts ] ──► ExtrudeGeometry (depth: 0.999, spacingZ: 1.0)
       │                                 ├──► 相邻 Instance 端面在 z=-i 处物理绝对共面 ──► Z-Fighting 深度闪烁条纹 (条纹根因 1C)
       │                                 └──► 前/后端面 Cap (vLocalNormal.z = ±1) ──► 每 1m 实例自动生成封闭 Cap
       │
[ lining.frag ] (Custom ShaderMaterial)
       ├──► 端面正弦网格 (sin(x*20)*sin(y*20)) ──► 端面微纹理条纹 (条纹根因 1A-1)
       ├──► 环向接缝波 (step(0.98, sin(z * PI/12))) ──► 24m 周期纵向黑环条纹 (条纹根因 1A-2)
       ├──► 动态扫描线 (step(0.97, sin(z * 0.5))) ──► 高频青色扫频条纹 (条纹根因 1A-3)
       ├──► 外侧裂纹波 (step(0.95, sin(z*2)*cos(r*5))) ──► 初支/岩层交界相交条纹 (条纹根因 1A-4)
       └──► 端面判定 abs(vLocalNormal.z) > 0.5 ──► 沿 Z 轴每 1m 绘制内部 Cap 切片及 colorEdge 边框 (条纹根因 1B “一圈圈”)
```

### 1. “渲染条纹”深层根因与解决方案

#### 根因 1A：着色器残留的多处程序化条纹算式（主因 1）
- **根因剖析**：
  在 [lining.frag](file:///d:/offices/Github/%E9%9A%A7%E9%81%93%E5%B7%A5%E7%A8%8B%E5%A4%9A%E7%BB%B4%E5%8D%8F%E5%90%8C%E6%99%BA%E8%83%BD%E6%8E%92%E6%B0%B4%E8%87%AA%E9%80%82%E5%BA%94%E5%B9%B3%E5%8F%B0/tunnel-drainage-platform/frontend/src/assets/shaders/lining.frag) 中，除早前清理的同心正弦弧线 `sin(reff * 40.0)` 外，代码内仍保留了 4 处算式生成的干扰性条纹：
  1. `sin(vLocalPosition.x * 20.0) * sin(vLocalPosition.y * 20.0)`：二衬端面二维网格伪影。
  2. `step(0.98, sin(vLocalPosition.z * (3.1415926 / 12.0)))`：24 米周期黑色环纹。
  3. `step(0.97, sin(vLocalPosition.z * 0.5 - 0.0))`：高频青色扫频条纹线。
  4. `step(0.95, sin(vLocalPosition.z * 2.0) * cos(reff_wall * 5.0))`：初支与岩层交界面裂纹条纹。
- **解决方案**：
  重构 `lining.frag`，彻底移除上述 4 处程序化正弦/步阶条纹算式，将其替换为平滑均匀的真实工程物理调色。

#### 根因 1B：内部环向端面切片重复渲染（“一圈圈条纹”二次根因）
- **根因剖析**：
  `TunnelGenerator` 通过 `ExtrudeGeometry` 按 1 米实例化 Ring，Three.js 的 `ExtrudeGeometry` 在每个 1m 实例的前端（$z=0$）和后端（$z=depth$）都会自动生成封闭的 2D Cap 网格（法线 $vLocalNormal.z = \pm 1$）。
  在 `lining.frag` 中，代码通过 `if (abs(vLocalNormal.z) > 0.5)` 捕捉端面法线，并为端面绘制衬砌分层与 `colorEdge`（0.03 黑色边框）。由于**每一个 1m 实例都带有这两个端面 Cap**，在 100 米长隧道中，GPU 在内部渲染了 100 组垂直于 Z 轴的带黑框切片圆盘，全视角观察时呈现为明显的“一圈圈黑灰环向条纹”。
- **解决方案**：
  1. 在 `TunnelGenerator.ts` 中传入 `totalLength` Uniform。
  2. 在 `lining.vert` 中计算顶点世界坐标 `vWorldPosition` 并传递给片元着色器。
  3. 在 `lining.frag` 中，当 `abs(vLocalNormal.z) > 0.5` 时，判断 `vWorldPosition.z`，**仅保留隧道真正入口（$Z_{world} \approx 0$）与出口（$Z_{world} \approx -totalLength$）的端面 Cap，将中间所有 1m 实例的内部端面全部 `discard;` 废弃**。

#### 根因 1C：实例化几何体端面绝对共面导致的 Z-Fighting 深度冲突
- **根因剖析**：
  在 `TunnelGenerator.ts` 中，`ExtrudeGeometry` 若设置 `depth: 1` 且平移间距 `spacingZ = 1.0`，导致第 $i$ 环后端面与第 $i+1$ 环前端面在坐标 $z = -i$ 处物理空间绝对重叠，产生闪烁黑白莫尔条纹。
- **解决方案**：
  将 `ExtrudeGeometry` 拉伸深度调整为 `depth: 0.999`，配合内部端面 `discard;` 彻底消除 Z-Fighting 深度竞争。

#### 根因 1D：阴影贴图自遮挡（Shadow Acne 斑马纹）
- **根因剖析**：
  `Viewer3D.vue` 中对网格开启 `castShadow` / `receiveShadow`，而自定义 `ShaderMaterial` 采用独立 Blinn-Phong 光照解算未采样阴影贴图，引发自遮挡斑马纹。
- **解决方案**：
  微调 `dirLight.shadow.bias = 0.0001` 与 `shadow.normalBias = 0.05`，并对隧道 Shader 网格关闭无用的阴影投射以平滑曲面。

---

## 2. “旋转角度消失”与“模型空白无法显示”根因剖析

#### 根因 2A：InstancedMesh 视锥体裁剪（Frustum Culling）失效
- 在 `TunnelGenerator.ts` / `Reinforcement.ts` / `RockBoltGenerator.ts` 中，`InstancedMesh` 默认继承单个基准截面的包围球。一旦该基准包围球移出视锥体，导致整体网格被判定不可见而瞬间剔除。
- **解决方案**：显式设置 `this.mesh.frustumCulled = false`。

#### 根因 2B：自定义 Shader 缺失 `#include <common>` 导致编译报错
- 在开启 `logarithmicDepthBuffer: true` 后，ShaderMaterial 缺少基础库 `#include <common>`，引发 `isPerspectiveMatrix` 函数未定义错误，导致 GPU 程序编译失败。
- **解决方案**：在 `lining.vert` 与 `lining.frag` 头部显式添加 `#include <common>` 与 `#include <logdepthbuf_pars_vertex>`/`fragment`。

#### 根因 2C：双面渲染背光面光照为 0 与视角离色
- 旋转到隧道背面时法线反向导致漫反射光变 0。
- **解决方案**：使用 `gl_FrontFacing` 动态翻转背面视角下的 `normal_view`。

---

## Work Breakdown Structure

```
WBS 1.0 着色器程序化条纹彻底清理与材质重构
  ├─ 1.1 修改 lining.frag 端面逻辑：移除 `sin(x * 20.0) * sin(y * 20.0)` 伪影，恢复平滑混凝土材质
  ├─ 1.2 修改 lining.frag 侧壁逻辑：移除 `ringJoint` (sin(z*PI/12)) 24m 黑色环纹
  ├─ 1.3 修改 lining.frag 动态层逻辑：移除 `scanLine` (sin(z*0.5)) 青色扫频条纹
  └─ 1.4 修改 lining.frag 外侧界逻辑：移除 `cracks` (sin(z*2)*cos(r*5)) 裂纹条纹

WBS 2.0 内部环向端面切片剔除与 Z-Fighting 消除
  ├─ 2.1 修改 TunnelGenerator.ts：Uniforms 中增加 `totalLength`，并在 geometry 生成后调用 `computeVertexNormals()`
  ├─ 2.2 修改 lining.vert：输出 `vWorldPosition = (modelMatrix * instanceMatrix * vec4(position, 1.0)).xyz`
  └─ 2.3 修改 lining.frag：当 `abs(vLocalNormal.z) > 0.5` 时，检查 `vWorldPosition.z`，若处于非洞口中间段则执行 `discard;`

WBS 3.0 视锥体剔除与光照阴影偏置微调
  ├─ 3.1 在 TunnelGenerator.ts / Reinforcement.ts / RockBoltGenerator 中设置 `frustumCulled = false`
  ├─ 3.2 优化 Viewer3D.vue 中 `dirLight.shadow.bias` (0.0001) 与 `shadow.normalBias` (0.05)
  └─ 3.3 验证 lining.vert / lining.frag 顶部 `#include <common>` 及对数深度缓冲编译兼容性

WBS 4.0 渲染集成验证与性能测试
  ├─ 4.1 全视角 360° 旋转测试，验证模型不消失、不闪烁
  └─ 4.2 观察端面与侧壁，验证表面平滑均匀无同心弧/“一圈圈”切片条纹/Z-Fighting
```

---

## Acceptance Criteria

1. **条纹彻底消除**：
   - 三维隧道衬砌端面、内壁及外壁呈均匀平滑的物理材质感，无任何正弦网格条纹、纵向环纹、扫描线、“一圈圈”切片黑纹或阴影斑马纹。
   - 仅在隧道起点与终点渲染完整的衬砌洞口端面，中间段切片 100% 剔除。
   - 在 360° 旋转与平移视角时，无 Z-Fighting 深度冲突导致的端面黑白交错闪烁。
2. **视角旋转稳定**：
   - 在 `http://localhost:1420/` 主界面 360° 任意旋转、平移或缩放，隧道模型、锚杆及加固构件稳定可见，无突发隐形/消失。
3. **渲染性能**：
   - 帧率稳定维持在 60 FPS，无 GC 峰值、显存泄漏或 CPU 冗余渲染。
